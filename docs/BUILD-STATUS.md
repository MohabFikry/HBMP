# Mersal HBMP — Build Status & Execution Roadmap

Phases run in dependency order; **one sub-prompt ≈ one reviewable PR** (see `HBMP-Design/claude-code-prompts/00-MASTER-PROMPT-LIST.md`).

## Legend

Six glyphs were in use with nothing defining them, and the prose that tried covered three. `☑` and `✅`
mean the same thing and both are kept only because ~120 rows already carry one or the other; a sweep to
unify them would rewrite most of the file for no gain in truth. What each one means:

- ☑ = shipped, with a dated note recording what and where
- ✅ = shipped (same as ☑; used by the phase-21 rows and the go-live gate table)
- ◐ = in progress — started, landed in part, and the note says which part
- ◪ = partially shipped and BLOCKED on something external; the note names the blocker
- 🟡 = built but not yet load-bearing, or gated on infrastructure that does not exist here yet

`☐` (not started) is DEFINED BY ITS ABSENCE: as of 2026-08-01 no sub-prompt carries it, because every one
that existed has shipped or is tracked as ◐/◪/🟡. The gate refuses a legend entry nothing uses — a vocabulary
that outlives its rows is how a legend stops describing the document — so the glyph returns here on the day a
new phase adds a row that needs it.

`tools/ci/check-build-status.py` enforces this: every glyph used must be defined here, every glyph
defined must be used, and no sub-prompt id may appear twice with two different statuses. That last rule
exists because eleven ids did — one row saying shipped and another saying not started, hundreds of lines
apart, so whichever a reader hit first is what they believed.

## Dependency order
`0 → 0b → 1 → 2 → 2b → 3 → 4 → 5,6 (parallel) → 14 (retrofit) → 7 → 8 → 10 → 10b`; `8b` + `9` run continuously from R0/R1; `11` gates go-live; `12` after 11; `13` after core. `10b` (claims) needs 5/6 fulfillment records, 2b contracts/tariffs, and 7 authorizations. **`14` (branch scoping & clinical sensitivity) is a cross-cutting retrofit of the built services and runs before `7` and `9`** — approvals must be built member-scoped and blind to sensitive results, and the frontend needs the branch switcher + restricted-result state. **`15` (call centre portal) is additive** and needs `3` (appointment engine), `2` (eligibility search), `1` (contacts), `8` (notifications) and `9` (design system/portal catalog) — all complete — so it can run now; it pairs with `14` (the Call Centre role is MemberScoped / all branches, i.e. `BranchUnrestricted` once 14 lands).

## ⇒ New here? Read docs/HANDOFF.md first (full continuation guide).

## Progress

| # | Phase | Sub-prompt | Status | PR/commit |
|---|-------|-----------|--------|-----------|
| 0 | Foundations | 0.1 Monorepo scaffold, CI/CD, dev IaC | ☑ | cac74b3 |
| 0 | Foundations | 0.2 Identity & access (Keycloak, MFA) + `libs/auth` | ☑ | (0.2) |
| 0 | Foundations | 0.3 Audit spine: `audit-service` + `libs/audit-client` | ☑ | (0.3) |
| 0 | Foundations | 0.4 AuthZ engine: `libs/authz` (RBAC+ABAC, row+field) | ☑ | (0.4) |
| 0 | Foundations | 0.5 Service template + Kong gateway + `libs/events` + observability | ☑ | (0.5) |
| 0b | Master Data | 0b.1 `masterdata-service` schema + read/search APIs | ☑ | (0b) |
| 0b | Master Data | 0b.2 Loaders: ingest real ICD-10/CPT/ATC drugs | ☑ | (0b) |
| 0b | Master Data | 0b.3 Interactions/allergens seed + validation endpoints | ◐ | (0b) |
| 1 | Registration | 1.1 `patient-service` | ☑ | (1.1) |
| 1 | Registration | 1.2 `policy-service` | ☑ | (1.2) |
| 1 | Registration | 1.3 `document-service` integration | ☑ | (1.3) |
| 1 | Registration | 1.4 Registration workflow + activation | ☑ | (1.4) |
| 2 | Eligibility | 2.1 `eligibility-service` + cache | ☑ | df91758 |
| 2 | Eligibility | 2.2 Reception search (min-necessary) | ☑ | 4d930ce |
| 2 | Eligibility | 2.3 Visit gating + encounter stub | ☑ | 15ea73e |
| 2b | Provider Network | 2b.1 `provider-service` | ☑ | (2b.1) |
| 2b | Provider Network | 2b.2 Onboarding workflow | ☑ | (2b.2) |
| 2b | Provider Network | 2b.3 Provider isolation (ABAC PO + RLS) | ☑ | (2b.3) |
| 3 | Appointments | 3.1 Appointment domain + slot booking | ☑ | a39b647 |
| 3 | Appointments | 3.2 Reschedule/cancel/no-show | ☑ | 19c0669 |
| 3 | Appointments | 3.3 Queue + reminders hook | ☑ | (3.3) |
| 4 | Clinical EMR | 4.1 `emr-service` + treating-relationship ABAC | ☑ | (4.1) |
| 4 | Clinical EMR | 4.2 `orders-service` + approval routing | ☑ | (4.2) |
| 4 | Clinical EMR | 4.3 `pharmacy-service` Rx + referral creation | ☑ | (4.3) |
| 5 | Lab/Imaging | 5.1 Provider order queue + search | ☑ | 0c88fec |
| 5 | Lab/Imaging | 5.2 Atomic idempotent consume | ☑ | 0c88fec |
| 5 | Lab/Imaging | 5.3 Result upload + routing | ☑ | 0c88fec |
| 6 | Pharmacy | 6.1 Dispensable search | ☑ | ce79500 |
| 6 | Pharmacy | 6.2 Partial dispensing (batch/expiry) | ☑ | ce79500 |
| 6 | Pharmacy | 6.3 Substitution + out-of-stock | ☑ | ce79500 |
| 14 | Branch & Sensitivity | 14.1 `branch` entity + seed the six branches | ☑ | d878aec |
| 14 | Branch & Sensitivity | 14.2 User↔branch assignment + active-branch context (`X-Active-Branch`) | ☑ | 9e2edc4 |
| 14 | Branch & Sensitivity | 14.3 `BranchScope` ABAC + `RowScope` + policy-bundle scope modes | ☑ | a1529ab |
| 14 | Branch & Sensitivity | 14.4 Branch-scope appointments/queue/orders worklists | ☑ | 2bfeedb/25e6f4e |
| 14 | Branch & Sensitivity | 14.5 Practitioner + specialty + doctor↔branch assignment | ☑ | f3e54bc |
| 14 | Branch & Sensitivity | 14.6 Examination type + sensitivity classification | ☑ | 27db413 |
| 14 | Branch & Sensitivity | 14.7 Sensitive-result gating + release-request workflow | ☑ | 25d3154 |
| 14 | Branch & Sensitivity | 14.8 Branch switcher + restricted-result UI | ☑ | 0f5a4bd |
| 15 | Call Centre | 15.1 `callcentre-service` (interaction + caller verification) | ☑ | bdcd50a |
| 15 | Call Centre | 15.2 Member search + min-necessary 360 | ☑ | 324bfc7 |
| 15 | Call Centre | 15.3 Book/reschedule/cancel from the call | ☑ | 533467a |
| 15 | Call Centre | 15.4 Contact updates + referrals/follow-ups | ☑ | eea308c |
| 15 | Call Centre | 15.5 Call Centre portal (frontend) | ☑ | 66feba8 |
| 15 | Call Centre | 15.6 KPIs, notifications + E2E | ☑ | (15.6) |
| 7 | Approvals | 7.1 `approvals-service` + worklist + review | ☑ | 15ba511 |
| 7 | Approvals | 7.2 Decisions + downstream effects | ☑ | a098550 |
| 7 | Approvals | 7.3 Break-glass + SLA/TAT | ☑ | d13a4d5 |
| 8 | Notify+Reporting | 8.1 `notification-service` | ☑ | 35604d3 |
| 8 | Notify+Reporting | 8.2 `reporting-service` KPI read-models | ☑ | 4768621 |
| 8 | Notify+Reporting | 8.3 Executive dashboard contracts | ☑ | 4768621 |
| 8b | Admin Platform | 8b.1 User/role admin + SoD + access review | ☑ | 70e8f17 |
| 8b | Admin Platform | 8b.2 Master-data/template/config admin | ☑ | e761ea3 |
| 8b | Admin Platform | 8b.3 Tenant/provider + break-glass governance | ☑ | 8107dc1 |
| 9 | Frontend | 9.1 Design system in code | ☑ | 9e38a22 |
| 9 | Frontend | 9.2 Role portals + permission routing | ☑ | a282450 |
| 9 | Frontend | 9.3 Flagship screens + `@mersal/contracts` | ☑ | 5197336 |
| 10 | Case + Finance | 10.1 `case-service` + beneficiary-360 | ☑ | c1e0c63 |
| 10 | Case + Finance | 10.2 `finance-service` (no-diagnosis) | ☑ | e23c2fd |
| 10 | Case + Finance | 10.3 Case + Finance portals | ☑ | (10.3) |
| 10b | Claims Mgmt | 10b.1 `claims-service` + auto-derived claims (no double-billing) | ☑ | 0278c0b |
| 10b | Claims Mgmt | 10b.2 Batching + batch lifecycle (single-open-batch) | ☑ | e68a64e |
| 10b | Claims Mgmt | 10b.3 Automated pre-adjudication (9-step, all reasons) | ☑ | 2323e2f |
| 10b | Claims Mgmt | 10b.4 Officer review + line-level decisions (SoD, dual control) | ☑ | 735021a |
| 10b | Claims Mgmt | 10b.5 Provider-submitted claims + document matching | ☑ | 6851e54 |
| 10b | Claims Mgmt | 10b.6 Beneficiary reimbursement + OCR (assistive) | ◐ | fc2d76a; **downgraded 2026-07-27 by 18.C2 (audit R2 W8)**. The reimbursement WORKFLOW is complete and correct — intake, document matching, dual control, the ManualAssessment fallback, and the rule that a reimbursement can never auto-match without an authorized underlying service. What is NOT built is the two seams that would let it auto-process at all: `NullOcrProvider` extracts nothing (no in-cluster Tesseract ara+eng) and `NoAuthorizedServiceResolver` finds nothing (no approvals/orders/pharmacy authorization query). Both defaults are deliberately empty rather than optimistic — they route every request to a human instead of inventing an extraction or an authorization — so the service is SAFE, just not automatic. Every reimbursement today is assessed manually. Ticking this ☑ claimed a capability that structurally could not run; the seams are correct, the wiring is the remaining work. |
| 10b | Claims Mgmt | 10b.7 Reconciliation + append-only adjustments | ☑ | 2bac316 |
| 10b | Claims Mgmt | 10b.8 Settlement advice + exports (no payment execution) | ☑ | acf86eb |
| 10b | Claims Mgmt | 10b.9 Appeals + claims KPIs | ☑ | 7dc6ea7 |
| 11 | Hardening/NFR | 11.1 Perf/scale harness + baseline + index/cache ADR | ☑ | cac6e7a |
| 11 | Hardening/NFR | 11.2 STRIDE + CI security gates + OWASP API Top10 + sign-off | ☑ | c9d20c7 |
| 11 | Hardening/NFR | 11.3 Fleet metrics + dashboards/alerts + DR/restore + runbooks | ☑ | eb9c339 |
| 16 | Audit Remediation | 16.1 Secrets purge + doc reconciliation (C2) | ☑ | hbmp_app pw rotated + purged from git history; base-appsettings connstrings removed; ~14 code fallbacks fail-fast; gitleaks prose-password rule; doc reconciliation |
| 16 | Audit Remediation | 16.2 Durable transactional outbox (C1) | ☑ | EfOutbox + FOR UPDATE SKIP LOCKED reader + `outbox_message` per schema (16 svcs, live-applied) + env-driven default-durable + ADR-0013; 726 tests green |
| 16 | Audit Remediation | 16.3 Admin authz middleware + MFA + idempotency (C3) | ☑ | **CLOSED in Phase 17** (done once, on the new issuer): MFA enforcement = 17.3 (TOTP + amr → MfaEvaluator gates protected scopes); in-app user/role/scope admin = 17.4 (bearer admin-scope + MFA + hash-chained audit). C3 closed. |
| 16 | Audit Remediation | 16.4 RLS everywhere as `hbmp_app` (H1) | ☑ | full multi-tenant retrofit — `tenant_id` on ~45 tables + history twins; shared `libs/data` binder + stamping interceptor; all 13 svcs → NOBYPASSRLS hbmp_app + ENABLE/FORCE RLS (live-applied); per-svc 2-role isolation tests; ADR-0011 |
| 16 | Audit Remediation | 16.5 Kong JWT + missing routes + transport hardening (H3/H8) | ◪ | non-identity portion DONE: +7 missing route groups; `/beneficiaries` collision fixed via regex routes→emr/eligibility/document (live-verified); route-coverage CI guard (`tools/ci/check-kong-route-coverage.py`); CORS If-Match/ETag/credentials:false; `RequireHttpsMetadata:false` base→Development; shared `UseHbmpTransportSecurity` (HSTS/redirect/fwd-headers, non-dev); all ports→127.0.0.1 except Kong; OpenSearch secure-by-default; hello svc removed. **Global edge JWT RESOLVED in 17.6**: issuer in-house with stable JWKS, Kong routes `/connect`+`/.well-known`→issuer; JWKS validation enforced service-level in Tier 1 (community Kong can't discover rotating RS256 JWKS) + openid-connect plugin for Tier 2/3. H3 closed. Stale admin/masterdata rebuilt→Phase-14 endpoints live. |
| 16 | Audit Remediation | 16.6 Approvals sensitivity + break-glass runtime + FieldProjector + document fixes (H2/H4/H5/H9 + mediums) | ☑ | 8/8 + emr aggregation. H4 SensitiveDisclosure gate in approvals review + emr /clinical-context oversight endpoint built; H5 HttpBreakGlassProvider (fail-closed, admin /break-glass/active) in 15 svcs, sign-off corrected; H9 DocumentPolicies row-scope+audit+read/write split; H2 FieldProjector on emr clinical-context (patient identity/contact deferred — matrix gap); pharmacy SQL constant, patient PATCH audit, masterdata durable-outbox+screening audit, GateResults dedup (13 gates). ADRs 0010/0012/0014. Tests: DocumentPolicies(3)/ApprovalsSensitivity(3)/HttpBreakGlass(5)/FieldProjector(+3). Release 0/0. |
| 16 | Audit Remediation | 16.7 Live frontend wiring (scopes/roles/env/errors/i18n) (H6/H10) | ☑ | identity slice: live KC roles/users/scopes (call_center + claims_officer) + fail-closed role map (no-portal page) + provision-identity.sh. **errors DONE**: `http.ts` parses RFC 7807 problem+json → `ApiError.problem`/`.reason`; `AsyncSection` shows kind-specific headline + server `detail` (5 http.test.ts). **env DONE**: `apps/web/.env.example` documents VITE_* (LIVE/API_BASE/OIDC). **idempotency** verified already-done (HttpApiClient sends Idempotency-Key on every mutation). **i18n leak-audit DONE**: swept screens/shell/pages for hardcoded English in JSX text + aria-label/placeholder/title props — only leak was DoctorEncounter vitals labels (BP/HR/Temp/Ht/Wt), now bilingual via `t(S.*)`. **16.7 COMPLETE.** |
| 16 | Audit Remediation | 16.8 CI & test integrity (skippable tests, PG service, coverage, ESLint, OpenAPI) (H7) | ☑ | **COMPLETE.** **skippable-tests** (`7b9b665`): env-gated DB tests → `[SkippableFact]`. **backend-ci.yml** (`d431e26`): postgres:16 + warnings-as-errors build + `tools/ci/apply-migrations.sh` (hbmp_app + all 77 migrations to clean DB) + `print-test-db-env.sh` (11 integration + 13 RLS-pair + events vars → suites RUN) + `coverage-gate.sh` (domain floor `COVERAGE_MIN_DOMAIN`=55, measured 58.1%, target 80%); verified on scratch DB → 18 schemas/155 tables, full solution **0-fail 0-skip**. **ESLint** (`d6cc560`): `eslint.config.mjs` (ESLint 9 flat + typescript-eslint + react-hooks) + `lint:eslint` + frontend-ci job; workspace error-clean. **OpenAPI** (`generate-openapi.sh` + backend-ci step): Swashbuckle CLI emits every service's spec offline, fails on any generation error; verified **all 18 services** generate (audit uses the CI DB for its startup migration). Tool manifest `.config/dotnet-tools.json` |
| 16 | Audit Remediation | 16.9 Small items + ADRs 0010–0014 | ☑ | **COMPLETE.** ADRs 0010–0014 all exist. `hello` retired. CallCentre i18n enum leak fixed. **problem+json**: ~104 bare `NotFound()`→`Results.Problem` (41 files/14 svcs) + the 39 anonymous `new { error }` bodies in admin/masterdata → shared `ProblemResults` helper (Invalid/Conflict/Unprocessable; machine code in a `code` extension) — twin of GateResults (`4b63893`). **migrations-copy-to-output** on all 18 Infra projects. **idempotency** (`527926d`): Idempotency-Key on finance settlement-generate (RLS-free `finance.processed_request`, migration 0003, concurrent-key replay); the other admin/document/reporting mutations are naturally idempotent (projections dedupe on event-id, grants unique-constraint-guarded, transitions guard-checked) → no ledger needed. **eligibility consumer health-check** (`f018945`): first real `IHealthCheck` — `/health/ready` reports Healthy/Degraded on the RabbitMQ connection (thread-safe `ConsumerHealthState`). **web Dockerfile** (`e936760`): multi-stage node+pnpm→nginx SPA (build step validated; Docker daemon absent here). **FE**: CallCentre reschedule wired + history load-error distinct from empty (`f0a43a2`, +3 tests); finance export operator-date-window replaces hardcoded range (`6d90ae3`, +2 tests); RestrictedResultCard/danger text moved to real theme tokens `--surface-2`/`--st-bad-fg` — fixes a dark-mode bug (`9d9502b`). Web suite 53/53, eslint 0-error; backend suites green (admin 70, eligibility 30, finance 14). **Gate dedup**: the shared problem-shape is already in GateResults (16.6); the residual `CheckAsync` bodies genuinely vary per gate (purpose/resourceId/ProviderId/treating-set/super_admin-tenant) → a base class would be a leaky abstraction, deliberately NOT forced. **Scoped follow-up**: SPA ETag/If-Match/412 *opt-in* — the concurrency GUARD exists + is safe server-side (412 mapping + IfMatch on emr transitions, safe-by-default when absent); the SPA opt-in needs `RowVersion` surfaced on `AppointmentResponse`/`AppointmentRow`, a contract change on the **17.0 frozen-contract boundary** → do it there, not twice. |
| 17 | In-App Identity | 17.0 ADR-0015 + frozen token-contract snapshot | ☑ | **COMPLETE.** `docs/adr/0015-in-app-identity-openiddict.md` (Keycloak → ASP.NET Identity + OpenIddict; why-not-keep-KC; greenfield re-provision = no hash migration; reversible until 17.6 cutover; 17.1→17.6 sequence). `docs/security/token-contract.md` **freezes** the exact claims `libs/auth`+SPA consume (envelope iss/aud/RS256/JWKS; claims sub/`roles` flat lower-case/`scope` space-delimited/`tenant_id`/`provider_id`/`sid`/`src_ip`/`amr`/`acr`; frozen 17-role vocab; enforced scope set; `MfaEvaluator` signal contract; 300s/1800s/36000s lifetimes; PKCE+CC+refresh) + a 17.2 issuer conformance checklist. **ETag/If-Match opt-in landed here** (`7dee615`, the follow-up scoped to this frozen-contract boundary): emr `AppointmentResponse` now carries `RowVersion` (xmin) so the LIST board has a per-row token; SPA `zAppointmentRow.rowVersion`+`postRaw` If-Match, reception check-in echoes it and a 412 shows "changed—refresh"+reloads instead of double-acting. emr appt suite 29/29 (real PG); web 55/55; tsc clean. Note for 17.1: `claims_officer` is in the realm+SPA but missing from `RoleCatalog.Tiers` → add at T2. |
| 17 | In-App Identity | 17.1 ASP.NET Identity store + roles/scopes as data | ☑ | **COMPLETE** (`7ca5149`). New **identity-service** (Domain/Infrastructure/Api/Tests, in the sln + apply-migrations glob). ASP.NET Core Identity EF store over the `identity` schema: `ApplicationUser`(IdentityUser<Guid> + `tenant_id`/`provider_id`/`display_name`/`is_active`), `ApplicationRole`(+tier). **Roles/scopes as DATA**: `identity.scope` catalog + `identity.role_scope` matrix seeded in `0001_identity.sql` — **17 roles, 51 scopes, 88 grants** (matrix mirrors `permissions.ts`/11-permission-matrix; min-necessary hard rules hold structurally). `RoleScopeResolver` (roles→scope union) is the seam the 17.2 issuer uses for the `scope` claim. Identity core deliberately **not tenant-RLS** (`0002_identity_grants.sql`): the issuer authenticates pre-tenant-context, so `tenant_id` is a claim source not a row filter; hbmp_app grant only. Hand-authored DDL matches the EF model (proven by round-trip test). Api = thin store host + read-only `/identity/{roles,scopes,effective-scopes}`. Tests env-gated `IDENTITY_TEST_DB` **6/6** on real PG. Closed the drift the frozen contract flagged: `claims_officer`(T2) added to admin `RoleCatalog` (admin 31/31). Full sln 0-warning. TOTP token providers deferred → 17.3 (framework-layer). |
| 17 | In-App Identity | 17.2 OpenIddict issuer (PKCE/CC/refresh, frozen claims) | ☑ | **COMPLETE** (`19ec864`). OpenIddict 5.8 on the identity DbContext/schema. Server: auth-code+PKCE / client-credentials / refresh; **RS256, access-token encryption DISABLED** (plain signed JWT → JWKS validation); aud=`hbmp-api`; 300s/10h; ephemeral keys in dev (OpenBao at 17.6). `TokenPrincipalFactory` emits the frozen claims — sub, **flat lower-case `roles`**, space-delimited `scope` (via `RoleScopeResolver`, granted = user∩requested), `tenant_id`, `provider_id`, `amr` — with access-token destinations. `ConnectEndpoints` /connect/{authorize,token,userinfo,logout} + a **minimal password sign-in** (replaced by 17.3 UI+2FA). `ClientSeeder` seeds `hbmp-web`(public PKCE)+`hbmp-services`(confidential CC)+scope catalog. `0003_openiddict.sql` = OpenIddict's 4 tables (DDL **generated** from the EF model → exact match, idempotent, hbmp_app grants). **`IssuerContractConformanceTests`** mint REAL tokens (CC + full auth-code+PKCE user flow) and validate them through libs/auth's exact path (discovery→JWKS→RS256→aud→HbmpPrincipal): asserts flat roles, min-necessary scope (finance→finance:read not audit:read), tenant/provider, single-factor amr. **9/9** real PG; full sln 0-warning. |
| 17 | In-App Identity | 17.3 In-app login pages + TOTP 2FA + recovery + step-up | ☑ | **COMPLETE** (`8138449`). Accessible bilingual (EN+Arabic RTL) HTML served by identity-service (`AccountPages`): password login, TOTP challenge, authenticator enrolment (shared key + otpauth URI; QR SPA-side at 17.5), one-time recovery codes. **amr stamping**: a completed factor re-issues the app cookie with explicit amr (pwd; pwd+otp after 2FA), carried onto the token → `MfaEvaluator` gates protected scopes (this is C3's MFA half on the new issuer). Login routes `RequiresTwoFactor`→/connect/2fa; recovery-code sign-in supported. Test: full password→TOTP(RFC-6238 computed)→auth-code+PKCE asserts token amr=otp + MfaSatisfied. Identity **10/10** real PG; full sln 0-warning. |
| 17 | In-App Identity | 17.4 In-app user/role/scope admin on the real store (closes C3) | ☑ | **COMPLETE** (`14b46a6`) — **closes C3** (management half; MFA half was 17.3). `AdminEndpoints` /identity/admin/*: create user, set roles, deactivate (soft), reset password, edit role→scope matrix. Guard = bearer **admin scope (read/write) + MFA session** (OpenIddict local validation + MfaEvaluator); admin is step-up-only. Each mutation **hash-chain audited** — identity-service now runs the durable outbox (`AddOutbox` on the context + `9000_outbox.sql`) + AddHbmpEvents/DurableOutbox/Relay + AddHbmpAuditClient. No hard deletes (soft is_active=false + security-stamp bump). Tests: MFA admin creates+role-assigns (persisted); non-MFA→403; MFA-without-admin-scope→403. Shared `TestFlow` OIDC harness. Identity **13/13** real PG; full sln 0-warning. |
| 17 | In-App Identity | 17.5 SPA rewire to new issuer (closes H6 by design) | ☑ | **COMPLETE** (`9f10fbd`) — **closes H6**. SPA authenticates against identity-service (OpenIddict) not Keycloak: oidcClient endpoints → `/connect/{authorize,token,logout}`; roles read from the FLAT lower-case `roles` claim (array-or-single) not nested `realm_access.roles`; fail-closed mapping preserved (unmapped role→null→no portal). config: authority→in-app issuer, scope +`offline_access` −`hello:read` (unregistered scope would fail authorize); `roleFromRealmRoles`→`roleFromClaimRoles` (single source of truth = app role store, ends realm duplication). Web 55/55, tsc clean. |
| 17 | In-App Identity | 17.6 Cutover, Keycloak retirement, doc truth | ☑ | **COMPLETE.** All 15 services' `Auth:Authority`→`http://identity-service:8080` (appsettings + compose env), `ValidIssuers[0]`→`http://localhost:8090` (split-horizon). Compose: **Keycloak removed**, `identity-service` added (port 8090, `services/identity/Dockerfile`); `.env` KEYCLOAK_*→`IDENTITY_SERVICE_SECRET`; postgres-init drops the keycloak DB. Kong routes `/connect/*`+`/.well-known/*`→issuer; **edge JWT (H3)**: JWKS validation stays service-level in Tier 1 (community Kong can't do JWKS discovery w/ rotating RS256) + openid-connect plugin noted for Tier 2/3 — issuer now has a stable in-house JWKS. `UserSeeder` seeds demo staff (username=role, dev-only) so the live portals keep working post-cutover. `infra/keycloak/*` retired-for-reference — **removed 2026-07-29**: the realm + scope catalogue are archived under `docs/security/archive/`, the provisioning script and README deleted, and `libs/auth` no longer reads the retired issuer's nested `realm_access`/`resource_access` role shapes. Docs: CLAUDE.md Identity line, ADR-0015 cutover update. **Prod follow-up:** persistent OpenBao RS256 keys (dev uses ephemeral) → Phase 12. |
| 12 | Migration/Go-live | 12.1 Migration pipelines · 12.2 Release mgmt · 12.3 Pilot + hypercare | ☑ | **COMPLETE.** **12.1** (`7a54813`): `tools/migration` toolkit (`mersal-migrate`) — staging→validate→map→load→reconcile, versioned config, provenance + hash-chain audit per row; idempotent upsert-on-natural-key + rollback-by-batch **proven on real PG** (`PostgresSink`, env-gated `MIGRATION_TEST_DB`); STREAM C beneficiaries = identifier normalization (national ID/UNHCR/passport, Arabic-Indic folding) + deterministic+Jaro-Winkler dedupe, low/med confidence **never auto-merged** → review queue held; STREAM B provider isolation verifier; STREAM A master-data validate; reconciliation must balance; migration DPIA (`docs/compliance/migration-dpia.md`). Tests **26/26** (25 unit + real-PG). **12.2** (`4422e9a`): gated `release.yml` dev→QA→staging→prod (Semgrep+Trivy+cosign+Harbor, no `:latest`, env approvals); `check-migration-compat.py` expand/contract gate (selftest PASS, **all 83 migrations clean**, wired into backend-ci); `check-golive-gates.py` consumes phase-11+12.1 evidence (`--require-signed` correctly blocks prod until DPO/Security sign); Argo Rollouts canary + Linkerd + SLO-analysis auto-rollback (`infra/helm/rollout/`); `docs/compliance/go-live-gate.md`. **12.3**: persistent issuer RS256 keys from OpenBao (`IssuerKeys`, fail-fast, no dev-cert fallback, deterministic kid — closes the Phase-17 prod-key follow-up; identity **17/17** real-PG); cutover + fallback/paper + hypercare runbooks + incident register; bilingual AR/EN role quick-starts + provider onboarding kit (`docs/training/`); pilot go-live Grafana dashboard + hypercare exit criteria. Full sln **0 warn / 0 err**. **Infra-execution items (prod migration run, live canary drill, sign-offs, OpenBao provisioning) are operational gates 🟡** per the phase-11 pattern. |
| 13 | Interoperability | 13.1 FHIR R4 façade · 13.2 Adapters/ACL · 13.3 Interop test harness | ☑ | **COMPLETE.** **13.1** (`4fccd1c`): `interop-service` (bounded context `interop`, base `/fhir/r4`) — an ADAPTER over the core (owns no clinical data; reads/writes native `/api/v1` under the caller's bearer; maps to/from FHIR R4; stores only mapping/idempotency metadata; ADR-0016). Read+search all nine resources (Patient/Coverage/ServiceRequest+referral/MedicationRequest/DiagnosticReport/Encounter/Condition/Observation/AllergyIntolerance) + §12.1 status maps + Bundle/OperationOutcome; safe creates translate to native commands (If-None-Exist idempotent, real-PG); derived resources reject POST. **Min-necessary two layers, never a bypass**: `InteropPolicies` (libs/authz) role+SMART-scope+tenant per interaction (every deny audited; Finance/Reception/Pharmacy/Lab absent from Condition/DiagnosticReport reads) + owning-service field/record ABAC under the bearer. Every interaction hash-chain audited. Hand-rolled R4 JSON (minimal-dep; Firely swappable behind 13.3 harness). **13.2** (`11dbf50`): integration adapters + **anti-corruption layer** — IInbound/IOutbound adapters, IExternalPartnerRegistry, OCR/Arabic-NLP no-op hooks; a REAL FHIR referral ACL both ways + UNHCR/gov/insurer/HL7v2 stubs; **DpiaGate** (Disabled until DPIA sign-off AND data-sharing agreement — runtime `TryEnable` + DB CHECK + audited); registry/ingestion over `interop` schema (5 partners seeded Disabled), governance API `/interop/integration/*`. Inbound quarantines malformed/disabled; never writes core tables. **13.3**: `R4Conformance` structural validator + conformance tests (representative resources validate; CapabilityStatement == implemented interactions; min-necessary parity); consumer-driven **contract tests** (committed `*.contract.json` fixtures; adapter drift fails CI); **`tools/ci/check-integration-dpia.py`** DPIA gate (committed register `docs/compliance/integration-register.md`; selftest + enforce; wired into backend-ci) + `INTEROP_TEST_DB` in `print-test-db-env.sh`. Interop **39/39** (units + endpoint E2E + real-PG idempotency/registry); authz **43/43**; full sln **0 warn / 0 err**. **Operational gate 🟡**: real partner wiring (`HttpFhirDataSource` to live siblings, partner endpoints) verified in staging; every integration stays DPIA-gated. **This was the last phase before v1 roadmap.** |

| 18 | E2E Remediation | 18.A1 Coverage limits actually bind (X1) | ☑ | 2026-07-27 — policy-service consumes `orders.events`/`pharmacy.events`; `BenefitConsumptionApplier` is the sole writer of `consumed_value` (atomic guarded UPDATE, UNIQUE `source_ref`, symmetric reversal); ledger migration `0003`; producers now carry tenant/beneficiary/category/serviceDate. Policy 24/24 real-PG, full sln 0 failures |
| 18 | E2E Remediation | 18.A2 Claims money layer: rollups, cap, quantity (X2/X3/X8) | ☑ | 2026-07-27 — canonical `BatchRollupService` used by decision/adjustment/batch paths (adjustments survive → Decided and the freeze); cap = `Math.Min(billed, tariff)` on Approve/PartiallyApprove/Adjust and clamped in `Apply`; `ContractPrice = tariff × quantity`; adjustments no longer rewrite the decided `allowed_amount` (that double-counted against `total_adjusted`). Claims 166/166 real-PG |
| 18 | E2E Remediation | 18.A3 Concurrency + temporal correctness (X7/X9/X10) | ☑ | 2026-07-27 — guarded compare-and-set roll-up in both executors; service-aware eligibility cache key; period-anchored limit reset (mig 0004); FOR UPDATE SKIP LOCKED waitlist promotion + single-transaction cancel/no-show; payload-bound idempotency keys (`libs/events/IdempotencyKeyRules`, migs orders/0007 + pharmacy/0004); new `libs/time` Africa-Cairo `IBusinessCalendar` + architecture gate banning bare `UtcNow`. 955 tests green |
| 18 | E2E Remediation | 18.A4 Report-access + state-machine conformance test | ☑ | 2026-07-27 — report-access TTL cap + `ReportAccessWorkflow` with the /review and /supply-info transitions (3 states were unreachable, InfoRequested was a dead end) + request follows its grant; settlement SoD (creator ≠ releaser) + frozen-snapshot regeneration; adjustment dual-control TOCTOU closed with a scope row lock; fully-voided claim → Void; policy reactivation restores coverage. New `libs/spec-conformance` parses `23-state-machines.md` and gates 7 machines both directions — found and fixed 4 real divergences |
| 18 | E2E Remediation | 18.B1 Secrets regression + fail-fast (X4/X5) | ☑ | 2026-07-27 — MinIO keys purged from document AND audit (WORM) appsettings + startup fail-fast; identity m2m secret fail-fast, scope-limited to `ServiceScopes`, and RECONCILED so rotation applies; demo password to config with no default; 3 new gitleaks rules (config values, `??` fallbacks, URI-embedded credentials) |
| 18 | E2E Remediation | 18.B2 Finish RLS: no superuser, no deny-all trap (X6/S1/S2) | ☑ | 2026-07-27 — binder→connection→test, in that order, for claims/callcentre/admin/interop; provider flipped (its green test proved the policy, not the deployment). ALL 7 superuser connection strings gone: identity/masterdata/provider/admin/claims/callcentre → `hbmp_app`, audit → its OWN `hbmp_audit` role (member of `hbmp_audit_writer` only, so the shared role cannot read the audit trail). callcentre gained its first RLS DDL (0003); interop's fail-OPEN policy closed and extended to integration_partner + inbound_staging (0003). FOUND: admin's four migrations carried the same fail-open escape as interop (0005 closes them, adds the missing FORCE) — on the schema that owns role_binding and break_glass_grant. FOUND: eligibility's consumer stamped a hardcoded SoleTenantId → tenant now comes from the envelope (new `EventTenant`), 28 publishers enriched, unattributable messages dead-lettered. FOUND: admin write paths silently substituted the caller's tenant for a mismatched body tenant → 403. New `libs/data/Tests` gates the deployment posture (no POSTGRES_USER, known roles only, no new fail-open policy, allowlist can't go stale). 5×2-role RlsIsolationTests (A-only/B-only/no-GUC-zero + WITH CHECK). Full solution 28/28, 0 failures, 0 warnings |
| 18 | E2E Remediation | 18.B3 Admin/issuer authz, CSRF, scope integrity (S3-S9) | ☑ | 2026-07-27 — S3 admin + identity-admin gated at the ROUTE TABLE (read/write/break-glass split), asserted by route-metadata tests so a future ungated route fails the build rather than relying on every handler calling its gate. S4 antiforgery on all three rendered forms (`POST /connect/enroll-2fa` let a cross-site post register the ATTACKER's authenticator as the victim's 2nd factor) + Secure/SameSite=Strict on the four issuer cookies. S5 scope grant is now the intersection, `invalid_scope` when empty — and FOUND that the intersection also dropped `offline_access`, so no refresh token was ever minted (half of W1). S6 patient:read split from patient:write; reads go through BeneficiaryReadGuard (engine + tenant + PHI-read audit + FieldProjector) with new pii/contact field classes — reception gets a phone number, not a UNHCR number. S7 UseHbmpTransportSecurity first in identity. S8 Kong OSS `jwt` plugin globally against the registered issuer key (OIDC routes exempt). S9 per-IP 10/min credential + 60/min token limits. FOUND: new ScopeIntegrityTests read the policy bundles against the identity seed and reported **141 rule/role pairs no token could satisfy** — including the whole claims decision surface, medical_director's oversight, and break-glass for every clinician. 135 closed by identity 0004+0005; 6 missing ROLES declared as an open product decision. Full solution 28/28, 0 failures, 0 warnings, 1070 tests |
| 18 | E2E Remediation | 18.C1 Restore live sessions + branch scoping (W1/W2) | ☑ | 2026-07-27 — W1: the SPA requested `offline_access` and discarded the refresh token, so a session was ONE 5-minute access token. It never looked broken: the timeout logic tracked its own clock and `keepAlive()` moved that clock forward without getting a new credential, so the portal showed a healthy session while every request 401'd. Refresh token persisted (per-tab sessionStorage), rotation handled, single-flight so two tabs cannot make a benign race look like a replay, silent renew at T-60s, and `keepAlive` now buys a REAL token. A reload with an expired access token renews instead of redirecting to login. Note 18.B3 fixed the OTHER half: the scope intersection was dropping `offline_access`, so no refresh token was being issued in the first place. W2: `X-Active-Branch` was never sent — Phase 14 built the ABAC condition, the resolver, the contract and the switcher, and the header was the missing link, so choosing a branch changed React state and nothing else. Header now rides every request via a small activeBranch module, Kong CORS carries it on BOTH `headers` and `exposed_headers` (missing from the first blocks the preflight; missing from the second hides the echo), and `/me/active-branch` echoes the branch the server actually honoured so the switcher shows the confirmed value, not its own optimistic one. Frontend 10 files / 63 tests, tsc clean; backend 28/28, 1070 passed, 0 warnings |
| 18 | E2E Remediation | 18.C2 Make finished features reachable (W3-W8) | ☑ | 2026-07-27 — W3 interop/FHIR: all of phase 13 shipped with no compose block and no Kong route; added both (/fhir + /interop), with /fhir/r4/metadata explicitly exempt from the 18.B3 edge JWT plugin because a FHIR client fetches the CapabilityStatement BEFORE it has a token. Also gated interop's own groups (the S3 pattern the audit said was replicated there and 18.B3 missed). W4 report-access: the 14.7 workflow had no way to SEE a request — decisions took an id nothing displayed, so the sensitive gate was permanent-deny and requests sat until they expired. Added GET /report-access-requests (clinical-free, director sees all / doctor sees their own), an approver screen on BOTH the Doctor and Medical Director portals, and an hourly ReportAccessExpirySweeper (the sweep endpoint existed and nothing called it, so grants stayed Active for ever). W5 identity admin: /identity routed through Kong; AdminUsers now reads the identity STORE — including whether an account has a second factor, which no screen anywhere showed — and AdminPolicies renders the live role→scope matrix from /identity/effective-scopes, the same seam the issuer uses. W6 the SPA is in compose. W7 FR-BRN-026/027: new IPractitionerBranchDirectory; provider-service's serves-branch probe has existed since 14.5 and emr never called it. 422 at BOTH gates (a walk-in is slotless and never passes through availability). Unknown ≠ no: an unreachable provider-service must not stop a clinic booking patients. W8 10b.6 downgraded ☑→◐ — the honest option the prompt allows; the workflow is complete, the OCR + authorization-resolver seams are not, and the tick claimed a capability that structurally could not run. New ReachabilityTests read compose + kong directly, so a service without a route fails the build. Backend 28/28, 1079 passed, 0 warnings; frontend 10 files / 63 tests, tsc clean |
| 18 | E2E Remediation | 18.D1 Stop silent failures + undifferentiated errors (U1/U2/E3/E4) | ☑ | 2026-07-27 — U1: four write flows (result upload, beneficiary registration, provider creation, manual authorization) caught every failure as `catch { setStatus("idle") }` AND sent no idempotency key. The spinner stopped, nothing appeared, the form still held the operator's typing — which reads as 'nothing happened', so they press the button again and create a second clinical record. New `useWrite` hook mints the key ONCE per form instance and rotates it only after a CONFIRMED success (per-click keys make every retry a fresh write; a never-rotating key makes the next genuine submission a replay). U2: one bilingual `writeErrorMessage(ApiError)` — 401 re-auth, 403 access changed, 404 gone, 409 already actioned, 412 changed under you, 422 field errors, 429 slow down, 5xx may-not-have-saved, plus network and schema — each with an `action` (retry/reload/reauth/stop) and a `possiblyApplied` flag, so the UI never invites a blind retry of a write that may have landed. E3: reception check-in painted its green chip from a local 'we sent it' set; now the board reloads and the chip derives from a server `checkedIn` flag (distinct from `!checkInEligible`, which is also false for cancelled and no-show). E4: new `ConfirmAction` (Radix, focus-trapped, bilingual — `window.confirm` cannot be translated and the browser supplies OK/Cancel in ITS language) with TYPED confirmation on dispense; a yes/no in a repetitive queue becomes muscle memory within a shift. Dispense quantities now default to 0, not the full remaining amount. Frontend 11 files / 74 tests, tsc clean; backend 28/28, 0 warnings |
| 18 | E2E Remediation | 18.D2 Correct, safe and reachable UI (U3/U4/U5/U7/U8/U9) | ☑ | 2026-07-27 — U3 SAFETY: the Call Centre 360 rendered `<StatusChip kind="ok" label={status} />` — LABEL from the server, COLOUR hardcoded green — so a Suspended, Expired or Blocked member displayed as eligible with the real word in small text beside it. An agent under call pressure reads the colour. New shared statusLabels maps kind AND label from the same value, in both languages, with unknown states rendering neutral rather than confidently green; the raw enums for call outcome, identifier type and appointment type went with it. U4: `nav.mrs-rail { display: none }` below 760px with nothing in its place — on a phone the app had NO navigation, so whatever screen you landed on was the only one reachable. The NavRail docstring already promised a bottom tab bar; now it exists (≥44px targets, safe-area inset, RTL-logical, no new markup or focus trap). U5: the app-bar search was bound to nothing and `/` focused it — a shortcut training users toward a control that does nothing. Both removed; the palette is 18.F2. U7: new `useFormat` pins display to Africa/Cairo and the APP locale. Bare `toLocaleTimeString` formats in the MACHINE zone, so a UTC-set clinic PC rendered a 09:00 appointment as 07:00 — silently, and by a different amount across DST. 15 call sites converted, money moved from pre-formatted `EGP …` strings to raw numbers in the contracts (a formatted string cannot be re-localised, summed or sorted), and an ESLint `no-restricted-syntax` rule bans the next one at authoring time. U8/U9: `--radius-N`, `--brand-teal`, `--status-bad-fg` were never defined so every rule silently fell back; renamed to the real tokens with the legacy hex fallbacks dropped, and a test now asserts every `var()` in app.css resolves. The avatar surface moved off `--brand` (marked "decorative only", ~2.2:1 under white text) to `--accent`. Frontend 12 files / 85 tests, tsc clean, eslint 0 errors; backend 28/28, 0 warnings |
| 18 | E2E Remediation | 18.D3 Accessibility conformance sweep (U6/U10) | ☑ | 2026-07-27 — The gate covered 3 of ~45 routes, English only, light theme only, with `color-contrast` disabled. Two whole defect classes were invisible by construction: RTL mirroring and anything theme-specific. New table-driven suite runs axe over EVERY route in the portal catalog × {en, ar} × {light, dark} — driven off the catalog so a route added later is covered without anyone remembering — plus keyboard-operability checks on the four high-volume worklists. New Playwright job (a11y-contrast.spec.ts + CI) runs the same axe engine in Chromium against the BUILT bundle for contrast: jsdom has no layout or paint, so the rule reports nothing there whatever the palette does — leaving it 'enabled' in jsdom would be the real dodge. Fixes: 8 kv-grid blocks became real `<dl>`s (a dt/dd pair outside a list is announced as two unrelated fragments, so 'Plan: Basic' reads as 'Plan' … 'Basic'); interactive worklists became role=grid/gridcell (aria-selected on a tr in a plain table is silently IGNORED, so no row was ever announced as current) and ApprovalsWorklist + CaseManager got the onSelect they were missing — rows were focusable and Enter did nothing; PharmacyDispense quantity inputs now carry the DRUG NAME in the label (five identical 'Quantity to dispense' fields is a medication-error risk, not a nicety); ExecutiveDashboard renders its data table ALWAYS in an sr-only wrapper (it was behind a default-OFF toggle with the chart aria-hidden, so the default state for a screen-reader user was an empty region); skip link moved to inset-inline-start (physical `left` mirrors wrong in Arabic); scroll-margin-block-start stops sticky headers covering the focused row; DataTable 'Loading…'/'No results' and KpiCard's delta direction now follow the app language; .mrs-sm/.mrs-seg raised 32/38px → 44px, the project's own bar, and `size="sm"` is used exactly where it matters most — dense worklist rows. The new sweep caught one of my own over-corrections (a form reusing kv-grid for layout, wrongly made a `<dl>`). Frontend 13 files / 94 tests, tsc clean, 0 eslint errors; backend 28/28, 0 warnings |
| 18 | E2E Remediation | 18.E1 Make the gates enforce what they claim (Q1/Q2/Q4) | ☑ | 2026-07-27 — These were not missing checks; they were checks that existed and did not run, ran against the wrong scope, or announced a threshold they never applied — which is worse, because the build is green and the dashboard says 'gated'. Q1: check-kong-route-coverage.py was written in 16.8, committed, and NEVER invoked — and inspected only /api/v1, so it was structurally blind to the gap that shipped as W3 (the whole FHIR facade). Now wired into CI and extended to /fhir, /interop, /identity, /connect, /.well-known; verified it fails on a removed route. CI split-brain resolved: both pipelines now delegate to the same tools/ci/* scripts (the GitLab test job printed 'Coverage threshold: 80%' and enforced nothing), ADR-0001 amended to record that GitHub Actions executes and the GATES are shared scripts — a gate can no longer be strong in one pipeline and weak in the other. Q2: IDENTITY_TEST_DB added; Identity + Interop added to OpenAPI generation; all 20 specs committed to docs/api with a `git diff --exit-code` drift gate, so a contract change lands in the pull request instead of an artefact nobody opens. Two `if (Db is null) return;` tests → SkippableFact (a [Fact] that returns early reports PASSED — a green tick over nothing run). **Turning IDENTITY_TEST_DB on immediately exposed 7 real failures in tests that had skipped on every CI run**: the frozen scope vocabulary was missing the 16 scopes 18.B3 added to the seed; the harness still hardcoded the m2m secret literal 18.B1 deleted; the client-credentials test still asserted the broad scope 18.B1 narrowed; and the login flow posted credentials without ever loading the form, which 18.B3's antiforgery correctly rejects. All fixed — 37/37 identity tests now pass. Q4: domain floor 55→58 with a documented ratchet and target date (it was set as a temporary regression guard and never moved), and overall coverage is gated for the first time — it is the number that falls when the DB-gated suites stop running. New CiGateTests (9) assert the WIRING, because a script nobody calls is indistinguishable from a script that passes. Backend 28/28, 1100 passed, 1 skipped, 0 warnings |
| 18 | E2E Remediation | 18.E2 Structural quality — architecture tests + thin suites | ◐ | 2026-07-27 — New `libs/architecture` (NetArchTest + source scans) encodes the house pattern the R2 audit said would have caught 4 of the 6 criticals at build time: every service binds UseHbmpRls and UseHbmpTransportSecurity (with a documented, staleness-checked exemption list), every tenant_id table has an RLS policy (detector understands the dynamic DO-block form the migrations actually use), Domain never references Infrastructure/EF/Npgsql/RabbitMQ, money is never a float. **It found two real gaps on its first run**: provider-service hand-rolled the RLS binding instead of calling UseHbmpRls (a bespoke copy of shared wiring that drifted for three phases), and `provider.practitioner` — staff names and professional LICENCE numbers, tenant-scoped — had no RLS policy at all, because 0006 added it after 0003 established the pattern and nothing checked. Migration 0007 closes it. Also: `libs/data` — the most safety-critical library and the one with no tests — gets 7 covering the tenant stamper (explicit tenant never overwritten; nothing stamped when unbound, so a fail-closed policy rejects loudly instead of writing an invisible row; updates keep their original tenant so a cross-tenant read + save cannot MOVE a row). masterdata gets its first authz suite (21 endpoints, 1 mapper test before) recording WHY a bare RequireAuthorization is correct there. 7 anonymous-object 404s → RFC-7807; `services/hello` deleted (the 18.A3 clock allowlist caught its own stale entry, which had predicted the deletion). ◐ because three refactors are deliberately deferred rather than half-done — libs/testing extraction, HbmpGate consolidation, and the HttpApiClient `any` burn-down. See docs/PHASE-18-TODO.md for the reasons. Backend 28/28 (+1 new project), 1118 passed, 1 skipped, 0 warnings |
| 18 | E2E Remediation | 18.F1 + F3 (tenant fuzzer) Correctness engineering | ☑ | 2026-07-27 — New `libs/money`: a Money value type with the currency attached, banker's rounding at 2dp applied ONCE at construction, and NO implicit decimal conversion — which is what stops it being decorative, since an implicit conversion lets `allowed = billed` compile again and X3 return. `Money.CapTo` states the X3 invariant as a function rather than a clamp repeated at six decision sites. 12 property tests (~120k generated cases) including the one that matters: capping never RAISES a value, which an inverted comparison would pass the ceiling test while silently inflating every under-cap amount. New property-based ConsumeExecutor suite against real Postgres: 55 generated scenarios of concurrent (quantity, key) attempts with deliberate replays, asserting 0 ≤ consumed ≤ ordered, Σ fulfillment.quantity == quantity_consumed, aggregate status == RecomputeOrderStatus(order), and replay-is-a-no-op. Fixed seed, because a corpus that changes per run makes a concurrency counter-example unreproducible. **F3 tenant-isolation fuzzer** (the prompt's own 'control that would have caught X6/S2 automatically'): enumerates every tenant_id table from information_schema and proves policy+ENABLE+FORCE and, behaviourally, that an unbound session sees ZERO rows. Runs on psql — no pip dependency, because a check that needs an install is a check that gets skipped (Q1). Self-test builds a deliberately fail-open policy and confirms detection. **It found a real disclosure the R2 audit did not name**: `emr.appointment_history` held 105 rows with a BLANK tenant_id, and `UseHbmpRls` bound `""` for any principal without a tenant claim — so `'' = ''` matched and an unauthenticated caller could read an append-only CLINICAL history table. Closed twice: libs/data now binds a sentinel no row can equal (fixes the CLASS, for every table including future ones), and emr 0008 recovers each row's tenant from its parent appointment plus a non-blank CHECK. That CHECK immediately caught two test fixtures seeding untenanted appointments — the same defect that produced the 105 rows. Wired into both pipelines. Backend 1133 passed, 1 skipped, 0 warnings; 92/92 tenant tables proven isolating |
| 18 | E2E Remediation | 18.F2 Command palette + keyboard fix | ◐ | 2026-07-27 — Command palette (⌘K/Ctrl+K), permission-scoped BY CONSTRUCTION: it is handed the same already-filtered section array the nav rail renders, so it cannot surface a destination the caller may not open. A palette that listed everything and 403'd on selection would be an enumeration oracle for all sixteen portals. Subsequence matching ('fset' → Finance · Settlements) because a user is recalling a place, not typing its name; combobox + aria-activedescendant so focus stays in the input while a screen reader announces the highlighted row. This is what 18.D2 promised when it deleted the dead search. Also fixed: `g h` and `g q` both navigated to homePath — two shortcuts with one effect, so a user who learns `g q` and lands on Home concludes the scheme is broken. `g q` now resolves the portal's actual queue/worklist section. 12 palette tests incl. the leak cases and axe. ◐ — the rest of F2 (server-side sort/filter/paginate, keyboard worklist mode, autosave, telemetry) is deferred with reasons in docs/PHASE-18-TODO.md. Frontend 14 files / 106 tests, tsc clean, 0 eslint errors |
| 19 | Policy & Member Admin | 19.1 Payer + plan + effective-dated immutable plan version + benefit rules | ☑ | 2026-07-27 — `0005_pas_plan.sql` adds `payer` (replacing the free-text `policy.sponsor`), `plan`, `plan_version` and `benefit_rule` to the `policy` schema, additive + RLS'd. The benefit window is **half-open** `[effective_from, effective_to)` per design 38 §7.1, so a successor starts on exactly the day its predecessor ends — no gap for a service date to fall through, no day covered twice. **Both correctness invariants are enforced by the database, not the API**, because "the endpoint returns 409" is not an invariant a repair script or a psql session respects: `trg_plan_version_immutable` freezes an activated version's identity/window and permits only `Active→Superseded|Retired`, and `trg_benefit_rule_immutable` refuses any write to the rules of a non-Draft version (freezing the version row alone would freeze nothing that matters — the configuration lives in the rules). `ex_plan_version_no_overlap` (GiST) is deliberately **wider than the prompt's "no overlapping Active ranges"**: the resolver must answer for a PAST service date, which lands on a `Superseded` version, so two superseded versions overlapping would give it two right answers; Drafts are exempt because an amendment is authored while its predecessor is still live. `IPlanVersionResolver` + `GET /plans/{id}/version-at?date=` is the seam eligibility/authorization/claims must call instead of reading "the active version". Draft→validate→activate (predecessor auto-closed + superseded) and `POST /plans/{id}/amend` (clone Active→new Draft) are the only paths that change a live plan. `PlanVersionValidation` catches the plan-level nonsense the DB cannot express — nothing covered, a covered category with a zero limit, a reset on a Lifetime limit, a pre-auth threshold above its own limit — and reports **every** problem at once, since activation is irreversible. Scope split (identity `0006`): `policy:admin` authors the product, `policy:write` stays member administration, `policy:supervise` is the supervisory increment, `policy:read` is the PHI-free configuration read; **`beneficiary_mgmt` held no policy scope at all**, the silent-403 class 18.B3 exists to close. `policy_admin` + `beneficiary_mgmt_supervisor` roles are registered in `ScopeIntegrityTests.UndeclaredRoles` until 19.7 adds them to the frozen vocabulary. Policy **70/70** real-PG, authz 48/48, identity 37/37, full sln **1176 passed / 0 failed / 0 warnings**; Kong routes + OpenAPI spec updated (both gates green). |
| 19 | Policy & Member Admin | 19.1b **Network tiers** (Network Team owned) + tier-aware cost-share | ☑ | 2026-07-27 — schema, resolution, authz separation, cost-share model AND all three consumer wirings. provider `0008` adds `network_tier` + `provider_network_assignment`; policy `0006` **retracts 19.1's** `network_tier`/`copay_fixed`/`copay_percent` for `benefit_rule_tier`; policy `0007` makes two silent defaults explicit — `deductible_waived` (per category; primary care commonly waives it — deliberately NOT a zero deductible, since "this category is exempt" and "this plan has no deductible" survive an amendment differently) and `copay_counts_toward_deductible` (changes not what a member pays today but what they pay NEXT). **THE SEPARATION:** deciding WHICH tier a hospital sits in is network commercial policy; deciding what a member PAYS there is benefit design. Collapsing them lets one person set the out-of-network penalty AND decide who is out of network — policy admin gets 403 either direction (new `provider:admin` scope, identity `0007`). Resolution is most-specific-wins AT THE SERVICE DATE and **fails safe** to OON (partial unique index guarantees exactly one Active OON tier, so the fallback is deterministic). Withdrawal is **three verbs**: *revoke* (not yet effective), *close* (in force — keeps governing its own past window), *correct* (retroactively voids a mis-assignment, refused once a claim has adjudicated against it, audited as its own act). **CONSUMPTION — all three, one slice:** approvals `/authorizations/preauth-required` (fail-CLOSED: unknown ⇒ required, with `determinate:false` so an outage is not read as a benefit decision); eligibility `/check` cost-share preview with the cache key extended to **beneficiaryId+category+serviceCode+gating+providerId+locationId+serviceDate** (the X9 lesson, held by a reflection test over the key's own shape); claims `TierAwareAdjudicationFacts` replaces `PermissiveAdjudicationFacts`, driving step 6 + the split from the tier at `ServiceDateFrom`. All three reach ONE `libs/benefit-pricing` composition and ONE `libs/money` split — **`CrossServiceParityTests` asserts eligibility's quote and claims' charge match to the piastre**, which is the point of the slice: a refugee told one number at the counter and billed another has no reviewer and no recovery path. Provider **70/70**, policy 82/82, money 31/31, benefit-pricing 15/15, eligibility 41/41, claims 131/131, approvals 49/49; full sln **0 failed / 0 warnings**; Kong + OpenAPI + migration-compat gates green. ADR-0019 + ADR-0020 recorded, **both awaiting Medical Director + Finance signatures** — ADR-0019 carries the prior question: *does Mersal charge cost share to refugee beneficiaries at all?* (self-funded charity policies may be no-deductible/no-coinsurance, leaving the grid live only for donor- and government-funded payers). |
| 19 | Policy & Member Admin | 19.2 Policy, groups, enrollment + coverage generation | ☑ | 2026-07-27 — shipped in ONE migration wave with 19.2b (`0008_pas_enrollment.sql`), because 19.2b changes 19.2's shape: landing 19.2 alone would create enrollments with no plan and then need either a nullable FK we could never close or a backfill inventing which plan each member "must have meant". `policy` gains payer_id + previous_policy_id + max_members; `member_group`, `enrollment`, append-only `enrollment_event`. **Enrolment GENERATES coverage** from the elected plan's version (`CoverageGenerator`), stamping `source_plan_version_id` + `enrollment_id` — that provenance is what makes "why am I covered for this, and for how much" resolve to a dated immutable configuration rather than to whoever typed the row. **WINDOW SEMANTICS DIVERGE DELIBERATELY:** plan_version is HALF-OPEN (a config boundary is "first day the new rules apply"); enrollment + coverage are INCLUSIVE (a membership boundary is "last day of cover"), matching the already-shipped EligibilityEngine — making enrolment half-open would silently move every member's last covered day by one. Both stated in the migration header and asserted on both boundary days. The GiST overlap exclusion treats **Suspended as live** (a suspension pauses the benefit, it does not vacate the membership) while a terminated one frees the slot for re-enrolment. `enrollment_event` is append-only by trigger AND by withheld grant. Termination reason mandatory (DB CHECK); retro-effective changes additionally require `policy:supervise`. **EligibilityEngine gained a waiting-period gate** — a newly enrolled member holds valid coverage with intact limits and is still not payable, so the engine had to be told; it is a hard Ineligible with a WAITING_PERIOD reason rather than NeedsAuthorization, because no approval can shorten a waiting period. `CoverageGenerationParityTests` feeds REAL generated coverage into the REAL engine — the guardrail that a generation defect is not a compile error but a person quietly covered for the wrong things. Policy **128/128** (46 new, 8 real-PG), eligibility 41/41. The phase-1.2 `POST /policies` is superseded (a policy now carries a payer, not a free-text sponsor). |
| 19 | Policy & Member Admin | 19.2b **Plans under a policy** (`policy_plan`) + member plan election | ☑ | 2026-07-27 — same wave as 19.2. `policy_plan` (label, window, is_default, declarative `eligibility_rule` jsonb, max_members) with **at most one default per policy** (partial unique index — enrolling without naming a plan would otherwise be a coin toss over entitlement) and no overlapping active window per (policy, plan_version). `enrollment.policy_plan_id` is **NOT NULL**: there is no "enrolled but on no plan". Election criteria are **declarative data, not code** — `PlanEligibility` evaluates groupIds / relationships / age band / branchIds **conjunctively** (a rule accepting *either* half would let a member onto a restricted plan by satisfying the looser one), reports **every** failed criterion by name in the 422, treats an unknown age as a failure (not knowing whether someone qualifies ≠ them qualifying), and refuses a **malformed** rule rather than reading it as "no restriction" — a typo must not silently unlock a restricted plan. `change-plan` is an event with a MANDATORY reason; coverage regenerates from the new version and **consumption carries forward** (remaining = new limit − already consumed, floored at 0) — implemented as a SETTING (`Membership:PlanChangeConsumption`), not a constant, because **ADR-0020 is unsigned** and reversing it must not require migrating every member's accumulator. Renewal maps by plan LABEL and **reports** unmapped members instead of defaulting them. `consumed_value` is never written here — phase 18 remains its only writer. |
| 19 | Policy & Member Admin | 19.3 **Notes on policy + member** (signed · timestamped · cancellable · append-only) | ☑ | 2026-07-27 — `0009_note.sql`. This surface is where an officer records why an exception was granted, why a complaint was upheld, why someone was refused — read back in disputes months later by people who were not there. So all four properties are **structural, not conventional**: `trg_note_append_only` refuses any edit to the body, the signature, or what the note is about, and refuses DELETE outright (no DELETE grant either); the ONLY permitted mutation is Active→Cancelled, plus pin/unpin which change no content. A cancelled note can never be reinstated — a correction is a NEW note that may supersede it. **Visibility may be RAISED, never lowered** (`note_visibility_rank`): realising a note is clinical must be fixable, but lowering it retroactively exposes a body to roles correctly denied it, invisibly. **The signature is a snapshot from the token principal**, never a join and never from the request body — so it survives the author being renamed or de-provisioned, and a caller cannot sign as someone else (`HbmpPrincipal.DisplayName` added, reading `name`/`preferred_username`, falling back to subject because an unsigned record is worse than one signed with a username). **MIN-NECESSARY IS THE BODY, NOT THE SURFACE:** `NoteVisibilityRules` withholds Clinical bodies from finance/claims/call-centre/reception and Restricted from EVERY role (released only through the design-37 §6 grant flow), while a denied caller still receives the note's EXISTENCE — type, date, author, status — because hiding it entirely makes the record look empty and sends an officer away believing nothing was written. Asserted by **reflection over the serialized payload**, not over a property. The author always reads back their own. Cancellation needs a MANDATORY reason (DB CHECK), is author-or-`policy:supervise` only, and a denied attempt is **audited** — someone withdrawing a colleague's signed note without authority is exactly what a review looks for. Clinical/Restricted READS are audited including when the body was withheld. New scopes `note:read` (deliberately wide) / `note:write` (identity `0008`). Policy **172/172** (44 new, 12 real-PG); full sln 0 failed / 0 warnings; Kong + OpenAPI + migration-compat gates green. |
| 19 | Policy & Member Admin | 19.3b **Documents** on policy + member (classified, incl. past medical history) | ☑ | 2026-07-27 — `0010_policy_document.sql`. **REUSE, NOT REBUILD:** bytes, MIME/size validation, the fail-closed ClamAV scan, checksum and MinIO all stay in document-service (`IDocumentStore` seam); policy-service adds only linkage + classification. A second upload path would be a second place for malware to get in and a second place for retention to be forgotten. **THE CLASSIFICATION RULE:** `document_class` sets a visibility FLOOR the uploader may RAISE but never lower — `DocumentClassification.Resolve` returns null on an attempted downgrade so the endpoint 422s naming both values, because silently applying the floor teaches uploaders the field does nothing. Enforced in the DB too (`trg_policy_document_immutable`, reusing 0009's `note_visibility_rank`): the failure is SILENT — a downgraded clinical document looks exactly like one that was always administrative. **GAP RESOLVED:** the prompt says "anything mental-health / HIV-STI / genetic / substance-use / reproductive / GBV → Restricted" but none of those is a document class — they are properties of a MedicalReport's CONTENT. Added `sensitive_category` (design 37 §5 vocabulary) which forces Restricted whatever the class, and cannot be talked down; without it the rule was unimplementable and every such document defaulted to merely Clinical. **`document_date` is distinct from `uploaded_at`** with its own index — past medical history reads in CLINICAL order, so a 2019 discharge summary scanned in today sits in 2019, not at the top (asserted with uploads in the opposite order). **LIST ≠ DOWNLOAD:** the list is metadata-only and carries NO url (asserted over serialized JSON), everyone entitled sees a document EXISTS; download is separate, role-gated, and mints a **2-minute signed URL per request** as the CALLER. Restricted is existence-only for every role incl. super_admin — released only via the 37 §6 grant. **Upload authority is separate from download**: finance may attach an InvoiceReceipt but not a PastMedicalHistory. **DENIED downloads are audited** at High severity for PHI — reaching for a clinical record you may not read is what a review looks for. Withdrawal needs a mandatory reason and keeps the row AND the bytes (no DELETE grant, trigger refuses): "wrong member" is a reason to mark a document, not to make the mistake unfindable. Re-upload supersedes, never deletes. OCR is a WIRED, DISABLED seam returning null (not "") so "not enabled" cannot be read as "no text found". Policy **230/230** (58 new, 22 real-PG); full sln 0 failed / 0 warnings; all gates green. |
| 19 | Policy & Member Admin | 19.3c **Change timeline** (audit-stream projection, policy + member) | ☑ | 2026-07-27 — `0011_entity_timeline.sql` + **ADR-0022**. A PROJECTION, never a second log: nothing in the domain writes to it as part of doing its work. A hand-maintained log drifts from the audit trail the moment one path forgets, and a history that disagrees with the audit trail is worse than none — it looks authoritative and is quietly wrong. **Chose policy-service over reporting-service** (ADR-0022): the scope refs are policy_id/enrollment_id which this service owns; the diff projection REUSES 19.3's visibility rules (a second redaction path is the one that gets a clinical field wrong); and reporting-service is a **de-identified aggregate** read model whose financial_fact has no diagnosis column *asserted against information_schema* — per-member class-projected diffs would quietly make it identifiable while that test still passed. **DETERMINISM IS THE MECHANISM:** `EntryIdFor` is SHA-256 of the source event id (not a random guid) and the diff serializer sorts its keys, so a rebuild is **BYTE-IDENTICAL** and therefore VERIFIABLE — without it "replayable" would mean "similar-looking history with different ids", checkable only by eye. `TimelineReplayTests` asserts field-by-field equality across a rebuild. Idempotent at three layers: derived id, in-code dedupe (also collapsing duplicates WITHIN a batch), `UNIQUE(source_event_id)` as the racing-instance backstop. Append-only by trigger, **except inside a declared rebuild** (`app.timeline_rebuild` GUC, transaction-scoped) — discarding ALL derived data is safe in a way removing one inconvenient line is not. Diffs are MINIMIZED (a full-row diff is a copy of the row = a second PHI database with none of the controls) and class-projected at READ time (a stored-redacted diff would need re-storing per entitlement change, and the already-written copy would still hold the values). Access events — break-glass, restricted downloads — are on the member timeline per design 19. Unmapped event types still produce an entry (a hole with no trace is the worst failure for a completeness record). Bilingual AR/EN summaries authored, not machine-translated. Exports carry no diffs at ALL regardless of role. Policy **269/269** (39 new, 8 real-PG); full sln 0 failed / 0 warnings; all gates green. **Open:** live wiring from the event bus lands with the shared bus, like the other fan-out consumers; the projection logic and its guarantees are complete. |
| 19 | Policy & Member Admin | 19.4 **Utilization**: individual · group · plan · policy · payer | ☑ | 2026-07-27 — `0012_utilization_attribution.sql` + **ADR-0023**. A **DIRECT QUERY over the accumulator, not a projection** (the prompt allows either and asks for the choice): a projection makes reconciliation a property somebody has to KEEP true; a direct read makes it one that CANNOT become false. A missed event or a bad rebuild and the report quietly disagrees with the number eligibility refuses care on — and nobody finds out, because finding out means comparing two things nobody compares. policy-service, not reporting-service: every scope resolves locally, and reporting is a de-identified aggregate store (same reasoning as ADR-0022). **THE DISTINCTION THE MODEL IS BUILT AROUND: `consumed` ≠ `activity in a window`.** coverage_limit RESETS at period boundaries; benefit_consumption does not — so summing the ledger over all time EXCEEDS the accumulator and both are right. Kept lexically apart everywhere (`limit/consumed/remaining/percentUsed/resetsOn` = accumulator; `windowActivity/windowEvents`/tier split = ledger). **Reconciliation is asserted at RUNTIME, not only in a test**: every response carries `reconciliation{accumulatorTotal, reportedTotal, reconciled}` computed via an independent `SUM(consumed_value)` SQL path, and a mismatch audits as `RECONCILIATION-MISMATCH` — a report is read on days no test runs. **TIER SPLIT (19.1b) required attributing the ledger**: 0012 adds `provider_id`/`provider_location_id`/`service_date` to benefit_consumption and orders/pharmacy now emit `providerId`. Stores the PROVIDER and resolves the tier at REPORT time (freezing a tier code would defeat 19.1b's *correction* verb, and keeps an HTTP call off the consume path); stores the SERVICE DATE not applied_at (which lags by broker+retry, and would price February's care against March's network). **Unknown attribution is its own `UNATTRIBUTED` bucket, never in-network** — folding it in biases the error toward flattering the network on the one number the network is judged by. Deliberately NOT 19.1b's rule (pricing treats unresolved as OON to protect the member; a report has no such asymmetry). **Cross-service facts fail SEPARATELY** — three new counts-and-amounts-only endpoints (`/encounters/utilization`, `/authorizations/utilization`, `/claims/utilization`), each narrow AT THE OWNER (a post-wire projection has already put PHI in a log/trace/retry buffer); **null means 'could not ask', never zero**, and the response NAMES the services that did not answer. **No clinical field exists in any utilization type for any role** — asserted by reflection, not filtered (a filter must be remembered; a missing field cannot be forgotten); amounts stay role-gated on 19.3's Financial line. Percentages NULLABLE for unlimited benefits (0% invites 'plenty left' on something never metered; 100% flags a phantom outlier) and UNCAPPED above 100 (a mid-period limit reduction is the row worth reading). Terminated members leave the member list but their spend stays in scope totals. Outlier threshold configurable (default 80%). Audited CSV export. Policy **317/317** (48 new, 9 real-PG); full sln 0 failed / 0 warnings; all gates green. **Open:** pre-0012 ledger rows can never be tier-attributed (provider not recorded); a backfill from the fulfillment tables is deferred to 19.7 rather than smuggled in here. |
| 19 | Policy & Member Admin | 19.5 Policy query · member query · coverage details · admin 360 | ☑ | 2026-07-27 — `0013_query_surface.sql` (policy) + `0006_user_payer_assignment.sql` (admin) + **ADR-0024**. **PAYER SCOPE HAD TO BE BUILT, NOT APPLIED**: nothing knew a user's payer and the token contract is FROZEN (phase 17), so the restriction lives in admin-service beside role bindings + branch assignments (14.2's own argument: an entitlement the phase-16 access review cannot enumerate is one nobody revokes) and is resolved per request via `IPayerDirectory` (≤60s cache). Modelled as a **RESTRICTION not an entitlement** — no assignment = unrestricted, because the inverse would have meant granting every existing officer every payer on day one, and a grant given to everyone stops reading as a grant. **THE THIRD STATE IS THE POINT**: "no rows" and "could not reach admin" are identical on the wire and opposite in meaning, so `PermittedPayers` is {Unrestricted, restricted-set, **DenyAll**} and the directory fails to DenyAll — the OPPOSITE polarity to branch scope, where an empty set already denies. Scope is a **PREDICATE INSIDE THE SQL, INCLUDING THE COUNT**: a total of 4 000 beside a page of 25 tells a restricted user how large another payer's book of business is. **A cross-payer id is 403, not an empty list** (acceptance) — a deliberate inversion of "don't confirm existence", which protects a resource whose existence is the secret (a PERSON); a payer is an ORGANISATION and answering "no such policy" to an admin reading the policy number off their screen sends them to raise a data-loss incident over a permission setting; audited `PayerScopeDenied`/High. Member query filters/bands/sorts/pages **entirely in SQL** (the row count is the membership); policy query materialises aggregates (policies number in the hundreds, and both band filters are useless after pagination). **Branch scope resolved ON DEMAND in member query, not middleware** — policy admin is member-scoped by 38 §6, and a member LIST is the one place an operational role could sweep past their branch; a NULL branch is NOT hidden by narrowing (a search is not a worklist; hiding every pre-0013 member breaks the counter) but IS excluded by a named branch. 0013 adds `enrollment.branch_id`, which the enrolment request already carried for the plan's branch rule and then **discarded**. **Identity filters resolve at patient-service with the CALLER's token** and an empty match returns NOTHING, never everything (a failed name lookup must not answer with the whole membership); an unreachable directory is 503, not an unfiltered page; truncation is declared. Names batched at the owner via a new **counts-and-names-only** `/beneficiaries/summaries` (a 40 000-row filter never becomes a 40 000-name disclosure). Coverage details show the **version in force on the SERVICE DATE** (v1 for a February date though v2 is Active) **beside** the member's own generated ceiling — a real divergence after an amendment, flagged rather than resolved — plus per-tier cost-share grid, waiting-period state, exclusions and effective-dated history. The 360 **AGGREGATES**: patient-service answers for itself (own projection, own PHI audit); withheld payer sections are counted and NAMED rather than refusing the record, because "there is other cover you can't see" is what routes the question onward. Note bodies still go through 19.3's rules. Reflection asserts **no clinical field on any query/coverage payload**; amounts on 19.4's exact reader line, `terminationReason` off the front desk. Page size capped (an uncapped page is an unclassified export); sort from an allow-list; CSV escapes formula-leading values. Policy **373/373** (+56: 44 pure, 12 real-PG); full sln 0 failed / 0 warnings; Kong + OpenAPI (policy 50, admin 35, patient 8) green. **Open:** no role is payer-restricted by default — who gets restricted lands with 19.7. |
| 19 | Policy & Member Admin | 19.5b **Bulk upload & data extract** (dry-run, idempotent commit, rollback, as-of extracts) | ☑ | 2026-07-27 — `0014_bulk_and_extract.sql` (policy) + `0003_operational_document.sql` (document) + **ADR-0025**. **THE RULES MOVED, NOT JUST THE LOADER**: "do not invent a second importer" is really about the RULES, so enrol/terminate/reinstate/change-group/change-plan/cancel moved out of the HTTP handlers into `MembershipCommands`, which both the single-member form and the bulk appliers call — a bulk path re-implementing "is the plan in force" becomes a way to create memberships the form refuses, and nobody finds out until a claim is denied against coverage that should never have existed. `MembershipOptions` moved to the domain with them (a setting only the API could read = a bulk plan change silently using the default). **ONE TRANSACTION PER ROW**, never one for the file (50k rows in one tx holds locks for minutes and blocks every reception desk); Idempotency-Key = (job_id, row_number) and NOTHING else, so it survives a resume — two layers tested separately because they fail differently: an Applied row is not re-processed, and a row whose state was lost between the write and the mark still replays into a SKIP. **Partial failure is the normal outcome and is reported**: a bad row fails alone, the job ends Completed-with-errors, and a CHECK enforces submitted = valid + invalid (a job that cannot say what happened to a row lost it, and the report still renders). **An unknown or missing column fails the WHOLE file** — `effective_date` where the template says `effective_from` would otherwise import 10 000 memberships starting today, every row "valid"; headers are case/order-insensitive because reordering columns is not a mistake. **Spreadsheet dates are read from the underlying value, never `GetString()`** (03/04/2026 has no unambiguous string form), and locale-dependent CSV dates are REJECTED, not guessed. Rollback CANCELS rather than terminates (a mis-uploaded membership never should have existed; a termination leaves them covered for the gap) and refuses **per row** where benefit was consumed — 497 clean reversals + 3 needing a human beats refusing all 500 — and only marks `RolledBack` when every row came back. Extract columns are intersected with the role and the withheld ones are **NAMED** (a spend report missing `total_consumed` silently is not narrower, it is wrong); `Clinical` is a named always-denied class so the refusal reads as a rule, not an oversight. **AS-OF = the facts as NOW KNOWN about that date**, not what was believed then (the second question is already answered by `enrollment_event.occurred_at` + the 19.3c timeline; conflating them gives one number wrong for both); the plan is reconstructed from dated events — `Enrolled`/`PlanChanged` now carry `policyPlanId` — and a pre-19.5b membership renders as "(current; not reconstructed)" rather than silently showing today's plan. Operational files (upload, PHI-bearing error report, extract) go to a **separate** `document.operational_document` through the SAME validate→checksum→**fail-closed ClamAV**→MinIO pipeline; download is an authorized, audited STREAM, not a signed URL (a bearer credential in a query string outlives every revocation, and a URL redeemed at MinIO writes no audit event). Scheduled extracts require an explicit `service_scope_payer_ids` (CHECK-enforced — an empty scope is unconfigured, not unrestricted) and an unsupported cron is refused at write time rather than stored and never fired. patient-service gained `PUT /beneficiaries/{id}/contacts`; provider-service's existing tier verbs are reused — both with the CALLER's token. **Found + fixed a latent bug in the 19.2 write path**: `coverage.enrollment_id` had a real FK in the DB but no declared EF relationship, so enrolment had no insert ordering between membership and generated coverage — it held one row at a time and broke on the third. Also corrected 19.5's `0006` admin migration, which had copied 0004's pre-remediation permissive RLS (0005 exists to remove exactly that). Policy **431/431** (+58: 46 pure, 12 real-PG); full sln 0 failed / 0 warnings; Kong 65/78, OpenAPI policy 63 / document 4 / patient 9. **Open:** the commit loop still runs inline (async worker = wiring, not design) and the scheduled-extract runner is not fired — both with 19.7. |
| 19 | Policy & Member Admin | 19.6 Beneficiary Management + Policy Administrator portals | ☑ | 2026-07-27 — new `policy_admin` portal (`/policy/*`), four new membership sections on the beneficiary-management portal, and `network.tiers` on the Network Team portal. **THE PORTAL SPLIT IS THE DESIGN DECISION**: beneficiary management owns the MEMBERSHIP book (members, groups, utilization, bulk) and gets no `policy.plans`/`policy.payers` at all, because the person enrolling a member must not also decide what that plan pays for; the two roles share the SAME screens, so a second implementation cannot become a second answer to "may this officer see the money". Network tiers are one screen read by both: the Network Team decides WHICH tier a provider sits in, policy administration prices benefits AT a tier, and the write affordances are ABSENT rather than present-and-refused (`mayAdministerTiers` mirrors provider-service's `NetworkTierGate`). **The plan-version editor is two levels because a benefit has two kinds of fact**: coverage/limit/reset/waiting/pre-auth/exclusions is one row per benefit category, and what the MEMBER PAYS additionally depends on where they were treated — so each category expands into a cost-share matrix with one column per **Active** tier, making an unpriced tier visible as an empty cell, which is exactly what activation refuses. Read-only-ness comes from the server's `editable` flag, never re-derived from the status (deriving the rule twice is how a read-only screen ends up with a working save button); an Active version renders the explicit "immutable — amend to change" affordance plus the amend action, with every field `disabled` so a keyboard user cannot type into it either. A **version diff** is rendered rather than left to the eye — "what did this amendment change" is what every amendment is reviewed on. **A new API module (`src/api/policyApi.ts`) rather than folding ~60 operations into the cross-portal `ApiClient`** — but unlike the Phase 15 Call Centre precedent it goes through `http.ts`, so its failures are `ApiError`s that `writeErrorMessage` can render (CallCentre's private `fetch` wrapper could not, which quietly opted it out of the phase-18 D1 RETRY/RELOAD/STOP rule). Idempotency keys are minted **per form instance and rotated only after a success**, tested on both edges: a retry after a failure reuses the key, a genuinely new note gets a new one. The shared notes panel renders a cancelled note struck-through and **still present** with canceller + reason + Cairo timestamp and the four-cue chip (neutral hue + icon + ghost pill + the word), and a withheld body as a NAMED locked state carrying existence/type/author/date — never an empty body. Every proportion drawn ships its data table **unconditionally in the DOM** (sr-only, no toggle to find — R2/U6), bars `aria-hidden` beside it. `Tabs` force-mounts its panels, so each panel gates its own fetch: selecting a policy must not fire six requests including a notes read nobody asked for. **Two backend gaps the editor exposed, both closed here**: there was no way to read the benefit-category catalogue (so the row an administrator opens the editor to ADD could not exist), and `BenefitRuleView` returned a category **id** while the rules PUT takes a **code** — a draft could be read and not written back. `GET /api/v1/benefit-categories` + `benefitCategoryCode` on the projection; null rather than a guessed code when the catalogue is absent. **`InlineAlert` gained a `warn` tone** — it had ok/bad/info only, so "this page is a subset of the matches" had to be shouted as an error or whispered as information; StatusChip has carried `warn` since 0B and the two vocabularies now agree. Web **126/126** (18 new) + axe clean over every route × {en,ar} × {light,dark}; policy **434/434**; full sln **1614 passed / 0 failed / 0 warnings**; Kong 66/79 + OpenAPI (policy 64) green. **19.6a — the dry run (gap closed same day)**: `POST /enrollments/{id}/change-plan/preview` runs the SAME resolution and the SAME arithmetic as the change itself — `ResolvePlanChangeAsync` is shared, so the preview is not a second implementation that happens to agree. A client-side estimate could never have been right: which consumption travels is a SETTING (ADR-0020, unsigned), the new plan's limits are the other half of the sum, and a category the new plan does not cover produces **no row at all** in the outcome, so a withdrawn benefit simply vanished. Each row now carries both ceilings plus `held` (which separates "unbounded today" from "not covered today" — a null limit cannot say both), and `droppedCategories` names what would be withdrawn with the limit and usage about to stop being covered. Gated at **Write, not Read**: modelling what moving a member does to their entitlement is the authority to move them, and `policy.read` is broad enough that it must not double as a way to interrogate one member's consumption against every plan. Takes **no reason** — an officer cannot be asked to justify a decision before being shown what it does — while the change keeps its mandatory one. The dialog cannot be confirmed until the dry run answers, and the carry hint is chosen from the server's `consumptionPolicy` rather than hard-coded (a screen that always claims consumption carries states the wrong rule outright wherever it does not). Tracks NOTHING: the tracker is asserted empty, so no later `SaveChanges` can flush a plan change nobody confirmed. Policy **440/440** (+5 real-PG), web **129/129** (+3), full sln **1619 passed / 0 failed / 0 warnings**, OpenAPI policy 65. |
| 19 | Policy & Member Admin | 19.6b **Analytical dashboard** (6 views, shared URL filters, drill-down, compare, export) | ☑ | 2026-07-28 — `0003_analytics.sql` (reporting): `fact_enrolment` / `fact_utilization` / `fact_cost` / `dim_label`, all tenant-scoped and RLS-protected. **THE DASHBOARD NEVER TOUCHES THE BENEFIT SPINE**: six aggregate views joining `policy.enrollment` and `policy.coverage_limit` would put full scans on the rows a reception desk is checking eligibility against, and leave row-level PHI one `SELECT *` from a screen whose entire purpose is totals. **THE BANDS MOVED TO `libs/benefit-pricing`**: QueryModel's own header demanded one definition of `UtilizationBand` and then put it in POLICY'S domain, which works until a second service needs the rule — so the dashboard would have shipped a copy and a member could sit High on a chart and Medium in a query with both screens correct. Policy re-exports it; no call site changed. **Payer scope is a PREDICATE, before any grouping** — never a filter the client sets and never a post-filter, because a total is exactly the shape in which a leak is invisible; a payer-restricted caller also never sees unattributed (`payer_id IS NULL`) rows, since they asked for one payer's book and 'might be anyone' is not it; `DenyAll` (the admin-service outage value) shows nothing, asserted. **The membership events were WIDENED at the source** (payer/group/branch/relationship/status) rather than letting the projector go asking: a consumer that has to look the payer up is a consumer querying the transactional spine. Analytics shares phase 8.2's dedupe ledger AND transaction — two ledgers would let one projector accept what the other rejected and the read models would disagree about how many members exist with no way to tell which was right. **`fact_cost` has no clinical column** and a test asserts it against `information_schema`, not against the entity: a column added by a migration and never mapped is still readable by anything holding a connection, and finance holds one. Financial + Network views sit behind the FINANCIAL zone (phase 8.2's split, applied rather than re-argued). **Filters live in the URL** — 'look at this' is how a finding gets escalated, and a link that drops its filters sends the recipient to a different number under the same title; one `FILTER_KEYS` list so clear-and-read cannot drift. Compare mode is period-over-period of the SAME LENGTH (a 31-day window against February is a chart that lies by 10%) and each delta carries `direction` as a WORD plus a server-decided `better`, because enrolment up is good news and cost per member up is not. **Every chart ships its data table unconditionally** with the server's own one-line bilingual summary (U6: a table behind a default-off toggle is a feature nobody finds); bars are `aria-hidden` decoration with pattern + printed value, never colour alone. Drill-down returns **ids and figures, never a name** — resolving the person is a separate audited call, because a list that already carried identities would have made the audit event a formality by reading every row nobody opened. Export reuses the displayed series (so the dashboard cannot become a side-channel with a different allow-list) and neutralises leading `=`/`+`/`-`/`@` in tenant-authored labels. reporting **32/32** (+10), web **139/139** (+10) with axe clean over the three new routes × {en,ar} × {light,dark}, full sln **31 assemblies / 1633 passed / 0 failed / 0 warnings**, Kong 67/80, OpenAPI reporting 17. **Found in passing:** the shared dev DB carried three `profile:*` / `callcentre:history:read` scopes seeded by the unmerged phase-20 worktree, which broke identity's frozen-vocabulary test; removed from the dev DB (that branch re-seeds them). **Open:** the live event-bus fan-out into these projections is still the deferred wiring 19.7 carries, so the facts are fed through the projection seam. |
| 19 | Policy & Member Admin | 19.7 **Roles, routes, backfill, docs + ADRs** | ☑ | 2026-07-28 — **PHASE 19 COMPLETE.** `policy_admin` and `beneficiary_mgmt_supervisor` added to the frozen role vocabulary, the admin RoleCatalog, identity's seed (`0009_policy_admin_role.sql`), libs/authz and the SPA's role→portal map — one reviewed change, exactly as 19.1's migration said it had to be. **Two roles, not one**: policy_admin authors the PRODUCT; the supervisor is the increment over MEMBER administration (cancel another user's note, approve a retro-effective change), because a supervisory power every officer holds is not a supervisory power. policy_admin gets `reporting:read` but **not** `reporting:read-financial` — a benefit author sees enrolment and utilization and asks Finance for the money views. Network tiers stay READ-ONLY for it: pricing AT a tier and deciding WHICH tier are different authorities (ADR-0019). **BACKFILL, reversible by batch, reconciled**: `0015_backfill_pas.sql` creates the default payer ("Mersal — self-funded" — the truthful answer for every pre-19.2 policy, and naming it beats a NULL that hides the genuinely unknown ones), a plan version REVERSE-ENGINEERED from actual coverage rows and named "Legacy coverage (reconstructed)" so it is never mistaken for an authored product, one default policy_plan per policy, and enrolments from coverage with the idempotency key AS the natural key. Provider's `0009` adds the T1/OON default tier set and assigns every provider **from their contract start date** — assigning from today would have made every historical service out-of-network overnight and reported a network collapse that never happened. Both write a reconciliation report of COUNTS, not a status; all six measures reconcile. **Verified end to end, not asserted**: a scripted PKCE login as `policy_admin` returns an RS256 token with exactly the seeded scopes, Kong validates it at the edge, and reporting-service answers **200** on enrolment/outliers and **403 on financial** — the zone split working, observed. **Three real defects found by the platform's own guards while wiring**: (1) granting `reporting:read` in the seed was only half the wiring — `ReportingPolicies` checks the ROLE too, so the token had the right scope and got `role-not-permitted`; (2) naming `finance` on the operational rule without granting it the scope, caught by the scope-integrity guard; (3) both `backfill_provenance` tables carried `tenant_id` with no RLS policy, caught by `Every_tenant_scoped_table_has_an_rls_policy`. **DOCS**: ADR-0017 (effective-dated immutable plan versions), ADR-0018 (append-only signed notes), ADR-0028 (document classification, raise-only — written as ADR-0021, renumbered in 24.7 because phase 21's user & access model also claimed 0021) written; 0019/0020/0022 already existed. Updated 07 (FR-POL-020..025, FR-NET-010/011, FR-NOTE-010..012, FR-DOC-010), 10 (§3.22/§3.23), 11 (§3.7 + five hard rules), 14, 16, 22, 23, 00-README-INDEX. Full sln **31 assemblies / 1633 passed / 0 failed / 0 warnings**; web **139/139**; Kong 67/80; SPA-scope, migration-compat and architecture gates green. **Open (carried out of phase 19):** the live event-bus fan-out into the 19.6b projections and the async bulk-commit worker are both wiring the projection seam covers today; ADR-0019/0020 still need sponsor signatures. |
| 20 | Patient Profile | 20.1 `profile-service` contract + composition engine (caller-token, section-gated) | ☑ | 2026-07-29 — `libs/authz/ProfilePolicies.cs` (design 39 §4's role × 15-section matrix as data) + `services/profile`, a composition service with **no DbContext, no migrations and no `.sql` file** — verified: the tree contains zero. 15 `ISectionProvider`s fan out under the CALLER's bearer; the matrix gates BEFORE fetch and variants narrow AFTER; one `ProfileViewed` event names served **and withheld** sections. 86 tests. Detail in the phase-20 note below. |
| 20 | Patient Profile | 20.2 Consolidate the four existing partial 360s onto the contract | ☑ | 2026-07-29 — `HttpBeneficiary360Assembler` is gone (verified absent from the tree); `services/case/Api/ProfileBackedBeneficiary360Assembler.cs` delegates to profile-service under the caller's own bearer. Three of the five endpoints it used to call never existed, so those blocks had been silently empty in every environment. |
| 20 | Patient Profile | 20.3 Beneficiary photo (consent-gated, role-limited, signed TTL, audited) | ☑ | 2026-07-29 — `IdentityPhoto` on policy's `DocumentClass` (`services/policy/Api/PolicyDocumentEndpoints.cs`), consent-gated at upload with refusal permitted and never blocking care, short-TTL signed retrieval, audited, allow-list NARROWER than the Administrative class it is filed under. |
| 20 | Patient Profile | 20.3b Call history: `summary` field, Full/Operational/Meta projection, server-generated `copyText`, `CallSummaryCopied` audit | ☑ | 2026-07-29 — `callcentre/…/0004_call_summary.sql`: a NEW capped column, deliberately not a rename of `notes`; required at close unless Abandoned; corrections write a revision row and set a visible edited marker. `copyText` is generated from the NARROWED row, so a Meta viewer's clipboard cannot carry a summary their JSON omitted. |
| 20 | Patient Profile | 20.4 Profile screen + patient context bar + search entry + module deep-links | ☑ | 2026-07-29 — `apps/web/src/screens/PatientProfile.tsx`: one role-driven screen, three visually distinct states, four-cue call direction (RTL-mirrored), an accessible copy button that copies the server's `copyText` verbatim. Deep-links are rendered or ABSENT, never disabled — a greyed-out "Start encounter" on a receptionist's screen advertises a capability they will never have. |
| 20 | Patient Profile | 20.5 Performance budgets, Kong routes, docs + **ADR-0026** | ☑ | 2026-07-29 — `perf/k6/06-patient-profile.js` encodes both budgets and asserts the context bar returns a SUBSET as well as being fast: a regression that made it fetch the whole profile would still be correct, and would silently cost seconds on every clinical screen. Kong routes present, `docs/api/profile.json` committed, `docs/adr/0026-patient-profile-server-side-projection.md`. **The row title said ADR-0023, which is the utilization read model** — corrected to 0026. |

| 21 | User & Access Model | 21.0 ADR-0021 + additive token-contract extension (byte-compat test on old + new fixtures) | ✅ | |
| 21 | User & Access Model | 21.1 `tenant_membership` principal + tenant-local roles + access-neutral backfill | ✅ | |
| 21 | User & Access Model | 21.2 Catalog + overrides + one evaluator, two modes, parity suite + the A1 no-PHI-wildcard test | ✅ | |
| 21 | User & Access Model | 21.3 Time-bounded branch-scope grants, precedence chain, sentinel fail-closed | ✅ | |
| 21 | User & Access Model | 21.4 Program enablement: features + live-count caps, distinct problem types | 🟡 | Mechanism + admin surface built and tested; **no service calls the gate yet** — see the note below |
| 21 | User & Access Model | 21.5 Attribution, access-review snapshot, switch guards, session degradation (A6) | ✅ | |
| 21 | User & Access Model | 21.6 Admin UI (memberships, overrides, grants, features, effective-access preview) + routes + docs | ✅ | |
| 24 | Coverage, CI truth & hygiene | 24.0 Measure coverage honestly (denominator audit, per-module report, ADR-0027) | ☑ | 2026-07-30 — The 19-point "collapse" was a MEASUREMENT BUG: coverage-gate.sh summed every class in every cobertura report and `dotnet test` writes one per test assembly, so 161 files were counted more than once (one in 28). It reported 22,625 domain lines against a Domain layer with 12,488 physical ones. Union merge in `tools/ci/coverage-report.py` (--selftest, --compare-naive): honest domain **82.5%** (floor 58), overall **50.7%** (floor 45). Floors untouched, per the sponsor decision. `coverage-exclusions.txt` is deliberately EMPTY and carries the refutation. |
| 24 | Coverage, CI truth & hygiene | 24.1 A gate that cannot go blind (fail-at-end, freshness, one-way ratchet, auto-raise) | ☑ | 2026-07-30 — CI had not reached the test step since ~27 July: it died at migration-compat every push, so six later gates reported nothing while the build was red for one reason. Restructured to fail-at-end; static gates run BEFORE the build. Floors moved to `coverage-floors.json` (single source of truth) + monotonicity guard + auto-raise + daily freshness alarm. THREE further blind spots found here: the `paths:` filter had no `docs/**`, so the commit fixing the OpenAPI contract triggered no run at all; `generate-openapi.sh` could exit 0 having written zero specs; and two `continue-on-error` steps had no `id`, so their failure could not fail the build — one of them the `*_TEST_DB` export, without which ~100 DB-gated proofs skip and the suite still reports green. |
| 24 | Coverage, CI truth & hygiene | 24.2 Invariant registry with a CI checker | ☑ | 2026-07-30 — `docs/quality/invariant-registry.yaml`: 20 invariants, each naming the tests that prove it; `check-invariant-registry.py` indexes ~1,600 test methods from source and rejects a name that does not exist, was renamed, or is skipped. Eleven names I wrote from memory were rejected on its first run. |
| 24 | Coverage, CI truth & hygiene | 24.3 Tests in risk order (money/entitlement first) | ◐ | 2026-07-30 — Three real defects found and fixed red-before-green. (1) Two officers could both decide one claim line: only an xmin collision stopped it, which fires on overlap and does nothing when requests merely follow one another. (2) An enrolled member had NO coverage at eligibility-service — the enrolment path published a COUNT, and the projection is written only by `CoverageChanged` — so every check returned Ineligible "no active coverage". (3) The 19.2 waiting-period branch was dead code: the projection had nowhere to carry the boundary, so a member inside their waiting period was told Eligible. INV-OUTBOX-SURVIVES-CRASH proven and enforced; 69→55 of its call sites fixed, the rest ratcheted one-way in `docs/quality/outbox-atomicity-debt.txt`. Remaining: INV-DEDUPE-SURVIVES-RESTART (needs the durable store from 22.3.4) and the Api-layer gap. |
| 24 | Coverage, CI truth & hygiene | 24.4 Close the measurement holes (a11y, contrast, Arabic, skips) | ◐ | 2026-07-30 — The a11y sweep waited for `<main>` to EXIST, which the shell satisfies before any lazy route resolves: **55 of 112 routes handed axe an empty element**, so half the platform was audited to no effect. Now waits for content and asserts it; all 112 × 2 langs × 2 themes pass on real content. The last skipped test in the suite (migration sink) skipped because nothing exported MIGRATION_TEST_DB — the same cause as identity's twelve in 18.E1, four months later — now runs, with `check-skipped-tests.py` capping it at zero. Ten English literals were being copied into the Arabic field. Remaining: make the Playwright contrast job blocking. |
| 24 | Coverage, CI truth & hygiene | 24.5 Migration 0010: rehearse, prove both directions, apply | ☑ | 2026-07-30 — Rehearsed on a restored dump: with a non-superuser owner and FORCE off, 6 of 6 rows visible with no tenant GUC and 6 under a wrong one; with FORCE on, 0/0/6. Rollback written and executed (7→0), rolled forward twice for idempotency. Applied to dev. NOTE: 0010's own header claims FORCE stops the owner bypassing — true only for a non-superuser owner, and `hbmp` is SUPERUSER + BYPASSRLS, so the advertised protection does not materialise here. Running the fuzzer afterwards found **1,191 rows across 7 tables with `tenant_id = ''`** — belonging to no tenant, invisible to every real one. |
| 24 | Coverage, CI truth & hygiene | 24.6 Purge the 62 MB PDF from history, make it unrepeatable | ☑ | 2026-07-30 — Classified first (CPT 2022 code book: no beneficiary data, so not a DPIA incident; AMA-copyrighted, so a licensing question). Mirror backup, rehearsed on a throwaway clone, `git-filter-repo`, force-pushed all refs + tags, verified from a fresh clone: 96 MB → 22 MB, zero occurrences. Branch protection was NOT restored because there was none — the API returns 403 on this plan. `check-large-blobs.py` (pre-commit + CI) stops the next one. Runbook: `docs/runbooks/history-purge-2026-07-30.md`. |
| 24 | Coverage, CI truth & hygiene | 24.7 Make the status documents stop lying | ☑ | 2026-07-30 — Eleven sub-prompt ids were listed twice with opposite glyphs (one row shipped, another not started, hundreds of lines apart) and six glyphs were in use with nothing defining them. Stale duplicates removed, legend written, `check-build-status.py` enforces both rules plus "every defined glyph is used". HANDOFF.md rewritten — it still described Keycloak (replaced in phase 17), hello-service, and "107 tests green" against a suite of ~2,160. |
| 25 | Branch Management | 25.0 ADR + the sponsor decisions | ☑ | 2026-07-31 — **ADR-0029** (not 0025: that number was taken by the bulk-and-extract engine and ADRs run to 0028 — one sequence across the platform, not per-phase; phase 21 hit the same collision and 24.7 resolved it the same way). Records the authority-vs-reach decision — ONE permission set, two reach modes — and the rejected alternative of two roles with two capability lists, whose failure is drift: someone adds a capability to the coordinator, forgets the manager, and the supervisor of six clinics can do less than the coordinator of one. Records `BranchSetScoped` and why `MemberScoped` was rejected for the manager: it is an ungoverned "everything" with no grant behind it, and reach that no assignment produced cannot be reviewed, revoked or explained. **Five sponsor decisions implemented as PROVISIONAL — all five RATIFIED as recommended on 2026-08-01 (`docs/decisions/phase-25-sponsor-pack.md`), see 25.9:** D1 controlled substances excluded from v1 *by CHECK constraint, not convention*; D2 consumption does NOT link to an encounter (keeps inventory PHI-free); D3 a coordinator MAY create a practitioner, guarded by licence uniqueness; D4 the clinics manager has WRITE everywhere; D5 vaccines/injectables are PHARMACY stock, not clinic stock. |
| 25 | Branch Management | 25.1 Roles & scopes: one set, two reaches | ☑ | 2026-08-01 — `branch_coordinator` + `clinics_manager` seeded with an IDENTICAL 16-scope set (reception's twelve + four branch authorities), granted from ONE list CROSS JOINed onto both so the seed cannot disagree with itself. Pinned in THREE independent places: the migration text, the resolved store, and the SPA's role map. `ScopeIntegrityTests` had to learn a fourth grant shape — an unrecognised shape does not fail there, it reads as ZERO grants, so the reachability check would have passed vacuously. **`BranchSetScoped`**: `branch_id IN (permitted)` rather than `= active`; both existing modes were wrong for a supervisor of six clinics, and `MemberScoped` is ungoverned reach with no grant behind it. The active branch FILTERS in this mode and never defines the set. Sentinel fail-closed preserved, every zero-row assertion paired with a negation. **Retires the phantoms** `branch_manager`/`clinic_manager` — named in libs/authz and the SPA, never seeded, held by nobody. `check-spa-scopes.py` caught a real gap: the four scopes were in the issuer's interactive set and not requested by the SPA, so a coordinator would have signed in fine and 403'd on every branch endpoint. |
| 25 | Branch Management | 25.2 Practitioner administration at branch level | ☑ | 2026-08-01 — The write group admits `branch:practitioner:write` beside `provider:write`; `BranchReachGuard` is the other half, and without it the widening would be strictly WORSE than not widening — the whole network's roster behind what looks, in the route table, like a sized permission. Maadi→Dokki is 403 + audited at High. Edits naming no branch are checked against the branches the practitioner actually serves; an UNASSIGNED practitioner is reachable only network-wide (the tail of D3 — "nobody's" must not mean "everybody's"). UNIQUE `license_no` (partial: live, not-null) enforced at the DATABASE with a backfill check that ABORTS over duplicates rather than choosing which clinical identity survives; dev verified clean (4 practitioners, 0 duplicates) first. Duplicate create → 409 `urn:hbmp:practitioner-exists` carrying the existing id and name, because the next action is "assign them to my clinic instead". **Two gaps found:** the licence fields had NO write path after creation, so a renewal could not be recorded at all; and the licence field-mask keyed on `provider:write` alone, so a coordinator could not see the licences they maintain without being handed the external provider directory. |
| 25 | Branch Management | 25.3 Licence expiry as an enforced gate | ☑ | 2026-08-01 — **The most consequential finding in design 42, closed.** `license_no`/`license_expiry` existed since provider 0006 and NOTHING read them: bookability checked status + branch assignment only, so a doctor whose licence expired last year was bookable. Now evaluated AS AT THE SLOT DATE — the probe gained `asOf`, and generation applies the bound PER DATE, so a licence expiring 30 Sep yields September slots and no October ones. Boundary INCLUSIVE and written down (a doctor is not unlicensed on the last day of their own certificate), asserted on both boundary days at both layers. A NULL expiry is NOT expired — collapsing them would have emptied every clinic's calendar on day one and read as an outage, so the pressure is applied at entry instead. **FLAG, NEVER CANCEL**: 12 future appointments flagged, 0 cancelled, proven against a real DB through the consumer's own code; a completed encounter from before expiry is untouched. Sweeper warns 90/60/30 and announces on the day, idempotent without a `last_warned` column. **Two real defects found by the tests:** a Cairo-offset cutoff written straight to the query throws (Npgsql refuses non-zero offsets on timestamptz), and a UTC cutoff would have left the FIRST CLINIC of the day after expiry unflagged. Also closed a gap 25.1 opened: every branch-scoped read narrowed on `ActiveBranchId is {} active`, correct for two modes and silently UNRESTRICTED for the third — a manager with three grants would have read all six branches, seeing MORE with every screen working. |
| 25 | Branch Management | 25.4 Roster exceptions + ONE availability computation | ☑ | 2026-08-01 — `provider_availability` is a weekly rule with no way to express leave, so the only way to stop slots was to DELETE the rule — losing every other week to cover one absence. `roster_exception` adds the layer: Leave/PublicHoliday/ClinicClosed subtract, AdHocClinic adds, and a whole-day closure BEATS an ad-hoc clinic (a stale ad-hoc row must not reopen a branch somebody closed). Availability = rule − exceptions ∩ assignment ∩ licence, computed in `SlotGeneration` and nowhere else; the assignment and licence arrive as DATE BOUNDS because both are date-varying across a generated range, combined by `BookableUntil` so neither call site can forget one. **The one-place rule is asserted STRUCTURALLY as well as behaviourally** — every behavioural test drives `SlotGeneration`, so all of them would keep passing if a second implementation appeared and a screen used it, which is the failure the rule is actually about. Impact preview lists the affected appointments rather than counting them, and the apply refuses unless the acknowledged count still matches. Dry-run parity is structural: preview and apply call the same method. Kong route and migration-compat gates both caught real gaps. |
| 25 | Branch Management | 25.5 Clinic inventory: schema + append-only ledger | ☑ | 2026-08-01 — New service; nothing existed before. On-hand is `SUM(quantity)` over an append-only ledger — no `quantity_on_hand` column anywhere, asserted against `information_schema`. Append-only enforced twice (REVOKE + trigger) so a repair script is refused too. **The concurrency test found a real bug in the implementation:** `SELECT ... FOR UPDATE` over movement rows locks rows that EXIST and does nothing about a concurrent INSERT — two callers issuing the last unit each computed on-hand = 1 against a pre-insert snapshot and both succeeded, leaving −1 on the shelf. On a derived balance there is no balance row to lock, because not storing one is the design; replaced with `pg_advisory_xact_lock` on the stock line, stable over five consecutive runs. Transfers are paired movements summing to zero; expired medical stock is quarantined (Issue refused, reasoned WriteOff is the way out, and that exemption IS the mechanism); controlled substances blocked by CHECK (D1). |
| 25 | Branch Management | 25.6 Inventory API + worklists | ☑ | 2026-08-01 — Items / stock / movements / transfers / alerts, every one branch-reach checked, with `Idempotency-Key` REQUIRED on movements (a double-posted receipt is a phantom stock level and the ledger has no UPDATE to fix it). A coordinator sees one clinic and a manager all six through the SAME routes — `BranchSetScoped`, no separate manager surface. **No route, request contract, entity or column carries a beneficiary identifier**, asserted over all four surfaces, with the guard itself tested against the exact addition it exists to stop ("just an optional encounter id"). `AddHbmpDurableOutbox` AND `AddHbmpOutboxRelay`, adjacent and commented — document-service shipped the first without the second. **Four gates caught real gaps:** missing Kong route; no OpenAPI spec (`BranchReachGuard` was never registered in DI, so minimal APIs inferred it as a request BODY and the GETs could not bind); unresolvable invariant-registry test names (a nested `FixedClock` helper re-parented every test in the file); and `INVENTORY_TEST_DB` missing from the test-db env list — added WITH the service, since a concurrency proof that never runs is worse than none. |
| 25 | Branch Management | 25.7 Branch Management portal | ☑ | 2026-08-01 — ONE portal, two roles, and one LITERALLY: both `PortalDef` entries reference the same section array and the SPA's permission list is one constant referenced twice, so neither can drift. The manager's Branches-overview is **reach**-scoped, not permission-scoped — making it a permission would have broken the one-permission-set invariant to hide a table. **Licence status carries four cues**, with a test per word: three states → three DISTINCT chip kinds (hue+icon+shape), never grey, and the EXPIRED label states the CONSEQUENCE ("cannot be booked") rather than the fact; "no licence recorded" stays distinct from "expired". **The impact preview blocks a blind closure in two independent places** — Apply disabled until previewed AND acknowledged, any edit invalidates the preview, and the server separately refuses a stale count. Affected appointments are LISTED, and the outcome states "0 cancelled". A house gate caught a defect in this change: three screens used a bare `toLocaleString`, which renders in the MACHINE's zone — a clinic PC on UTC shows a 09:00 appointment as 07:00, and one of those screens is the list a coordinator rings people from. axe clean across every route × EN/AR × light/dark. |
| 25 | Branch Management | 25.8 Docs, seeds, routes, status | ☑ | 2026-08-01 — Docs 10 (§3.22/§3.23, and §3.20 marked SUPERSEDED + never-seeded), 11 (§3.5b + four hard rules), 14 (the portal, and why the branch control switches vs filters), 16 (inventory-service), 22 (six new tables incl. the ledger), 23 (§6b — the licence and roster gates on the appointment lifecycle, and that a flag does NOT change appointment state), 00-README-INDEX (doc 42). `tools/dev/seed-branch-management.sql` makes every alert path demonstrable — a licence expiring in 20 days, one expired last week, a doctor on leave next week, both item categories, a batch expiring in 18 days, an already-expired batch WITH stock (the quarantine state), and one line below its reorder level — and ends by COUNTING each path, because a seed that silently seeded nothing leaves screens looking correct and empty until a demo. Idempotent, and the ledger block is guarded rather than deleted-and-reinserted, because append-only means exactly that. Five invariants registered with named tests. **25.8a (2026-08-01): the seed granted no BRANCH REACH, so everything above it was invisible.** `branch_coordinator` and `clinics_manager` are the branch-scoped roles and `admin.user_branch_assignment` ships EMPTY — no migration inserts a row — so `HttpBranchDirectory` fail-closed to nothing and both demo accounts authenticated perfectly and then saw an empty portal: the coordinator refused everywhere, the manager with zero branches. Every practitioner, item and batch was seeded into a screen neither could reach, and the symptom reads like a broken login rather than a missing grant. Now: coordinator gets ONE Home (Maadi, where the stock is), manager gets Home + five Additional — which is what makes `BranchSetScoped` observable at all, six branches in one response from the same screens. Subjects are LOOKED UP by `normalized_user_name`, never hardcoded, because `UserSeeder` mints a fresh `Guid.NewGuid()` per machine. Missing demo accounts raise a NOTICE, not an exception — they are created by identity-service at startup rather than by a migration, so running the seed first is a sequencing mistake, not a broken environment — and the two reach counts head the verification query so a skipped grant shows as a 0 rather than a notice that scrolls away. Verified against the real dev DB: run twice inside one transaction, 7 rows not 14, then rolled back. |
| 25 | Branch Management | 25.9 Sponsor sign-off D1–D5 + closing D5's enforcement gap | ☑ | 2026-08-01 — **All five ratified as recommended**; nothing overturned, so no DPIA and no DPO/Medical Director signature (both were only ever in scope for reversing D2 or D5). Record: `docs/decisions/phase-25-sponsor-pack.md`, which is the source of truth — the signatory's printed name is deliberately the one field still blank, because nobody else can supply it. **The pack corrected its own header on the way**: it had said D5 needed the Medical Director unqualified, which would have held a decision that changes nothing clinically behind a signature it does not need. **The finding that made the exercise worth doing:** asking "what is each decision enforced BY?" rather than "what did we decide?" showed D1–D4 held by a `CHECK` constraint, a unique index, a five-fact suite and a reach mode — and **D5 held by nothing**. No reference to vaccines, injectables or any medicine identifier existed anywhere in inventory-service, so "vaccines are pharmacy stock" was a rule people had to remember, described in the ADR in the same confident tone as the four that were real. **Closed (ADR-0029 §4.1):** `POST /inventory/items` now calls a new `GET /api/v1/drugs/classify` on masterdata-service and refuses any item matching the medicines master with 422 `urn:hbmp:medicine-not-clinic-stock`, **naming the matched drug** so the answer is *why* and not just *no*. Answered by masterdata rather than a word list in inventory because "what counts as a medicine" is a clinical question whose home is the medicines master — a copy in a storekeeping service is a second answer that drifts the first time a drug is added to one and not the other. Three choices inside it: matching is **bidirectional containment**, not equality (the real mistake is never typed exactly — "Hepatitis B Vaccine 20mcg/ml vial" must hit the master's "Hepatitis B Vaccine"), floored at six characters so a master entry called "Water" cannot refuse every consumable mentioning water; an **unreachable master refuses the item (503) rather than admitting it**, because the cheap version treats "could not ask" as "not a medicine" and leaves the gate open in exactly the window nobody is watching — the cost is that a new gauze SKU waits a few minutes; and there is **no override tick-box**, deliberately, because an override is how a decision made once centrally becomes six clinics each ticking a box on a Tuesday (the accepted cost: a genuine consumable sitting in the medicines master is refused, and the remedy is to correct the master, where the correction is visible to whoever is accountable for it). **A latent 500 fixed on the way:** `CreateItemRequest`'s four required strings are declared non-nullable, which stops nothing — `"sku": null` deserializes to null and the following `.Trim()` was an unhandled exception waiting for the first client to send one; now a 400 naming the fields. inventory 56/56, masterdata 30/30 (9 new tests across both), full suite green. |
| 25 | Branch Management | 25.10 The gate that had no local entry point | ☑ | 2026-08-01 — **`openapi-drift` had been RED in CI for a day while local runs reported every other gate green**, and three committed specs (`approvals`, `patient`, `policy`) were stale behind the code. Both halves matter. The gate was blocking and correct; what was missing is that its comparison lived **only** inline in `.github/workflows/backend-ci.yml`. Every other gate has a script under `tools/ci/`, so "run the gates locally" covered ten of eleven and nobody noticed the eleventh was absent — the only way to run it was to push and wait ~8 minutes for the scoreboard, which is not a thing anyone does before a commit. **Before saying "all gates green", check the set you ran is the set CI runs**, or the claim is about your tooling rather than the code. New `tools/ci/check-openapi-drift.sh` (`--fix` regenerates in place); CI now CALLS it instead of keeping its own copy, so the local answer and CI's cannot differ, and it keeps 24.1's refuse-to-compare-nothing rule (verified against an empty dir). `openapi-generate` + `openapi-drift` added to `REQUIRED_GATES` so their SILENCE alarms and not just their failure. **The first version of the script reproduced the very defect it was written about:** `diff` exits 1 when files differ, and under `set -e` + `pipefail` that killed the script inside a command substitution — exit 1, no output, no way to tell a drifted spec from a broken gate. Fixed and proved by injecting a fake path into a spec: the gate names the file and the changed-line count, `--fix` repairs it, and the re-run is clean. |
| 26 | Prescribing workspace | 26.1 Ingest the drug list: `drug_indication` + the missing columns | ☑ | 2026-08-03 — `Master Lists/egyptian-drug-list_5.xlsx` INSPECTED before mapping, and three findings changed the design rather than the implementation. **(1) Every indication code is a 3-character ICD-10 CATEGORY** — all 874, not one dotted — while `masterdata.icd_code` stores dotted codes and `emr.diagnosis` records the specific one, so equality matching would have reported "not a listed indication" on virtually every prescription; a warning that always fires is one clinicians learn to click through, so both sides normalise to the category. **(2) `Z76` is a filler, and 1,019 products (4.5%) carry it as their ONLY code** — they load with zero indications and report "not checked", because storing it would let a product with no clinical data render as checked. **(3) The mapping is ATC-L4 clinical judgement, not a published dataset** — the workbook says so itself, and that sentence is carried into the UI as the finding's caveat. Loaded: 22,653 drugs, **214,402 indications**, zero unmatched ICDs, `strength` 17,873, 11 s / 176 MB (SAX-streamed; the sheet is ~750k cells). Idempotent — a second run against the real DB inserted 0 with identical counts. `drug_id` is now DERIVED from the source row id (`Guid.NewGuid()` made id stability an accident of the trade-name string never drifting; any drift orphaned every indication and prescription line pointing at the old row). **What the file CANNOT supply, reported rather than papered over:** no Arabic trade name (no such column), no UNHCR formulary (column AG is a header with no data), no dosing data. `masterdata:read` added — see the note under 26.7. 14 loader tests. |
| 26 | Prescribing workspace | 26.2 Drug search fit for a combobox | ☑ | 2026-08-03 — `GET /drugs/search`, one field over trade name AND active ingredient AND Arabic name, because a prescriber searches by whichever they know: "augmentin" and "amoxicillin" reach the same product. `masterdata.search_key` normalises case, Latin accents, Arabic tashkeel and the alef/ya/teh-marbuta variants, and the GIN trigram indexes are built on the SAME expression — normalising the query differently would leave the index unused, which fails silently as latency rather than as a wrong answer, so there is a test asserting the PLAN. Measured on the full 31,651-row catalogue: 5–17 ms at ≥3 characters, 75 ms at 2 (pg_trgm has no trigram to look up below 3), all inside the 300 ms budget. Serves the CURRENT market list only: the workbook and the superseded CSV overlap partially, leaving 8,998 legacy rows with no indication data, and offering a prescriber two entries for one product where only one can be checked would be a safety regression dressed as completeness. 9 tests. |
| 26 | Prescribing workspace | 26.3 The validation engine (`libs/clinical-validation`, test-first) | ☑ | 2026-08-03 — **The prompt says there are three silent catches. There are six.** Lines 71/84/103 were the `catch (HttpRequestException)` blocks it names; 64/80/96 turned any non-2xx into "no alerts" through a bare `if (resp.IsSuccessStatusCode)`, with no catch at all — the more dangerous form, because it reads like handling. It had a live consequence: after 26.1 scoped masterdata, a token missing `masterdata:read` gets a 403, which took exactly that path and rendered as "no interactions found". **Fixed as a class, not as instances.** Ports return `Fetched<T>`, whose `Unavailable` case carries NO payload — there is no empty collection to mistake for a negative result, so there is no code path from a failed fetch to `Ok`. "Clinical checks never block" is enforced the same way: every clinical checker is typed to `ClinicalState`, which has no `Blocked` value, so writing one is a compile error. A 5 s per-source timeout added (the platform has no resilience layer at all, and an unbounded wait leaves a doctor on a spinner mid-consultation; a timeout is an ANSWER here). 30 pure-domain tests + 14 that kill the real transport six ways — refused, 500, **403**, 404, 502, unreadable body — plus partial-outage and hung-dependency. Four invariants registered. |
| 26 | Prescribing workspace | 26.4 Prescription schema + validate/submit API | ☑ | 2026-08-03 — `duration_days` (the field that makes a dose ceiling checkable at all), `primary_icd_code` + `diagnosis_snapshot` (a SNAPSHOT, not a join: a correction next week must not rewrite what was actually checked), and two append-only tables — `prescription_validation` stamped `Step1`/`Step2`, `prescription_line_override` with a mandatory reason. **`POST /prescriptions/validate`** is advisory and persists no draft. **Submit re-validates from scratch and reads nothing the client claims**: a forged clean payload for a drug the engine warns on is still refused. The ACKNOWLEDGEMENT gates submission, not the warning — a prescriber may proceed past any clinical warning with a recorded reason, but not silently — while a benefit refusal cannot be acknowledged away. **A bug 26.3 introduced, caught here:** `prescription_alert.kind` admitted only `DrugInteraction`/`Allergy`, so the new `Unavailable` alert would have failed the INSERT and taken the whole prescription write down — trading a silent false-OK for a hard failure to prescribe is not an improvement. Widened. 7 endpoint tests on a new pharmacy HTTP harness; two invariants registered. |
| 26 | Prescribing workspace | 26.5 The prescribing workspace UI | ☑ | 2026-08-03 — Replaces a modal with four plain text inputs, hard-coded `J01CA04`/"Amoxicillin 500mg", and `drugId: req.drug.code` — the ATC STRING where the API expects a Guid, so the path could not work against real data. Real ARIA 1.2 combobox (roles, `aria-activedescendant`, 250 ms debounce, ≥2 chars), following the `CommandPalette` pattern already in the codebase rather than inventing a second one; the ingredient renders under the trade name and stays visible after selection, because two trade names holding the same molecule is the commonest prescribing duplication and the cue must be there at review time too. **Five states, four cues, in TWO visual classes:** answered (Ok/Warning/Blocked) are solid filled chips; unanswered (Not checked / Check unavailable) are dashed with a hollow glyph — so "we have no answer here" reads before colour or text. The encounter's LIVE diagnoses are passed in (the modal received only an encounter id, so the indication check had nothing to compare against); an empty list reports "no diagnosis recorded" rather than passing. Editing a line invalidates the run AND drops its acknowledgements, because a reason belongs to the finding it was given for. 16 tests incl. axe EN + AR against POPULATED fixtures. |
| 26 | Prescribing workspace | 26.6 Provider visibility by card number | ☑ | 2026-08-03 — Three defects fixed together, and the third is the instructive one: `card_number` existed and was unique but no filter reached it; `IdentifierType` had no member for it; and **`GET /beneficiaries/resolve` — which pharmacy has CALLED since phase 6 — did not exist at all**, so the request 404'd, the client swallowed it, and those search arms silently returned nothing. A client calling a non-existent endpoint and failing quietly is worse than an absent feature, because the feature looks present. Now implemented, requiring **two identifiers**: a card is shared, photographed and reused, so the number is a lookup key and never proof of identity. **Doc 43 D5 says to reuse the phase-15 ≥2-identifier rule — that rule was deliberately DELETED** with the challenge screen it belonged to (`VerificationPolicy`'s own comment calls a minimum on a set nobody submits "the shape of a control"), so it is implemented afresh here rather than reused from something that would not have run. Ambiguity is not a match; identifiers that match different people resolve to nobody. The payload is asserted to contain no clinical field over the SERIALIZED response, and echoes identifier TYPES never values. 7 tests. |
| 26 | Prescribing workspace | 26.7 Docs, routes, registry, ADR | ☑ | 2026-08-03 — Docs 11 (§2b `masterdata:read`, and why the grant is broad ON PURPOSE), 17 (`/drugs/search`, `/prescriptions/validate`, `/beneficiaries/resolve`, `CheckState`), 22 (`drug_indication`, `duration_days`, the diagnosis snapshot, the two append-only tables, `CardNumber`, §11.2b check states), 23 (§3 — validation states are NOT prescription states; they are orthogonal, and what they govern is whether submit is permitted), 00-README-INDEX + README (doc 43, count → 43). Kong route added for `/api/v1/drug-indications` — **the coverage guard caught it**; `/drugs/search` and `/prescriptions/validate` were already covered by prefix. OpenAPI drift gate re-run and 3 specs regenerated. **ADR-0032, not 0026:** the prompt asks for 0026, which phase 20 took, and the sequence has run to 0031 — the same collision phases 21 and 25 resolved by taking the next free number, recorded rather than silently applied. 8 invariants registered; the registry checker caught a test renamed in 26.1 and a frontend-test naming convention on the way. |
| 26 | Prescribing workspace | 26.8 Live label checking against openFDA (**ADR-0033**) | ☑ | 2026-08-04 — ADR-0032 chose LOCAL interaction checking. What it did not weigh is what happens if the table is never populated, and `masterdata.drug_interaction` holds **zero pairs** — so the check reported `NotChecked` on every line of every prescription since the day it shipped, and the dose check had no rule table at all. openFDA is added as a SECOND source, scoped to the drugs on the SAME prescription. **The label pass can never return green** (the user's decision): a label scan finds a mention or finds nothing, and "nothing" from prose is not reassurance — so it emits `NotChecked`, and dose/duration render as reference text with no verdict. Three hazards handled before they could mislead: openFDA is **snake_case** (web defaults bound every field to null, which would have reported "no label" for the whole catalogue), the `openfda` block is not `open_fda`, and salt stripping is **suffix-only** because stripping anywhere turns `sodium chloride` into `chloride` → benzalkonium chloride. INN→USAN synonyms added (paracetamol, salbutamol, adrenaline all 404 under their INN). Verified live in BOTH directions: warfarin+amiodarone is caught from each drug's own label. A latent bug surfaced on the way — drug names rendered as "5mg" because a dose was the fallback. |
| 26 | Prescribing workspace | 26.9 The prescription page: listing, money, substitution | ☑ | 2026-08-04 — Search → Open now navigates to `/pharmacy/rx/RX-…`, keyed on the reference printed on the paper in the patient's hand, so a reload or a link to a colleague lands on the prescription. Prescribed / dispensed / remaining are **three columns, never subtracted into one**: "14 of 30" and "16 remaining" answer different questions. Three tiles through the SHARED pricing path (eligibility → `libs/benefit-pricing`), never a second implementation, and **never 0.00 for an unknown** — a zero at a counter reads as "free". Building it needed the member→plan-version link 19.2b names as unimplemented; the data existed on `policy.coverage.source_plan_version_id` and was simply never projected. **Two real bugs found:** the web client had never sent `substitutedDrugId`, so a substitution chosen at the counter was recorded as a straight dispense of the original medicine; and the projection stores `"Pharmacy"` while callers send `"PHARMACY"`, which no SQL comparison honoured. Live: total 290.50 EGP, split correctly withheld — **no PHARMACY benefit rule exists anywhere** (all 208 are CONSULT) and the DB refused to seed one, which is the effective-dating guard working. |
| 26 | Prescribing workspace | 26.10 The fulfilment authorization + the same surface for labs & imaging (**ADR-0034**) | ☑ | 2026-08-04 — A dispense or a performed examination now **issues an authorization**: `AUTH-YYYY-NNNNNN`, `Kind=Fulfilment`, `Status=Issued`, separate from the prescription or order it was delivered against. The item keeps `ordered_code` and `fulfilled_code` as **two columns**, so a substitution cannot overwrite what the prescriber wrote — and approvals-service has no client for the prescription at all. Issued asynchronously on approvals' OWN queue: binding `pharmacy.events`/`orders.events` would COMPETE with policy-service on a point-to-point transport and silently stop the benefit accumulator. At-least-once is guarded twice (`processed_event` for a redelivered id, UNIQUE `fulfilment_ref` for a redelivery under a NEW id — the case the ledger cannot see). `Issued` is terminal and unreachable, and the reviewer inbox **keeps its `Review` default**: a few hundred dispenses a day would drown the twelve decisions, so the register is its own screen. Labs/imaging get the same page — ordered/performed/remaining, three tiles, a per-line control — but the control **asks the approval team** rather than offering a list, because `examination_type` records no equivalence and deriving one from the category would put "any radiology procedure" behind a button. **A real bug the tests caught:** a Guid key set on an entity reached through a tracked parent's navigation is taken to be an existing row, so the second dispense against a prescription generated an UPDATE of a row that does not exist. Verified live: substitution recorded on `AUTH-2026-000657` while `prescription_line.drug_id` still reads the prescribed molecule. |
| 26 | Prescribing workspace | 26.11 Why the counter could not quote a price — four defects on one chain | ☑ | 2026-08-05 — A screenshot of "the cost of this prescription could not be worked out" turned out to be four separate faults, three of them older and larger than the symptom. **(1) The contract named a field the server does not send** — `zRxPriceLine.lineId` vs `prescriptionLineId` — so EVERY pricing response failed validation and the client threw, while the endpoint had been returning a correct 290.50 EGP throughout. Nothing caught it because the dev fixture was written from the CONTRACT, so schema, fixture and tests all agreed with each other and none had seen the server; the OpenAPI gate cannot help on minimal-API routes that declare no response schema. Now guarded by `server-shape.test.ts` against REAL captured responses. **(2) The eligibility projection held display names where the closed vocabulary belongs** — three seeds writing `Laboratory`, `Consultation`, `Outpatient`, `Oncology` into the field the engine MATCHES on — so eligibility answered "no active coverage for LAB" to every seeded member who held laboratory cover. Invisible because the failure is indistinguishable from the truth, and PHARMACY worked only by the accident that `Pharmacy` matches `PHARMACY` case-insensitively. Seeds fixed, 96 rows backfilled, column now CHECK-constrained. **(3) A 403 was being reported as a benefit fact.** The cost-share route sat behind `policy:read` — the entire benefit product, which a pharmacist neither holds nor should — and the pricing client collapsed the refusal into "this plan does not price pharmacy at the resolved tier". Undetectable while that sentence was ALSO true. New narrow action `policy:price-lookup` (satisfied by `policy:read` OR `eligibility:check`), and `TierPricingFailure.Unavailable` so an outage stops masquerading as an answer — the money counterpart of ADR-0033's rule. **(4) The plans members are actually on had no benefit rules at all** (the 232 CONSULT rules belong to plan versions with zero members), so there was nothing to quote. Authored through the governed amend→validate→activate path, which exposed a fifth: `ex_plan_version_no_overlap` was checked per row, so a two-row activation swap failed or succeeded on the order EF happened to emit its UPDATEs — now `DEFERRABLE INITIALLY DEFERRED`. Also fixed 19.2b's own resolution to use the version IN FORCE ON THE SERVICE DATE rather than the one the member enrolled under, without which no amendment could ever reach an existing member. **Verified live: 290.50 total, 218.10 member, 72.40 payer, tier T1.** |
| 26 | Prescribing workspace | 26.12 The dispensing counter, rebuilt around what a pharmacist actually checks | ☑ | 2026-08-05 — **The patient is NAMED.** The header showed `•••1df2`, which answers "which row" on a worklist and answers nothing at a counter, where the question is "is this the person in front of me". It now renders the same `PatientContextBar` every clinical screen uses — one server-side, min-necessary, PHI-audited projection with the allergies alongside — rather than a name field bolted onto the pharmacy contract. **Each line carries what gets checked against the packet:** the trade name, the ACTIVE INGREDIENT under it (two trade names holding one molecule is the commonest prescribing duplication there is), dose · route · frequency · duration, and the UNIT PRICE — because "290.50 for the prescription" does not tell a pharmacist whether the expensive item is the antibiotic or the syrup, which is exactly the conversation when a patient cannot afford all of it. The ingredient is a client-side join against master data, the pattern `icdTitles` and `branchLabels` already use; the price comes from the pricing call the page already makes. **Absences are named, never blank:** "Duration not recorded", "Active ingredient not recorded", "No price" — a blank duration cell reads as a one-day course, and only one of those is a reason to ring the prescriber. **A fixed action bar** at the foot: a running count of what is about to be handed over (previously below the fold on any five-line prescription), Dispense all, and Audit — which re-reads the prescription and the price and REPORTS what moved rather than silently swapping numbers under a pharmacist who is about to confirm them. Its test caught a real defect: a failed re-read blanked the whole page, taking away the prescription a pharmacist was working from because a refresh timed out; a stale screen that says it is stale is the honest state. Per-line tick fills that line's remainder (`aria-pressed`, not colour). An off-formulary substitution now reads as "sent to the approval team" instead of a generic failure — the control working, not a fault. Tiles gained the tier, each share as a % of the total, and a left rule on what the patient owes. **Backfilled** `expires_at` on 4 of 8 prescriptions from the same policy arithmetic the creation path uses; `duration_days` deliberately NOT derived — "20 tablets BD" is ten days only if the quantity is the course, and it feeds the dose check. 7 new tests. |
| 26 | Prescribing workspace | 26.13 The same counter for the bench — action bar and unit prices on labs & imaging | ☑ | 2026-08-05 — The investigation-order page gets everything 26.12 gave the dispensing counter, because a bench and a counter are the same situation and a technician moving between them should not have to learn two screens. **Unit price per line**, lifted out of the tiles so the ROWS and the total read one payload — fetching it twice would let a row and a total disagree about what an examination costs. Today every one reads "No price", which is the honest state: no examination in master data carries one. **The fixed action bar** — a running count of what is about to be recorded, Perform all, and Audit, which re-reads the order and the price and REPORTS what moved rather than swapping numbers under a technician about to confirm them. **The patient is NAMED** via the same profile strip, with allergies alongside — a masked token answers "which row" and nothing a bench needs before drawing a sample. Expiry now carries how many days are left, not just a date. Per-line tick with `aria-pressed`. **A failed re-read no longer blanks the page** on either screen: the document a technician is working from stays up and the audit says it is unconfirmed — stale and known to be stale beats gone. 9 new tests; two invariants registered (`INV-A-STALE-COUNTER-SAYS-IT-IS-STALE`, `INV-AN-UNRECORDED-LINE-FACT-IS-NAMED`). |
| 26 | Prescribing workspace | 26.14 Counter polish — and the one-line bug it uncovered | ☑ | 2026-08-05 — Five presentation asks, and the last one turned out to be a defect. **`code: String(l.drugId).slice(0, 8)`** — a display shortening applied to a field NOTHING displays and three things use as an identity: the active-ingredient join, the approved-alternatives lookup behind the substitute control, and the drugId sent on submission. So every medicine read "active ingredient not recorded" and the substitute modal reported "no approved alternative is listed" for the ENTIRE catalogue, because master data answers 404 to a prefix. Proved live: full id → 200 with **8 alternatives**, truncated → 404. Both symptoms are states the screens are DESIGNED to render honestly, which is exactly why nobody saw it — an honest empty state is indistinguishable from a broken lookup unless something asserts the lookup's INPUT, which a test now does. **Names capitalised in sentence case, not title case:** the Egyptian drug list is lower-case across all 22,653 products, and title-casing would produce "600Mg", "I.V" and "F.C. Tabs" — a dose and a route turned to nonsense to fix a capital letter. Display-side, because the names are the market list's own and rewriting them would put a derived spelling where the authority's belongs; a no-op on Arabic, which has no case. **A purpose-built `replace` glyph** for substitution — `swap`'s two straight opposed arrows are the transfer icon and read as "move between two places"; a closed loop says "exchange this for an equivalent" and has a silhouette distinguishable from the tick beside it. **The per-row label is hidden, not deleted:** `hideLabel` was declared on `FieldBase` and honoured by `Labelled` but only `SelectField` ever passed it on, so on an `InputField` it fell into `...rest` and hit the DOM as an unknown attribute — plumbed through both it and `TextareaField`. **Alignment:** the substitute icon hugs the medicine instead of being stranded at the far edge of the widest column, and the quantity box is sized to the number it holds. 4 new tests; two invariants registered. |
| 26 | Prescribing workspace | 26.15 Diagnosis, the counter's note, the print slip — and a silently dropped keystroke | ☑ | 2026-08-05 — **The diagnosis is on the counter.** `diagnosis_snapshot` has existed since 26.4 and nothing read it: a medicine only makes sense against what it is FOR, and a pharmacist checking a broad-spectrum antibiotic against "acute sinusitis" is doing something a pharmacist handing it over blind is not. Codes resolve to titles through master data — the same client-side join the ingredient uses — and an empty snapshot says "no diagnosis was recorded" rather than showing nothing. It stays a SNAPSHOT: a correction to the encounter next week does not rewrite what was checked here. **A dispensing note** (`dispense_event.note`, migration 0011, capped at 500 by the database because that table is append-only): collection arrangements, a replaced lot, who collected — things the counter knows, says out loud, and used to lose. It rides the dispense, not the prescription, because it describes THAT handover; it is not clinical and is never read by the clinical checks. **Quantity validation.** `max` on a number input does not stop typing, and 17 against 14 remaining went to the server for a 422. Now reported per-line and on the bar, with Submit refused — NOT silently clamped, because rewriting 17 to 14 under the pharmacist's hand changes the figure they are about to confirm without telling them. **The confirm-dispense dialog removed** as asked; what still stands between a mistake and a patient is the zero default, the running count, the over-quantity refusal and idempotency. **The bar is genuinely pinned now:** it was `position: sticky` on a GRID ITEM, whose sticky containing block is its own grid area — so it stuck to a box it already filled and did nothing. `.rx-page` is a block flow, and the bar pins to the scrolling pane. `Dispense all` left the bar for the card head; **Print** appears only after a handover, and prints what just left the counter rather than the prescription's running totals. **A real defect the tests found:** the bench's quantity input read `e.currentTarget.value` INSIDE a functional state updater — React nulls `currentTarget` once the handler returns, so the entry was silently dropped and the DOM showed 99 while the page believed 0. 8 new tests. |
| 26 | Prescribing workspace | 26.16 The counter could not price anything — a session that outlived its scopes | ☑ | 2026-08-05 — Reported twice as "active ingredient and price fail to load", and neither the code nor the data was at fault: every API returned 200 to a fresh token, CORS passed, the contract parsed, the deployed bundle made the calls. The service log gave it up — `POST masterdata/drugs/prices/by-ids` answering **403** to the browser's forwarded token, while the same call with a newly minted token answered 200. **Token scopes are frozen at authorisation.** `masterdata:read` and `eligibility:check` were granted to the pharmacist role AFTER sessions had begun, and RFC 6749 §6 forbids a refresh from requesting scopes beyond those originally granted — so every silent renewal faithfully reissued the same gap and the session could never recover for as long as it lived. On a screen that handles money it rendered as "Active ingredient not recorded" and "No price": an authorisation gap reported as a fact about the medicine. **The SPA now compares its token's granted scopes with what the application requests, at restore and at renewal**, and ends a session that is short — one sign-in, then correct, for every user in that state without anyone diagnosing it. Fails OPEN on an unreadable token: a shape we cannot parse is not evidence of staleness, and re-authorising on it would loop a user through sign-in forever, which is worse than the 403. A service-account read was considered and correctly refused — `NoServiceAccountArchitectureTests` forbids it platform-wide. Also: row cells centred (the medicine cell is three lines and everything beside it clung to the top), the substitute glyph replaced with a double half-arrow, and the icon-button hover made legible at arm's length with no `translateY` lift and nothing at all offered when disabled. A Dev fixture that set `unitPriceEgp` directly on prescription lines was removed — the server never sends it, so the fixture was hiding the join it existed to exercise, the same trap as the `lineId` bug. 6 new tests; one invariant registered. |
| 26 | Prescribing workspace | 26.17 The patient and payer tiles now follow what is actually being dispensed | ☑ | 2026-08-05 — The three cost tiles all answered the same question — *what does the WHOLE prescription cost* — while a pharmacist was entering 7 of 14. A partial dispense is the ordinary case rather than the exception (stock is short; the member can only pay for part of a course today), so the two share tiles overstated what was owed at that moment by exactly the part not being collected. **The obvious repair is wrong, and wrong in the direction hardest to notice.** `libs/money` applies a deductible in full, THEN a copay, THEN coinsurance — the share is not linear in the amount, so scaling the whole-prescription figure by the ratio invents a number. Measured against the live engine on RX-2026-000042: the whole prescription (EGP 2,268) carries a member share of **613.60**, the two units actually being handed over (EGP 324) carry **224.80**, and scaling would have quoted **87.66** — under by more than half, on the one screen in the platform with no reviewer between it and the patient. So the basis travels to the server as `?dispense=<lineId>:<qty>` (`?perform=` on the bench) and the split comes back **composed by the benefit engine**, debounced and sequence-guarded so a slow answer for "1" cannot land on top of the answer for "14". The prescription total is deliberately left alone — it is what the prescriber wrote, and a member deciding whether they can afford to come back for the rest needs it to hold still — and each share tile now names its own basis ("For the 7 units being dispensed now" / "If all of it is collected") so one label never covers two meanings. An unknown line id in the basis is a **400**, not a line quietly skipped: a dropped line produces a smaller share that looks exactly as confident as a correct one. A basis worth nothing falls back to the whole-prescription question rather than quoting zero — "Patient pays EGP 0.00" on an untouched screen is the "free" reading the counter may never give by accident — and a failed re-quote clears the figures rather than leaving stale ones beside a changed quantity. The dev fixture gained the same deductible shape, because a flat percentage would have made the browser's scaled answer look correct in development and wrong in production. Ported to labs/imaging unchanged. 27 new tests (12 pharmacy, 8 orders, 7 web); one invariant registered; both service images rebuilt and verified live. |
| 26 | Prescribing workspace | 26.18 The reception dashboard and its schedule, redesigned | ☑ | 2026-08-05 — Three identical white KPI tiles and a schedule of chips that all read the same weight. The cards gained a subject glyph (calendar / check / cross) on a soft brand wash — `aria-hidden`, because the label already names the figure in words and the icon's job is letting the desk find the right tile by shape, not carrying meaning. `KpiCard` grew an optional `icon`; the label already had a flex slot with a gap waiting for one, so nothing else moved. In the schedule each appointment now leads with the **patient name at full weight** and demotes the time, which the hour band beside it already implies — it previously sat third in the reading order behind both. A **status accent bar** runs down the inline-start edge, so "who has arrived" is a scan down one edge rather than a read of eight pills; `border-inline-start` rather than a positioned pseudo-element, so it flips for Arabic without a second rule. The status word is still printed beside it (21-accessibility: hue may reinforce a state, never carry it), demoted from a bordered pill to an uppercase micro-label — inside a chip that is already a bordered box, a pill for one word is two frames deep. And the **doctor is named**, which the schedule did not do at all: "who is this person seeing" is the question the desk asks straight after "who is this". The join was lifted out of the visits column into one `doctorName` helper both halves call, because two joins over one map is how a screen starts naming the same person differently — and an appointment with no recorded doctor says so rather than leaving a blank that reads as a rendering fault. The schedule list gained an `aria-label`, without which it was one unnamed `list` among the page's others. 3 new tests; 814 green including axe over every route × locale × theme. |
| 26 | Prescribing workspace | 26.19 Scrolling, made one design across every portal | ☑ | 2026-08-05 — Every region that scrolls inside a page had grown its own answer to the same three questions. **Ten expressions capped ten regions** — `288px`, `320px`, `16rem`, `20rem`, `22rem`, `30vh`, `88vh`, `min(60vh,32rem)`, `min(70vh,640px)`, `calc(100dvh-84px)` — so the two dropdowns doing exactly the same job disagreed by 64px and the eleventh author had nothing to copy. Replaced by three role-named tokens (`--scroll-picker` 288px = eight 36px rows, `--scroll-panel`, `--scroll-sheet`), chosen by *what is this surface* rather than *how tall was the last one*; two surfaces are deliberately off the scale (the bulk-edit workspace, the docked notification pane) and say why inline. **Not one contained its overscroll**: reaching the bottom of an inner list handed the momentum to the page, so scrolling a select dropdown to its end scrolled the document behind it and moved the field the dropdown was anchored to out from under the cursor. **None styled a scrollbar**, and the platforms fail in opposite directions — Windows draws a ~17px grey slab (6% of a 288px dropdown spent on furniture matching nothing else on the page), macOS draws an overlay bar invisible until you scroll, so a list that scrolls looks exactly like a list that does not. One `.mrs-scroll` treatment now supplies containment plus a thin tokenised bar, declared twice (standard properties for Firefox/Chrome 121+, `::-webkit-scrollbar` for Safari and older Chromium — neither alone covers the browsers this runs on), applied at **23 sites**. The page's own scroller and the nav rail keep the platform bar on purpose: a thin bar on the surface a user scrolls most is a real ergonomic loss — the rail gets containment only. **Thirteen regions that scroll but hold no control were unreachable by keyboard** (WCAG 2.1.1); they are now tab stops with the focus ring `.mrs-wl-scroll` already had, lifted out of the data table so it is not one component's private arrangement — and deliberately NOT applied to listboxes, which the owning input's arrow keys already drive. **The route-wide axe pass could never have caught the keyboard half**: `scrollable-region-focusable` only fires when an element actually overflows, and jsdom performs no layout, so every box measures zero and nothing ever overflows — real coverage, structurally blind to exactly this. Hence a static gate: 7 tests, each verified to FAIL on an injected violation before being trusted. `vh` banned as a scroll cap except as the fallback before its `dvh` twin — on mobile Safari a `vh` box is taller than the visible page while the URL bar shows, so the last rows sit behind the toolbar unreachable. 821 green including axe over every route × locale × theme; one invariant registered. |
| 26 | Prescribing workspace | 26.20 Tables, made one design across every portal | ☑ | 2026-08-05 — **Five vocabularies for one thing**: `.mrs-wl` (the worklist, 36 screens), `.mini-table`, `.pol-grid`/`.pol-costshare`, `.rx-dispense-table`, and an inline-styled table on the effective-access preview that carried no class at all — invisible to every search for one. They disagreed on every property a column header has: three sizes (11.5px, 0.78rem, 13px), two colours, and two of the five with **no uppercase and no letter-spacing whatsoever**. The member screen renders `.mrs-wl` and `.pol-grid` inside the *same card*, where they read as two different products. Now one treatment from `--tbl-head-*` tokens, with the worklist's values because that is the table the platform is mostly made of and the one 0B §10c describes. **The numbers were the worse half, because it looked solved.** A money column is read by scanning down it, which only works when the digits stack — left-aligned, `9.50` and `12,400.00` start together and end apart. Thirteen of the app's fifty-six money renders wrapped their value in `.tnum`, but `.tnum` sets `font-variant-numeric` and nothing else, on a `<span>` inside a cell still `text-align: start`: the fix had been applied, it read as applied, and every column was still ragged. Alignment belongs to the **cell**, so it is now a `numeric` column flag that moves the cell *and its header* — 25 columns migrated, 10 redundant `.tnum` spans removed. Deliberately **not** "contains digits": case numbers, MRNs, order references and dates are numerals read left-to-right like words, and right-aligning them breaks the column beside them — they keep `.tnum`. Also: the trailing hairline dropped everywhere (the card's edge already ends the table; only `.mrs-wl` had ever dropped it), the bulk-edit table's sticky header given `border-collapse: separate` — under `collapse` the browser paints the border with the TABLE, so a stuck header carried its background up and left its rule behind — and a `--surface-2` band so it is distinguishable from the rows scrolling under it. **The gate found three things I had missed** (a `<td className="tnum">` money cell, an unstyled sr-only chart alternative, and an over-broad assertion of my own), and **one of its five assertions did not bite**: the last-row check matched the word `border` inside `border-bottom: 1px solid red` and passed while the hairline was still drawn — a check that asserted nothing and reported success, caught only by probing it with the defect it exists to find. All five now verified against an injected violation. 828 green including axe over every route × locale × theme; one invariant registered. |
| 27 | Approval supervisor authority | 27.0 ADR-0035 — master lists, the approvals engine, document validity (**spec, awaiting review**) | ◐ | 2026-08-05 — Survey first, and it changed what the job was. **The authority already existed and had no door**: `medical_director` already holds `admin:edit-masterdata` and carries `admin:write`, and `POST /api/v1/admin/master-data` is fully built — effective-dated, versioned, rationale-mandatory, audited. Two things made it unusable: `portalForRole` returns exactly one portal per role, so a director has no route to the Master Data screen in the `admin` portal; and that screen is **read-only anyway**, listing versions in force while the write endpoint sits with no caller. So this was never "grant the supervisor authority". **No approvals engine exists** — `RequiresPreauthAsync` reads one boolean off the plan version and that is the entire pre-auth decision; assignment is manual; SLA is a reported TAT, not a configured target. **Document expiry: the dates exist, the policy does not** — provider credentials carry `valid_from`/`valid_to` and practitioners a `license_expiry` with a working sweeper, but its warning thresholds are the hard-coded constant `WarningDays = [90, 60, 30]`; `document.document` has no expiry column at all and `patient.beneficiary_identifier` does not model a refugee card's expiry. The ADR rests on a principle the codebase already states in `AdminPolicies.EditValidityPolicy` — *the supervisor sets the parameter and absorbs the consequence of setting it wrong* — and generalises it across all six parameters. Four decisions: a Governance group on the director portal (route duplication, **not** multi-portal — making `portalForRole` return many would change navigation, the palette, deep-link resolution and the spa-scopes gate to serve one screen); a master-list editor over the existing endpoint with a version diff before commit, **leaving `The_reference_catalogue_exposes_no_write_path` untouched**; a three-family effective-dated rule engine, fail-closed, rule-attributed in the audit (`decided_by = rule:<id>@v<n>`, never a person — attributing a machine decision to a human falsifies a hash-chained record) with a tenant kill switch; and document validity mirroring `libs/validity` exactly. **I recommended AGAINST auto-reject** and said why: the failure modes are not symmetric — a wrong auto-approval costs the payer money and a human reviews the claim later, a wrong auto-rejection denies care to a refugee with nobody having looked, and per `libs/benefit-pricing`'s own header they have "no reviewer in the loop and no recovery path". The throughput is available without the harm by routing to a priority queue with the engine's stated reason attached. One open question raised for the reviewer (restrict master-list editing to clinical code systems?). Every factual claim re-verified against the tree after writing. Build order sequenced so each of the five steps ships usable alone. |
| 27 | Approval supervisor authority | 27.1 Master lists — the authority given a door, and an editor | ☑ | 2026-08-05 — Step 1 of ADR-0035's build order. **Nothing was granted that was not already granted**: `medical_director` held `admin:edit-masterdata` and carried `admin:write`, and `POST /api/v1/admin/master-data` was already complete — effective-dated, versioned, rationale-mandatory, audited. It had no door. `portalForRole` gives one portal per role, the only Master Data screen lived in the `admin` portal, and that screen was **read-only anyway**, listing versions in force while the write endpoint sat with no caller. An authority that is granted and unreachable reads, from outside, exactly like one that was never granted. Added: a **Governance** section group on the director portal (its own group, not folded into Oversight — overseeing the queue and setting the rules that fill it are different acts), a `/director/master-lists` route, its own `director.masterlists` permission key rather than reusing `admin.masterdata` (sharing a key would give whoever held either the reach of both), and a `MasterListAdmin` editor. **The editor's job is the governance, not the form.** An edit APPENDS a version and says so on the page — a prescription written last March still resolves the code as it read last March, which is why there is no delete and why retiring a code is itself a version. The rationale is mandatory client-side as well as server-side, framed as what somebody reads in three years asking why a code changed the week a claim was denied. A **version diff before commit** shows the version in force beside the proposed one, over the union of both key sets so a REMOVED attribute is as visible as an added one — the deletion is the change most likely to break a downstream read. A diff that could not be READ renders as "cannot be compared", never as an empty diff: an empty one says "nothing changes", the one thing it must not say when the truth is "we could not see". A malformed attribute line is refused rather than dropped. **ADR-0035's open question answered: clinical governance is bounded to ICD-10, CPT, LOINC and ATC.** The grant exists because a director absorbs the consequence of a wrong clinical code — it misroutes a diagnosis into their own queue — and that argument does not reach formulary tiers or allergen groupings. It narrows nobody: `super_admin` keeps every system, and the bound takes the caller's whole ROLE SET so holding both roles is more authority, never less. Non-clinical rows are shown with no control rather than a button that 403s. **`The_reference_catalogue_exposes_no_write_path` re-run and green, untouched** — every edit still goes through admin-service. Three of my API assumptions were wrong and the compiler caught all three (`writeErrorMessage`'s module, `SelectField`'s value-not-event `onChange`, `AsyncStatus` being "success" not "ready"). 21 new tests (7 domain, 10 screen, 4 existing suites re-run); 839 frontend green including axe over every route × locale × theme; 112 admin tests with `--with-db`; openapi-drift, kong-route-coverage, spa-scopes, invariant-registry all clean. |
| 27 | Approval supervisor authority | 27.2 Document validity — the constant a supervisor could not touch | ☑ | 2026-08-05 — Step 2 of ADR-0035. The provider-credential sweeper worked and warned at 90/60/30 days — from `PractitionerLicence.WarningDays`, a **compiled-in constant**, so the one number a Medical Director most obviously owns was the one they could not set. `document.document` had no expiry column at all and `patient.beneficiary_identifier` did not model a refugee card's expiry. Added `libs/validity/DocumentValidityPolicy` as the deliberate sibling of `ValidityPolicy` — same store (`system_config`), same fallback discipline, same bounds discipline — plus its three-layer HTTP source (fresh → last-known-good → compiled default), the `/api/v1/admin/document-validity` read/write pair on the **same** `EditValidityPolicy` action (a separate action would be a second answer to "who decides how long something stays good for"), and a supervisor screen splitting the two families apart: an identity document's lapse stops somebody being **SEEN**, a credential's stops somebody **PRACTISING**, and one table under one heading would present two different judgements as the same call. **Two numbers, not one** — a renewal cadence and a threshold LIST, because the behaviour being replaced fires at three points and a single threshold would have silently deleted two of them. **Two columns, not one**, in the expand migration: `expires_on` alone cannot distinguish "the card says March" from "nobody recorded one so the cadence suggests March", and Mersal does not decide when a government-issued card lapses — a policy-computed date shown as the real expiry would be the platform inventing a fact about a refugee's papers. A CHECK enforces that date and source travel together. **An empty threshold list is refused in three places** (parser, endpoint, screen): "warn at no point" would silence every expiring credential and the failure would look exactly like a quiet week, so every fallback direction is toward MORE warning. The default still reads exactly 90/60/30 — moving a value into configuration must change who may set it and change nothing about what happens by default, or any later surprise is impossible to attribute; 91 provider tests confirm the sweeper is unchanged. **Live testing caught a real defect in my own design**: the enum invented `RefugeeCard`/`ResidencyPermit` while `beneficiary_identifier.identifier_type`'s CHECK spells the vocabulary `NationalID, Passport, RefugeeID, UNHCRNo, MemberNo` — a policy that would have been configured, saved, audited and matched **no row in the database**. Realigned, with `IdentifierTypeFor`/`KindForIdentifierType` making the tie explicit and testable; the reverse mapper is a switch and not a search, because `FirstOrDefault` over an enum returns the enum's DEFAULT on no match and would have governed an unknown type by NationalId's policy. `MemberNo` maps from nothing on purpose — Mersal issues it and it lapses on nobody else's timetable. Both migrations verified applied, replayed clean (idempotent via DO-guards rather than DROP-then-ADD, which the migration-compat gate correctly cannot distinguish from a contract-phase drop), and both CHECKs probed with the defects they exist to refuse. 21 new tests (12 library, 9 screen); 849 frontend green including axe over every route × locale × theme; 47 validity-library tests; openapi-drift regenerated and clean. |
| 27 | Approval supervisor authority | 27.3 The approvals engine — routing and SLA rules | ☑ | 2026-08-05 — Step 3 of ADR-0035, and the first engine family. **Chosen first because it cannot decide anything**: routing and SLA change WHO decides and BY WHEN, so the worst outcome a bug here produces is work on the wrong desk, not a benefit decision made without a human. Two constants replaced: assignment was manual, and `SlaOptions` (1/4/48 hours by priority) was deploy-time appsettings. Built the `approvals.rule` store — effective-dated, append-only, RLS by tenant, rationale enforced by a CHECK that also refuses a blank one and a window that closes before it opens — plus a **pure, synchronous evaluator** so the decision routing a beneficiary's request can be tested without a database, a clock or a server. **The order is total** (priority, then rule id): a partial order would leave two same-priority rules resolving by whatever the query returned first — the same request routed two ways on two days with nothing changed and nothing to point at. **A malformed predicate is SKIPPED, never treated as a catch-all** — the dangerous reading is the convenient one, because a broken catch-all would swallow the whole queue onto one desk while sitting in the list looking live. **Fail-closed means the DEFAULT, not nothing**: if the rule store cannot be read the request still lands in the queue that existed before rules did and still keeps its priority-based deadline, because a request with no deadline is worse than one with a generic deadline and a request nobody can see is worse than one routed imperfectly. **Routing may only name a declared queue** — a typo would otherwise send work somewhere nobody watches, and the symptom is a queue gone quiet, which reads like a good week. ADR-0035 asked for a watcher count; there is no watcher model in the platform yet, so this is the achievable half of that intent and says so. New scope **`auth:configure`, deliberately NOT granted to `medical_approval`**: deciding one case and authoring the rule that shapes a thousand are different powers, and a reviewer who could edit their own routing could route work away from themselves — verified live that the grant landed on `medical_director` and `super_admin` only, and tested that scope-without-role is still refused. The screen keeps **superseded versions on display, dated**, because the question a supervisor actually asks is "why did this go there last week" and today's rules cannot answer it; predicates render as sentences rather than JSON; a catch-all is **warned about rather than refused**, since it is how you give unmatched work a home but above a specific rule it takes everything. Three API assumptions were wrong and the compiler caught all three (`SegmentedControl` takes `segments`/`aria-label`, `SelectField` is a custom listbox not a native select, `AuthSource.OrderLine` not `Order`). **Live probing caught two defects the test suite could not.** (1) `JsonSerializerDefaults.Web` gives camelCase and case-insensitive properties but still expects enums as NUMBERS, so a predicate arriving as `{"priority":"Emergency"}` failed to parse and a valid rule was refused as malformed — with a message that was true and useless. No unit test could catch it: they all SERIALIZE with the same options they deserialize with, so a number goes in and a number comes out and the round trip is always clean. Fixed, and pinned by two tests that assert a literal wire string — including one proving a number still parses, since rules stored before the converter are live and refusing them would silently disable a supervisor's existing routing. (2) `identity.role_scope` is TENANT-SCOPED and the platform-default row does not stand in for a tenant with its own grants: the first migration inserted only the default, and the demo tenant's director got a token that authenticated fine and 403'd on the first request — the scope existed, the client could request it, the token was quietly short. Corrected to the per-tenant shape 0026 had already learned. Also: the web image build failed on an unused import my `tsc` run missed because I ran the tests after that edit and not the typecheck; and a compose `build web approvals-service identity-service` aborts the whole command on the first failure, so approvals was never rebuilt and the endpoint 404'd for a reason that had nothing to do with it. 32 new tests (17 evaluator, 4 authz, 11 screen); 861 frontend green including axe over every route × locale × theme; 99/99 approvals with `--with-db`; migration applied, replayed clean, all three CHECKs probed with the defects they refuse; openapi-drift regenerated; spa-scopes now 80 scopes all requested. |
| 27 | Approval supervisor authority | 27.4 The approvals engine — pre-authorization triggers, additive only | ☑ | 2026-08-05 — Step 4 of ADR-0035. The third rule family, and **the additive-only property is structural rather than checked**: `PreauthAction` is a record with one string, so there is no rule anybody can write — by hand, by migration, or by a future author who thought a boolean would be convenient — that removes a gate the plan makes. The plan version's `RequiresPreauth` is a contractual term between the payer and Mersal, and a local rule able to switch it off would silently override a contract and surface months later as a denied claim nobody could trace to a configuration change. Reinforced at the call site: the rules are consulted **only when the plan says no**. **An UNKNOWN amount never clears an amount floor** — an absent figure is not a small one, and treating null as zero would let exactly the requests nobody could price slip under an "anything over X needs a look" rule, which is the set most likely to deserve the look. **A catch-all is REFUSED**, unlike routing where it is merely warned about: for routing it gives unmatched work a home, for pre-authorization it would put every act of care on the platform behind a decision — not a configuration change but a service outage with a benefit rationale. And the fail-safe runs the OTHER way from routing: if the rules cannot be read the plan's answer stands and no requirement is invented, because gating care nobody chose to gate over a brief database outage would stop a beneficiary being treated for an infrastructure reason. A pre-auth rule must also carry a **reason shown to the provider**, separate from the audit rationale: two audiences, and "authorization is required" with no account of why is how a gate becomes something people work around. **Wiring the rules into `/preauth-required` found that endpoint had never worked at all** — four pre-existing defects stacked: it was never registered in `Program.cs`; registering it threw at route-table construction because `TierPricingService` is a plain class and minimal APIs inferred it as a BODY parameter on a GET (which would have taken out EVERY route on the service, since endpoint construction is one composite operation); `AddHbmpTierPricing` — whose own summary names approvals as a consumer — had never been called there; and then **`HttpNetworkTierResolver` threw `EnsureSuccessStatusCode` on a 403 instead of degrading**, which is a bug in shared library code that would have 500'd eligibility and claims the same way. That last one is fixed properly: the method's contract is already "null means the tier could not be resolved", the callers already read that as `TierUnresolved` and require authorization, so a non-success now logs which dependency refused and fails closed. Verified live: the endpoint answers `required=true, determinate=false` rather than throwing. **Not yet proven end to end**: the additive rule firing on a request the plan does NOT gate, because the director's token is refused by provider-service's tier resolver — a pre-existing scope gap independent of this work, now visible instead of fatal. 7 new domain tests and 5 new screen tests (24 evaluator, 16 screen); 112 approvals and 63 eligibility with `--with-db`, 131 claims, 19 benefit-pricing. |
| 27 | Approval supervisor authority | 27.5 The approvals engine — bounded auto-approval behind a kill switch | ☑ | 2026-08-05 — Step 5, the last of ADR-0035's build order, and the only place in the platform where money is committed with no human in the loop. **Every gate fails toward the human**: there is no input — not a missing switch, not an unpriced request, not an unreadable rule — that produces an approval by omission. **NO ROW MEANS OFF.** Auto-approval is opt-in per tenant and stays opt-in, because a new tenant, a restored database and a failed migration all produce "no row" and every one must mean nobody is paid without a human having looked. The switch lives in **approvals' own schema, not admin-service**: the control somebody reaches for at 02:00 must not depend on another service being reachable, and "could not read it" has to resolve to off deterministically rather than on whether an HTTP call timed out. It edits no rule — a switch you can only reach by editing the thing that is misbehaving is not a kill switch — so turning it off stops every auto-approval instantly and leaves the rules in place for an investigation afterwards rather than first. **The ledger can no longer name the wrong author.** `reviewer_id` became nullable and `decided_by_rule` sits beside it under a CHECK that **exactly one** is set, probed live in both directions. Attributing a machine decision to a human is a falsified audit record and this ledger is hash-chained precisely so that cannot happen later — writing a person's id would falsify it at the moment of writing instead; a sentinel Guid meaning "the system" is worse still, because it reads as a person's id everywhere it is joined and every report and investigation downstream would need to know the magic number. **A ceiling on the ceiling**: a per-rule maximum bounded by a platform hard maximum of EGP 5,000, because otherwise "bounded" would mean bounded by whatever the last person to edit a rule typed and one mistyped figure would be the entire control. An **unpriced request is never auto-approved** — "we could not work out what this costs" is not a small amount — and an outstanding clinical warning refuses, because a clinical check may only WARN and approving past one unread would turn warn into ignored. A **catch-all is refused outright**: approving ANY request under the ceiling without a human is the worst rule anybody could write. Every refusal has its own value so a supervisor whose rule never fires learns whether the switch is off, the amount was over, or a warning was outstanding — three different remedies, where "it did not auto-approve" is not an answer anybody can act on. **There is no AutoReject family and its absence is asserted, not assumed**: a wrong auto-approval costs the payer money and a human reviews the claim later, while a wrong auto-rejection denies care to a refugee with nobody having looked — per libs/benefit-pricing's own header they have "no reviewer in the loop and no recovery path". The throughput is available without the harm by routing to a priority queue with a stated reason, which the Routing family already does. Verified live end to end: switch off by default → reason required in both directions → catch-all refused → ceiling above 5,000 refused → narrow rule publishes → switch off again with the rule surviving but inert. 12 new domain tests and 8 new screen tests; 124 approvals with `--with-db`. |
| 27 | Approval supervisor authority | 27.6 The pre-auth path, closed end to end | ☑ | 2026-08-05 — The open item from 27.4, finished. Approvals-service forwards the CALLER's token (the platform forbids service accounts, so every read is attributable), and neither approvals role held the two scopes the pre-auth question needs: `provider:read` to resolve the provider's tier, and the narrow pricing-lookup reach to ask what the plan says at that tier. Granted both — `eligibility:check` rather than `policy:read` deliberately, since it buys the cost-share lookup and not the authored policy book, the plan editor or the version history: an approver needs to know what a plan says about THIS care at THIS tier, not to read every plan the payer has written. Per-tenant, per 0027's lesson. **And the sibling of the library bug from 27.4 was still there**: `HttpBenefitCostShareSource` had the same `EnsureSuccessStatusCode`, so fixing only the tier resolver moved the 500 one call further down the same method — two call sites in one file, one line apart in intent, and only one had been looked at. Both now log which dependency refused and fail closed, which their own contracts already described. **Proven end to end at last**: on a plan version that prices LAB at T1 without requiring authorization, `required=false` before the rule and `required=true, requiredByRule=<id>` after it, with the supervisor's reason returned to the caller — while LAB under the floor and CONSULT at the same amount both stay ungated. Two things found on the way and recorded rather than smoothed over. Kong holds a stale upstream after `--force-recreate` and answers 404 until restarted, which reads as a missing route. And policy authored cost-share against a network tier id that **does not exist in provider-service** (`aaaaaaaa-0000-…`, alongside 10 rows correctly on the real T1) — a seed inconsistency between two services that surfaces as a permanent "not priced at this tier", i.e. as a benefit answer rather than as the data gap it is. **A comment of my own was corrected rather than left flattering**: it claimed an unknown amount not clearing a floor protected the requests nobody could price, when the code gives no such protection — that case is gated earlier by the plan path's own indeterminacy, and what actually remains is a caller who could send an amount and does not. Closing THAT means making the amount mandatory on the question, which is its own decision and is now written down as one instead of implied by a comment. |
| 27 | Approval supervisor authority | 27.7 An editor over a list its own author could not open | ☑ | 2026-08-05 — Reported from the running screen: three "You don't have access to this page" panels on the supervisor's Governance pages. Two separate causes, and the audit chain separated them. **One real bug of mine.** `GET /api/v1/admin/master-data` gates on `AdminPolicies.ReadAccess`, which is `org_admin, super_admin` — so 27.1 gave clinical governance the master-data EDITOR while the list behind it still answered to the admin-only read. Granting the write and forgetting the read is the same defect as granting the authority and giving it no door, which is the thing ADR-0035 set out to fix; I reproduced it while fixing it. Widening `ReadAccess` would have been wrong — it also gates the access matrix, the SoD matrix, break-glass and the access-review campaigns, and a Medical Director reading the ICD table is not one reading who can do what on the platform. Added a narrow `admin:read-masterdata` held by the editors plus the platform admins, applied to the two master-data reads only, and pinned it with a test asserting **whoever may EDIT may also READ** — a subset check that would have failed the moment 27.1 shipped — plus its converse, that the master-data read does not drag the platform-admin reads along with it. **The other two panels were not a bug.** The engine's 403s produced NO audit event at all, which is itself the diagnosis: the handler never ran, so the framework-level `auth:configure` check refused before the ABAC gate could record anything, meaning the browser's token simply predates the grant. The session guard from 26.16 catches exactly this — but at RESTORE and at renewal, so a tab left open across the grant keeps its short token until it reloads. Working as designed, and worth knowing rather than worth changing: a guard that re-checked on every request would be re-parsing a JWT on every fetch to catch a case a refresh already handles. All four governance reads verified 200 afterwards; 114 admin tests with `--with-db`. |
| 27 | Approval supervisor authority | 27.8 The lab and imaging benches, search-first like the counter | ☑ | 2026-08-05 — The same two changes the dispensing counter got, applied to fulfilment. **The duplicate section removed, not renamed**: "Order Queue" and "Consume Order" both routed to the SAME component on both the lab and imaging rails, so each offered one screen twice under two names — exactly the duplication pharmacy had. What is left is one entry, "Perform Order". The `/queue` paths stay resolvable so a bookmark lands on the bench rather than a 404. **And browse became search.** The bench listed every open order in the tenant — a board a technician scrolls looking for the patient in front of them, which is both the wrong workflow and the wrong disclosure: it put other patients' orders on screen to reach one. It now opens on the question, with the counter's asymmetry intact — an ORDER NUMBER identifies the order on its own, a CARD NUMBER does not identify a person and takes a second identifier alongside it (doc 43 §7 D5). **The beneficiary resolver was EXTRACTED rather than copied.** `IBeneficiaryResolver`, its four-outcome result and the patient-service implementation moved from pharmacy into `libs/beneficiary-lookup`, and pharmacy now consumes the shared one. Two counters asking "who is this member" must answer identically — including on the failure paths, which are the ones that matter and the ones a second implementation drifts on first: orders' search would otherwise have had to re-derive that a 404 means "matches nobody" while a 403 means "we could not ask", and the pharmacy counter has already shipped that bug once. The orders search gained the same three refusals with the same distinct meanings: 422 for one identifier, 503 for an unreachable directory, empty only for a real miss. One wiring detail worth recording: the shared resolver asks the factory for a client NAMED "patient", which orders did not register — without it a relative URL throws, and it throws an exception the resolver deliberately does not catch, because a misconfigured host is not a fail-safe "member not found". Six tests updated to search before asserting, and one moved off `/lab/queue` entirely — it had used the bench as a convenient example of a load-time error state, which the bench no longer has. **And the grant the benches never needed until now.** `pharmacist` has held `patient:read` since the counter was built; `lab_tech` and `imaging_tech` never did, because they never asked. Verified live: one identifier 422, two identifiers 503 — the honest fail-safe ("the directory could not be reached, this is NOT a report that the patient has no orders"), which is the right thing to SAY and no substitute for being able to ask. Granted per-tenant, and the same three probes then read 422 / 200 / 200. Third time this session that a new read path met a scope nobody had needed before, and each time the fail-safe held rather than the screen inventing an answer. 874 frontend green; 75 orders and 82 pharmacy tests unchanged, which is the point of the extraction. |
| 28 | In-app login & password reset | 28.0 ADR-0036 — signing in without leaving the application (**spec, awaiting review**) | ◐ | 2026-08-05 — Survey first, and it separated one request into two unequal halves. **The login is a second application and looks like one**: `AccountPages.cs` is 349 lines of C# string-literal HTML — password page, TOTP challenge, enrolment, recovery codes, membership chooser — with its own inline CSS, its own EN/AR dictionary and its own accessibility, a complete parallel implementation of things `@mersal/design-system` already owns, with nothing keeping the two in step. The SPA's live login is one button whose entire job is to send you elsewhere; its own header comment already records the copy-level version of that smell. Kong **already** routes `/connect` + `/.well-known` to the issuer *"so the SPA reaches one origin"* — and `VITE_OIDC_AUTHORITY` defaults to `:8090` while the app serves from `:5173`, so the one-origin intent was designed and then not wired. **Password reset does not exist at all**: no self-service anything, and the administrative endpoint takes a `newPassword`, so an admin chooses and therefore knows it. **And its delivery leg does not exist either** — the registered `IEmailProvider` writes a log line and returns success. The decision that shapes everything: **signing in is up to four steps, not one** (password → TOTP → enrolment → membership selection, plus lockout and deactivation), which is why the obvious shortcut is refused — **the token endpoint has no way to say “now give me your TOTP code”**, so a password grant would not be a login redesign but the quiet removal of MFA from a platform whose admin scopes and break-glass are gated on it. Five decisions: a first-party status API (`authenticated` / `two_factor_required` / `membership_selection_required` / `locked` / `invalid_credentials`) that establishes the ordinary Identity cookie and never returns a token, followed by the EXISTING PKCE authorize with `prompt=none`; one origin as a hard requirement with a gate, because `SameSite=Strict` — already set, on the stated assumption that nothing here is cross-site — would silently drop the cookie and the symptom would look like bad credentials; reset on ASP.NET Identity's security-stamp token (single-use for free, **no migration**) under a NAMED provider so a 30-minute reset does not shorten every other data-protection token; revoke-all-sessions on success and **no touching of the second factor**, or mailbox control becomes account takeover; and the server pages kept but **frozen**, since OIDC still needs an interactive login and two evolving login surfaces is the duplication this fixes. **Three findings recorded rather than smoothed over.** `prompt=none` is not honoured today — authorize challenges to the HTML page, so the SPA would be bounced to the page it exists to replace; the protocol's `login_required` is also what breaks the redirect loop. The login path is where `RecordAttemptAsync` and the concurrent-session cap live, so an API that forgets them does not fail loudly — it silently empties the screen that exists to show a user their account is being attacked. And **there is no Content-Security-Policy anywhere in the repository**: typing the password into the SPA does not create the XSS risk (an attacker with script execution can forge a login form under either model) but it removes the forgery step, so CSP is sequenced as a prerequisite, not a follow-up. Reset **refuses to lie about delivery** — 503 and a hidden link when no provider is configured, never “we have sent you a link” while nothing was sent. Trade-offs taken on the record: lockout IS distinguishable (the alternative sends a locked-out nurse to reset a password that was never wrong, and does not unlock her), and no per-account reset cap yet. Six build steps, each shippable alone, with the existing flow working until the last. |
| 28 | In-app login & password reset | 28.1 The credential rate limit partitioned on the gateway, not the client | ☑ | 2026-08-05 — Found while surveying for "one origin", which would have made it permanent, so it went first and the rest of ADR-0036 moved down one. **Pre-existing, live, and not a weak limit — a pre-authentication denial of service on signing in.** `IssuerRateLimits` partitions on `RemoteIpAddress` so that *"one abusive source cannot deny service to the rest of the tenant"*; behind the gateway that address IS the gateway, so the sentence was exactly inverted — one bucket of ten credential requests per minute for every caller on the platform, exhaustible by anyone with no account and no credentials. **Proven before it was written down**: twelve POSTs to `/connect/login` through Kong from the host gave ten 200s and two 429s; a request from a DIFFERENT source IP immediately afterwards got 429; the same source got 200 once the window elapsed. `Server: Kestrel` and `Retry-After: 60` on the 429 identified the limiter rather than Kong, whose own counter still showed 1189 of 1200 remaining. **Two independent routes led there**, which is why reading either alone would have missed it: in Development `UseHbmpTransportSecurity` returns BEFORE `UseForwardedHeaders` ever runs, and outside Development it runs but `ForwardedHeadersOptions.KnownProxies` defaults to loopback — a gateway on a container network is not loopback, so the header is silently ignored and the outcome is identical. Fixed with a **pure `ClientPartition.Resolve`**, because the whole thing is hop arithmetic and both ways of getting it wrong are invisible at runtime: one hop too few and the proxy's own address is the key again, one too many and a value the CLIENT wrote is the key, which lets an attacker rotate a header for a fresh bucket per request. The chain is counted from the RIGHT so a client-supplied prefix is unreachable, and clamped at 0 rather than refused — a shorter chain than configured degrades to the earliest address anyone recorded, where refusing would degrade to the shared bucket, which is the outage. The unattributable bucket stays shared and stays short (18.B3's reasoning is unchanged) but now **reports itself**: a silent fallback is how this comes back after someone edits a hop count, and it would look exactly like a busy morning. Hop count moved into `Forwarding__TrustedHops` and STATED in compose rather than defaulted, because it becomes 2 the moment nginx fronts the gateway. **Every new assertion was probed with an injected violation before being trusted** — reinstating the original behaviour failed 12 of 16, and silencing the misconfiguration signal failed exactly 1. **And the first version of the fix shipped doing nothing.** Live re-probe still 429'd; a temporary diagnostic showed `peer=::ffff:172.18.0.4 xff='172.18.0.1' key=::ffff:172.18.0.4` — the forwarded header had been correct all along. Kestrel listens dual-stack, so a peer that connected over IPv4 arrives IPv4-MAPPED, `IPNetwork.Contains` compares address family first and returns false across families, so every IPv4 CIDR rejected the gateway and the header was discarded as a stranger's. A green unit suite sat behind a fix that changed nothing in production — the missing test was the mapped-address one, now written, along with its converse (a real IPv6 client must NOT be rewritten, or distinct clients merge into one bucket — the same defect from the other direction). Normalisation happens ONCE and both the trust check and the key derivation read that value, rather than 27.6's shape of two call sites one line apart with only one looked at. **Verified live after the fix**: host exhausts its bucket at 10 → a different source IP immediately gets 200; that source then gets its own 10-then-429, so the limit still bites per client; and three spoofed `X-Forwarded-For` values from an already-exhausted host all stay 429, confirming the client-supplied prefix is out of reach. 20 new tests; 143 identity tests with `--with-db`, all green. |
| 28 | In-app login & password reset | 28.2 One origin, and the first Content-Security-Policy the platform has ever had | ☑ | 2026-08-05 — Step 2 of ADR-0036, and the half of "stop moving to another platform to log in" that is purely topology. The SPA served from `:5173` while the API was `:8000` and the issuer `:8090` — **three origins for one application**. `kong.yml` has routed `/connect` and `/.well-known` to the issuer since phase 17 with a comment saying it is done *"so the SPA reaches one origin"*, and the SPA pointed at `:8090` anyway for two years, because **nothing compared the two**. An intention recorded in a comment is not a constraint. Now `/api`, `/connect`, `/.well-known` and `/identity` are proxied to the gateway by the app's own nginx (deployed) and the Vite dev proxy (development) — the same arrangement in both, because a dev/prod split in AUTHENTICATION TOPOLOGY is the split most likely to be discovered in production. **The move was cheap for a reason worth stating**: `iss` is pinned via `Issuer__PublicUrl` (the ID2088 fix), so it does not follow the host the request arrived on and not one of the 19 services pinning `Auth__ValidIssuers` was touched. **`tools/ci/check-login-origin.py`** now fails the build when the three drift apart, is registered in `REQUIRED_GATES` so its SILENCE alarms too, and was probed with three injected violations before being trusted (authority back on `:8090`, API base back on `:8000`, redirect made relative — each caught, with the right message). It found real drift on its first run: **`.env.example` still configured KEYCLOAK** at `localhost:8080/realms/mersal`, retired in phase 17 — the same defect `LoginPage.tsx` records having carried in its user-facing copy, telling every developer who opened the file that their credentials went somewhere they no longer do. **And the platform had no Content-Security-Policy at all.** It has one now, sequenced BEFORE the SPA login rather than after: once a password is typed into this origin an injected script can keylog it, where today it would have to forge a convincing login form first. `script-src 'self'` is strict and asserted to have no `unsafe-inline`/`unsafe-eval`/`*` — a weakened one would still read as a Content-Security-Policy in any audit. `style-src` keeps `'unsafe-inline'` and that is recorded as a real concession with its cause (Radix's dialog pulls in react-remove-scroll, which injects a `<style>` element to lock the body); `blob:` stays on `img-src`/`frame-src` because documents and member photos are authorized audited STREAMS rendered from object URLs, and dropping it would break every preview as "document unavailable" — a lie about the document rather than about the policy. **Three defects found and fixed on the way, none of them mine to begin with.** (1) `preserve_host` is a Kong ROUTE property, not a service one — setting it on the service does not get ignored, the gateway refuses to parse and every route 502s. (2) It was needed because Kong rewrites Host to the upstream, so OpenIddict — which derives its ENDPOINT urls from the request, unlike `issuer` — published `authorization_endpoint: http://identity-service:8080/connect/authorize`: a container hostname no browser can resolve, advertised as where to send users. Pre-existing and invisible, because our client hand-builds its URLs and the only entry point that answered correctly was the `:8090` port this step stops using — the document was right on the path nothing used and wrong on the path everything now takes. (3) `$host` strips the PORT, so the first fix published `http://localhost/connect/authorize`, one port away from the application — a harder thing to notice than a wrong hostname; `$http_host` is the client's Host verbatim. **Verified live, end to end, through the app origin**: login POST 302 with `mersal.idp` set and held (which is the SameSite=Strict proof), authorize → code → token exchange → a valid token with `iss=http://localhost:8090/`, `roles=lab_tech`, `amr=pwd`; the CSP header present on the shell with zero inline `<script>` blocks in the shipped bundle and only the two fonts.* hosts referenced; and every probe answering IDENTICALLY through `:5173` and `:8000` (`/identity/me/sessions` 200/200, `/api/v1/queues` 403/403 — an authorization answer, not an authentication one). **`Forwarding__TrustedHops` raised 1 → 2 in the same change**, because this step adds a proxy and leaving it at 1 would have restored 28.1's platform-wide sign-in outage via a change that never mentions rate limiting; re-proven with the new chain (host exhausts at 10 through `:5173` → a different source IP immediately gets 200) and the one-hop path still correct by clamping. One 401 during verification was **my own broken probe** sending an empty bearer, not a defect — checked before reporting it as one. 14 new tests; 888 frontend green (65 files) including axe over every route × locale × theme. login-origin, build-status, spa-scopes, kong-route-coverage and invariant-registry all clean. **Two things recorded, not fixed**: `migration-compat` is red on four pre-existing contract-phase operations in callcentre and emr migrations this phase never touched; and the discovery document's `issuer` (`:8090`) no longer matches the URL it is served from, a known deviation from OIDC discovery that follows from pinning — correcting it means moving `Auth__ValidIssuers` on 19 services and invalidating every live token, which is its own decision and not one to smuggle into a topology change. |
| 28 | In-app login & password reset | 28.3 `prompt=none`, and a sign-in API that returns a status instead of a token | ☑ | 2026-08-05 — Step 3 of ADR-0036. Server-only, no UI, so the SPA login lands on something already proven. **`/connect/session/*` establishes the SAME Identity cookie the forms have always set and mints nothing**; the token still comes from the unchanged authorization-code + PKCE flow, which is why the frozen token contract, membership re-resolution, scope narrowing, `MfaEvaluator` and refresh rotation are all untouched — none of them is reimplemented. **The status vocabulary is the design**, because signing in here is up to four steps and the obvious shortcut cannot express any of them: the token endpoint has no way to say *"now give me your TOTP code"*. Proven rather than argued — after the password step on a 2FA account, a silent authorize answers `login_required`, so the password alone produced nothing usable. **`prompt=none` had never been honoured**: authorize challenged to the HTML login, so the SPA would have been bounced to the very page this ADR removes and would have followed that redirect forever. Now `login_required` with no session and `interaction_required` when the membership cannot be resolved — two errors because two remedies, and collapsing them would send a signed-in user back to a password prompt to answer a question about which organization they work in. **A sixth status was added while building the five**, and the ADR amended rather than the code bent: `no_membership` is reachable only with correct credentials so it leaks nothing, and folding it into `invalid_credentials` would tell somebody whose password was exactly right that it was wrong — §5.2's lockout argument from another direction. **The parity tests are the ones that matter**: `RecordAttemptAsync` and `OpenAsync` live inside the form handlers, and an API that forgot them fails silently — it empties the screen a person uses to see their own account being attacked, and stops applying the concurrent-session cap. So each test signs in BOTH ways and asserts the two produced the same record; probing that by neutering the API's calls failed 6 of 9, and probing `prompt=none` by ignoring it failed 3 of 24. **Four defects found in my own work, three of them only visible by running the thing.** (1) Antiforgery tokens are bound to the AUTHENTICATED user, so the token fetched while anonymous stops validating the instant the password step signs somebody in — the membership tests failed at the chooser while every step before passed. Every reply now carries a fresh token, rather than a documented rule to re-fetch, which is invisible when broken because it only bites on sequences that have a second factor. (2) The rate limit read its configuration EAGERLY at service registration, before `WebApplicationFactory` adds its overrides — a setting that appeared to exist and did nothing, reporting 429 while the value said 10000; resolved per request from a singleton now, and the shipped defaults (10/min, 60/min) are pinned by a test because making a security limit configurable is only safe while the default is the safe one. (3) My tests left users and a second TENANT in the shared identity database, which made `TenantFeatureProjectionTests` fail with a message about a backfill — an unrelated test, correctly. Found only by running the FULL suite rather than the filter; seeds now remove themselves via a disposable, and the 168 stray rows were cleaned out. (4) Two of my own probes read the response stream twice and compared whole bodies that differ by a per-request token — test bugs that read like server faults, fixed rather than worked around. **Verified live through the app origin**: a wrong password and an unknown username return byte-identical bodies apart from the rotating token; a correct password returns `authenticated`; the silent authorize then yields a code and a token carrying `roles=lab_tech, amr=pwd`; with no session it returns `error=login_required` to the SPA's redirect URI and NOT to `/connect/login`; the frozen interactive path without `prompt=none` still reaches the login page; and a POST with no antiforgery token is refused 400. 24 new tests; 167 identity tests with `--with-db`, all green. openapi-drift regenerated (identity.json, 158 lines); login-origin, build-status, spa-scopes, kong-route-coverage and invariant-registry clean. |
| 28 | In-app login & password reset | 28.4 The sign-in happens in the app now | ☑ | 2026-08-05 — Step 4 of ADR-0036, and the user's actual request. **The screen used to be one button whose only job was to navigate the browser somewhere else** — a different origin, a different visual language, and 349 lines of hand-written HTML in a C# file that nothing kept in step with the design system. Credentials, the second factor and the organization choice are now asked for here, by `/connect/session/*`, and only once every factor is satisfied does `silentAuthorize()` run the UNCHANGED authorization-code + PKCE flow with `prompt=none`. Nothing about how tokens are minted, narrowed, refreshed or validated changed — only where the person types. **It is a SEQUENCE, and the server decides which steps apply.** The screen does not infer the next step from what it knows about the account; it has no business knowing, and a client that guessed would be a second authorization system with a worse view. Asserted directly: after the password step on a 2FA account, `silentAuthorize` has NOT been called — the property a password grant could not have expressed, and the one that would have quietly disappeared. **The failure messages are the design.** ONE message covers an unknown username, a wrong password and a deactivated account, because the server already refuses to distinguish them and saying more here would rebuild the enumeration oracle in the browser — pinned by a test asserting nothing on screen says "unknown user" or "disabled". A locked account is told it is locked, with minutes, per §5.2: the alternative sends a locked-out nurse to reset a password that was never wrong, and the reset does not unlock her, so she loses the password AND stays locked out. **An unreachable sign-in service is never rendered as a wrong password** — it says so explicitly ("this is not a problem with your password"), and an UNRECOGNISED status is treated the same way, because a client and an issuer disagreeing about the protocol is an operational fault, not a credential verdict. All three probed with injected violations: reporting any of them as bad credentials failed exactly the three tests written to catch it. A wrong TOTP code carries the same `invalid_credentials` status as a wrong password and must NOT be reported as one — the password was accepted a moment earlier. A lost authenticator has its answer on the screen where you discover you lost it: a recovery-code toggle, which also drops the numeric input hints, since `inputMode="numeric"` on a recovery code gives a phone keyboard that cannot type it. **The dev role picker is untouched** — it has no issuer to talk to and is how 901 frontend tests run without a backend. Two things corrected rather than left: my membership chooser had hand-rolled inline styles for something `.mrs-choice`/`.mrs-choice-opt` already settles (44px target, visible boundary, the whole ROW as the label), and `Array.prototype.at` passed vitest — which transpiles without typechecking — and failed the image build, which is the typecheck on this machine and caught it exactly as it caught an unused import in 27.3. 13 new tests; 901 frontend green (66 files) including axe over every route × locale × theme; 108 css-classes/display-truth gate assertions; login-origin, build-status, spa-scopes, kong-route-coverage and invariant-registry clean. Deployed and confirmed in the shipped bundle. |
| 28 | In-app login & password reset | 28.5 Email that can actually be delivered | ☑ | 2026-08-05 — Step 5 of ADR-0036, and the prerequisite for a reset that is a reset. **The platform's only email provider wrote a log line and RETURNED SUCCESS.** Left alone, the reset screen would have said "if that account exists, we have sent you a link" while nothing was sent, forever, with no error anywhere — a failed operation rendered as a clean result, on the one screen a locked-out person reaches when nothing else works. `libs/email` replaces that shape rather than extending it: `IEmailSender` exposes **`IsConfigured`** so a caller can decline to offer a capability it cannot deliver, and with no transport a send **throws** instead of quietly succeeding, because a silent no-op is indistinguishable from a delivered message at every call site. MailKit behind the interface; nothing outside the library references it. `Security` defaults to **StartTls** and `None` must be asked for by name — Tier 1's local Mailpit over plain SMTP is the only reason the option exists, and defaulting to no encryption to make that convenient would send credentials and reset links in clear text everywhere else. Registration never fails for want of a mail server: the issuer being unable to START because no relay is configured would turn a missing convenience into an authentication outage. **And the ADR was corrected rather than quietly satisfied.** §6.3 said the transport would sit behind the existing `IEmailProvider` seam and would "also fix every other notification email". It does not. That interface takes a recipient USER ID, not an address — the logging stub never needed anywhere to send to — and notification-service's `Notification` entity stores no address and the service has no directory lookup, so an SMTP client wired in there would be a client with nowhere to send. `IEmailSender` therefore takes an ADDRESS, and identity-service is its first caller because it is the service that holds one. Every other notification email still goes nowhere; that is a real gap and it is now written down as one instead of assumed closed. **Mailpit added to Tier 1** (in-memory, bound to localhost, so no credential in a reset link leaves this host) and proven live: SMTP from inside the compose network is accepted and the message is visible at :8025. The identity → Mailpit leg is proven end to end in 28.6, where there is something to send. 9 new library tests, all green; identity-service rebuilt and serving. |
| 28 | In-app login & password reset | 28.6 Self-service password reset | ☑ | 2026-08-05 — Step 6 of ADR-0036, and a capability that **did not exist in any form**: no forgot-password endpoint, no reset screen, no template. What existed was an administrator TYPING a new password on somebody's behalf, so there was no moment at which only its owner knew it. **The token costs nothing to store because it is not stored**: ASP.NET Identity's reset token is bound to the user's SECURITY STAMP, and `ResetPasswordAsync` rotates that stamp — so single-use is free, with no table, no sweeper, no race between two tabs and **no migration**. Pinned by two tests, including one proving that an OUTSTANDING second link dies when the first is used, which is ordinary user behaviour ("did that send?") and the case a bespoke token table would most likely have got wrong. A **named** provider gives it a 30-minute life rather than lowering the global `TokenLifespan`, which would have silently shortened email confirmation and every other data-protection token as a side effect. **Two consequences, both asserted, and both stated on the screen BEFORE the fields**. Every session and refresh token is revoked, because if the reset was requested BECAUSE the account was compromised then leaving the attacker's session running defeats the exercise — the stamp kills outstanding TOKENS, `RevokeAllAsync` is what reaches the sessions, which the token endpoint checks by `IsActive` and never by stamp. And **the second factor is not touched**: no disable, no key reset, no recovery code consumed, because if a reset could clear MFA then control of a mailbox would be a complete account-takeover primitive and the second factor would be decorative on exactly the accounts worth attacking. Injecting `SetTwoFactorEnabledAsync(false)` fails that test. **It refuses to lie in both directions.** 202 for a real and an unknown username with byte-identical bodies and nothing sent for the unknown one — anything else is a free account-existence oracle needing no credentials, strictly worse than the login form. But 503 when no transport is configured, and the screen then says the capability is unavailable rather than "check your inbox". A password-policy failure returns 422 with the ACTUAL reason, because "at least 12 characters" is advice somebody can act on and reveals nothing. **The bug found by clicking, not by testing.** The link carries the token base64url-encoded — a raw Identity token does not survive a query string — and the endpoint never decoded it, so every correctly generated, correctly delivered, promptly clicked link came back "no longer valid", indistinguishable from expiry. Every unit test passed, because they all posted a token they had never put through a URL. The regression test now reads the link out of the CAPTURED MESSAGE; reinstating the defect fails 7 of 13. **Verified live end to end through Mailpit**: a real reset email arrived for `lab_tech` while `nobody-at-all` produced 202 and no message; the emailed link gave 422-with-advice on a weak password, 200 on a good one, 400 on reuse; the new password authenticated and the old one did not; and the database showed 5 sessions revoked by `self:password-reset`. **Two findings recorded.** The seeded demo password `Mersal2026!` is ELEVEN characters and fails the service's own 12-character policy — the seeder hashes directly and calls `CreateAsync(user)` with no password, so it was never validated; restoring it after the live test needed the same bypass. And the identity CORS comment still describes the pre-28.2 cross-origin topology. 13 new server tests and 11 new screen tests; 180 identity with `--with-db`; 914 frontend green (67 files) including axe over every route × locale × theme; openapi-drift regenerated; all gates clean. |
| 28 | In-app login & password reset | 28.7 The administrator issues a link, and the server pages are frozen | ☑ | 2026-08-05 — Step 7 of ADR-0036, and the half of §6.5 that makes 28.6 mean something. **`POST /identity/admin/users/{id}/reset-password` took `{ "newPassword": "…" }`** — so the administrator CHOSE and therefore KNEW the credential, and there was no moment at which only its owner did. Shipping self-service reset while leaving that in place would answer "is a password a secret only its owner knows?" both ways at once. It also produced a password that had to be COMMUNICATED — by phone, by chat, on paper — and every one of those channels outlives the moment, where a link that expires in 30 minutes and dies on first use does not. What an administrator keeps is the ability to START a reset for somebody who cannot start their own; what they lose is knowledge of the result. **The link-sending is SHARED with the self-service path**, not reimplemented: an administrative link that expired differently or verified differently would be a second reset system nobody was maintaining, and a test drives an admin-issued link through the ordinary reset endpoint to prove it is the same kind. Unlike the self-service endpoint this one is deliberately **blunt** — the caller is an authenticated administrator who already holds the user id, so "this account has no email address" (422) and "no transport is configured" (503) tell them nothing they could not already learn and are exactly what they need in order to do something else; vagueness there would be theatre with a real cost. A failed SEND returns 502 rather than being swallowed, because an administrator who believes they started a reset and did not will wait for a call that never comes. **`ResetPasswordRequest` is deleted, not deprecated**, and a reflection test fails if any type by that name returns — a record kept "for compatibility" is a shape somebody re-wires later. **`AccountPages.cs` is FROZEN** (ADR-0036 §7): bug and security fixes only, no new capability, with the reason recorded in the file — it is 349 lines of hand-written HTML with its own CSS, its own bilingual dictionary and its own accessibility, and letting two login surfaces evolve would re-create the exact duplication this ADR removed while claiming to have fixed it. It is NOT deleted: an authorize without `prompt=none` must still land a human somewhere usable, and a test asserts it still does. One finding while testing: the `/identity/admin` group requires an MFA SESSION and not merely the scope, so an administrator seeded without a second factor gets 403 — the gate working, not a defect, and deciding something about somebody else's account is exactly what it exists for. **Verified live**: an old-shaped call carrying `newPassword` now sends a LINK and the supplied password is discarded — `Mersal2026!` still authenticates and `Attacker-Knows-This-1!` does not. 5 new tests; 185 identity with `--with-db`, all green; openapi-drift regenerated; every gate clean. |
| 28 | In-app login & password reset | 28.8 The sign-in screen, redesigned | ☑ | 2026-08-05 — The final step, done LAST on purpose so it styles a finished sign-in rather than a placeholder. A full-viewport split: a glass-over-waves teal hero beside the card, collapsing below 900px to one column with the hero kept as a BANNER — it is the only thing on the page that says whose system this is, and a bare form on a phone could be anybody's, so a test asserts the media query never `display:none`s it. **Everything reuses what the project already has**: the accent/surface/border/radius tokens, Inter and Cairo, the light/dark variables, the existing i18n mechanism with every new string authored in both locales. The hero's gradient stops and its gold are DECORATIVE and scoped to `.login-split` as `--login-*` properties rather than sprinkled through the rules or added to `tokens.css` — a test pins that separation, because in the token file they would look like accessible colours and end up on text. The gold is the project's own `#E0A106` (the value the Mersal lockup already uses), not the brief's `#EDA827`. **The official logo is reused, not re-drawn**: `mersal-logo-dark.svg` is ALREADY gold Arabic, white Latin and a muted FOUNDATION line — exactly what the brief describes. It needed one design-system change, an `onDark` prop, because `Logo` picks its asset by THEME and the hero is deep teal in both: in light mode the theme-picked lockup would put a teal wordmark on teal at about 2:1, the precise problem the dark asset was created to fix. **Two numbers, handled differently, and that is the interesting part.** "7 journey phases" is the product vision's own count and stands. "18 role portals" is WRONG — the catalog holds 21 — so the figure is DERIVED from `PORTALS.length` instead of typed, because a false number on the first screen every user sees is exactly what the display-truth rule exists for, and a correct-today constant goes stale silently. **A checkbox that changes nothing is a lie on screen**, so "Remember this device" is wired to the issuer cookie's persistence (an optional parameter defaulting to false, so the frozen form path is untouched). It is OFF by default and the reason is recorded in both the C# and the TSX: Mersal's clinic workstations are SHARED, and a persistent cookie means the next person at that terminal is signed in as the last — offered because it was asked for, defaulted off because of where this runs. Direction is handled with logical properties throughout (`inset-inline-start` for the field icons, `border-inline-end` for the glass seam) and the submit arrow is mirrored under RTL, since "forward" points LEFT in Arabic; the wave loop and the card entrance both stop under `prefers-reduced-motion`. The language and theme toggles are new UI for controls that **existed on the provider and had no caller anywhere in the application**. **The truth gate caught a pre-existing defect the moment new CSS referenced it**: `--fs-footnote` and `--text-muted` do not exist — the real tokens are `--fs-caption` and `--text-2` — and they had been referenced from TSX inline styles for some time, resolving to nothing, because the gate scans CSS and not TSX. Fixed at every site. 13 new tests; 927 frontend green (68 files) including axe over every route × locale × theme; sign-in re-verified live through the redesigned screen with the persistent cookie set; openapi-drift regenerated; every gate clean. |
| 28 | In-app login & password reset | 28.9 Sign-in screen — review pass | ☑ | 2026-08-05 — Changes from screenshots. **Statistics tiles removed**, and their CSS with them rather than left as dead rules — asserted gone, because the version that carried them quoted "18 role portals" against a catalog holding 21, and if tiles ever return the number has to be derived again. **Logo doubled** (50 → 104px). New headline in two lines — *One platform. / Every beneficiary. Every step of care.* — with the break as MARKUP rather than a `\n` in the string, since a newline collapses to a space in HTML and an Arabic translation may want to break elsewhere; pinned by a test. New lede naming the platform. **The card now follows the reference**: heading and sub LEFT-aligned (a centred heading over left-aligned labels gave the card two competing alignments, which is what read as unsettled), the sub cut to one line, the required asterisks hidden — only the VISUAL mark, since `.mrs-req` is already `aria-hidden` and `required` stays on the input, so `aria-required` and browser validation are untouched — and remember-device sits on ONE ROW with the forgot-password link, which previously wrapped onto its own line directly above the submit and read as a second action. The checkbox is **drawn rather than tinted**: `accent-color` alone leaves each platform's own box shape and tick, so it rendered as a grey square on one screen and something else on another; it is now one brand-filled square with a white check. The submit button is **solid** — the gradient put a near-black corner at the start of the button that read as a shadow artefact and pulled the eye off the label. **The two toggle glyphs are replaced with real icons**: a globe beside the code of the language it switches TO, and a proper moon/sun, in 44px buttons. The bare "ع" was rendering from the fallback face at label size and read as a stray character rather than a control; it now draws from Cairo on the same baseline as "EN". One copy decision NOT taken from the reference: the button stays **"Sign in"** and not "Sign in with SSO" — since 28.4 the password is typed in this application rather than at an external identity provider, so "with SSO" would describe a redirect that no longer happens. Three existing tests asserted the old heading text and were updated. 5 new assertions; 930 frontend green (68 files) including axe over every route × locale × theme; sign-in re-verified live through the rebuilt screen. |
| 28 | In-app login & password reset | 28.10 Sign-in screen — second review pass | ☑ | 2026-08-06 — Four fixes from screenshots, and the first one had a real cause rather than a nudge. **The field icons sat high because they were centred on the wrong box**: the wrapper contains the LABEL as well as the control, so `50%` of it lands above the input. Anchoring to the bottom instead would have drifted the moment an error message appeared underneath. The field is now flattened with `display: contents` — label, control and error become items of the wrapper's grid — and the icon is placed in the SAME grid cell as the control, so `align-self: center` centres it on the control itself at any label length, with or without an error, at any font size. **The two toggles are one fixed 44px square** rather than a minimum: widths that depend on their contents came out different for the globe-plus-letter button and the bare moon, and two controls doing the same kind of thing at two different sizes is the misalignment you notice first. The language button opts out explicitly because it carries a second glyph. `display: block` on the SVGs, since an inline SVG rests on the text baseline and leaves descender space beneath it, so a flex row centres the box while the mark still looks high; and the Arabic glyph gets its own flex line box, because Arabic and Latin have different baselines — which is precisely why "ع" sat low beside the globe where "EN" would not have. **The blotchy diagonals across the hero were BANDING, not a gradient bug**: four teal stops across a 1000px panel step through 8-bit colour in increments the screen resolves as edges, and a blur smooths the pixels without adding the missing values back. A one-pixel dither layer breaks the flat regions so the eye stops joining them — kept almost invisible, because at any strength you can actually see it stops being grain and becomes texture. The glass itself gained blur, a brightness lift and a soft inner vignette, since glass has an EDGE and without one the pane read as a flat wash over the gradient rather than a surface on top of it. **The wave runs at 11s instead of 26s** — at 26 you had to watch for it to notice anything moving — and the lines are slightly thicker and closer. Still stopped entirely under `prefers-reduced-motion`. 3 new assertions; 932 frontend green (68 files) including axe over every route × locale × theme; sign-in re-verified live. |
| 28 | In-app login & password reset | 28.11 Sign-out on every refresh — root cause, and the entitlement endpoint | ☑ | 2026-08-06 — Reported as "why does the system log me off with every refresh?". **Reproduced live before touching anything**: drove a real sign-in through `/connect/session`, silent-authorized, exchanged the code and decoded the token. `restore()` held a guard requiring the access token to carry EVERY scope in `OIDC.scope`; the issuer grants the INTERSECTION with the user's role entitlement, on purpose — `reception` receives 15 of 80, `doctor` 22, `super_admin` 21. **No role in the system could satisfy it**, so the tokens were cleared on every page load. The same call in `renew()` wiped a healthy session sixty seconds before each 5-minute token expired, so a user was also signed out roughly every five minutes without refreshing at all. Its four unit tests passed because every one of them fabricated a token carrying the whole request list — a token no issuer here can mint; the fixture was the impossible case. **Split into the two halves of scope staleness.** The app's own request list moving is knowable locally: the scope string is now recorded beside the token and compared on restore, no round trip. The user's ENTITLEMENT moving is not — the gap between what a token asked for and what it holds is normally just least privilege working — so **`GET /connect/entitlement`** was added: bearer-authenticated, own data only (subject and membership come from the token, so there is no parameter with which to ask about anyone else), resolved from the MEMBERSHIP exactly as the refresh grant resolves it. Not a revocation channel (the refresh grant ends a suspended session within the access token's lifetime) and not audited (no PHI, once per page load, and a row per reload would bury the disclosures the trail exists to surface). The client intersects the answer with its own request list, so the policy decision stays where only the client knows the answer. **The loop guard is the load-bearing part**: the remedy is a full-page navigation, and a re-authorisation returning still short of the scope would bounce the browser with no screen in between — one attempt per problem, cleared only when a later load finds nothing missing, and the second failure keeps the narrow token and lets the 403 happen. Every failure mode is fail-OPEN (unreadable token, unreachable issuer, unexpected body, no `sessionStorage`), because a bootstrap check that could strand the portal is worse than the gap it finds. `silentAuthorize` was split so the navigation can be STARTED without awaiting a promise that by contract never settles — correct for the login screen, a deadlock for `restore()`. Both new suites were probed by re-injecting the defect: the old semantics turn the regression tests red, and an endpoint that echoes the caller's own token back fails the one assertion that makes it worth having. OpenAPI drift regenerated — it was already red on the 28.4 session endpoints, which are now in the committed spec too. 15 new assertions (5 issuer, 10 SPA); 190 identity green with `--with-db`, 946 frontend green (69 files) including axe over every route × locale × theme; endpoint verified live through the one-origin proxy — a token carrying 1 platform scope, an entitlement reporting 13. |
| 28 | In-app login & password reset | 28.12 Appointment note — an author a desk can act on | ☑ | 2026-08-06 — The note dialog read *"Written by c18b985c-cc5f-42eb-8b79-e41b7b84f975"*. 0014 captured `note_by`, the author's SUBJECT ID, and that is what reached the screen — while 0014's own rationale was that an unattributed instruction crossing a team boundary is one nobody can follow up. A uuid is unattributed in every sense that matters to the receptionist reading it, so the field existed and answered nothing. **Migration 0022 adds `note_by_name`**, snapshotted at write time from the token's `name` claim, following 19.3 (signatures are snapshotted, never joined) and 0020 (`allergen_display`), which made the same argument for the same reason: resolving an author id on read would put an identity-service call in a clinical read path that has none today, and the honest record is the person as they were named when they wrote it, not whatever they are renamed to. **`note_by` stays** — a display name is not an identity and the audit trail correlates on the id. Nullable and NOT back-filled: filling it from `note_by` puts the uuid back on screen wearing a different column name, and filling it from the directory claims the author's CURRENT name for a note written before it — the same false attribution 0014 refused when it declined to stamp existing notes with `updated_by`. Those read as "unknown", which is true. The attribution follows the TEXT, so an edit re-attributes and clearing the note clears it. **The dev fixture was complicit**: it put "Nada Reception" in `noteBy`, so the dialog looked correct in fixture mode and rendered a uuid against the real API — a fixture kinder than the server hides the defect it should surface first; it now sends an id and a name, as the server does. Separator changed from `·` to `,` as asked. Both new suites probed by re-injecting the defect: feeding the id back in as the author turns the frontend assertion red. 8 new assertions (5 emr, 3 SPA); 280 emr green with `--with-db`, 949 frontend green (69 files) including axe over every route × locale × theme; OpenAPI drift green; verified live on the very appointment from the report — note_by_name went from NULL to "Reception" on rewrite, with the id unchanged. |
| 28 | In-app login & password reset | 28.13 Appointment note dialog — redesign | ☑ | 2026-08-06 — Requested changes to the note dialog. **Attribution moved to the top as a header row**: author behind a person glyph on the leading edge, moment behind a clock glyph on the trailing one, `space-between` and logical properties so the pair swaps ends in Arabic without a second rule. It reads before the text now rather than after it, which is the order the question is actually asked — a doctor deciding whether to act on "the sister will interpret" wants to know how old it is and who to ring before weighing the instruction. The visible "Written by" / "Written at" labels became the glyphs, so both survive as `sr-only` text: without them the dialog announces "Reception, 06 Aug 2026" with nothing to say which is the author and which is the moment. **The hairline is gone and the note is wrapped in emphasised quotation marks**, which now do the separating — they say "the following is somebody's own words" at any length, which a bare paragraph under a header row could not. Real characters rather than a decorative pseudo-element: `“ ”` are direction-neutral, so bidi places them correctly in Arabic with no mirror rule. **The not-clinical line was removed by request, and this is the one to note.** It was the ONLY place in the application telling the reader that a booking note is not a clinical record — the doctor who opens it is reading free text written at a desk by somebody with no clinical authority, and no other screen says so. The string is retained in `S.scope` with the decision recorded beside it, so restoring the line is one line rather than archaeology; Rollup drops it from the bundle while nothing reads it, so nothing dead ships. The test that asserted the boundary was INVERTED rather than deleted, for the same reason — it now pins that the string is still in the file and no longer rendered. 5 new assertions; 951 frontend green (69 files) including axe over every route × locale × theme; verified in the shipped bundle. |
| 28 | In-app login & password reset | 28.14 Reception board, timeline and booking — nine fixes | ☑ | 2026-08-06 — A batch from screenshots; two of them turned out to be server bugs. **`WHEN` sat above the label line** because a rendered `<legend>` is laid out against the top of its fieldset's BOX, not in flow — so the moment the custom-range fields made that fieldset ~30px taller, the legend rose with it while SEARCH/FROM/TO/STATUS stayed put. The legend now carries the accessible name only and a `span` carries the visible one. **The no-show sentence became a danger chip**: at six rows it was six copies of the same paragraph down the middle of the table, and the REASON moved to `title` rather than being deleted — "deactivated" with no reason is the shape of message an operator stops reading. **`.row-actions` had no `align-items`**, so it defaulted to `stretch`: icon buttons drew their glyph wherever their stretched box centred and a bare `<span>` anchored its text to the top. **Patient file moved last** (it is the only control that leaves the board). **Cancel is danger-outlined**, not teal beside the edit pencil — filled would shout on every row. **Table scroll now chains to the page**: `.mrs-scroll` contains BOTH axes, right for a dropdown floating over the page and wrong for a table that IS the page, and `contain` blocks the chain even with no vertical overflow to consume — so the wheel died wherever the cursor sat over a table. Inline stays contained (browsers map horizontal overscroll to back/forward gestures). **Timelines** print the actor behind a person glyph and the moment behind a clock, with "by"/"at" kept as `sr-only` — and an unresolvable id is now said in WORDS with the raw value on `title`, because eight hex characters are not a name and nobody at a desk can act on them. Written up as 0B §10d, normative for every timeline in every portal; one shared component already serves reception, call-centre and doctor. **Patient search opens a dialog when AMBIGUOUS** — one match stays inline, since a dialog confirming the only option buys nothing — and the WHOLE ROW is the button rather than a 70px "Choose" pinned as far from the name as the layout allowed; a blocked row is not a disabled control but plain text, because a disabled button is still announced as one. **The edit dialog is `wide`** (a month calendar beside a times column was squeezed into a strip narrower than the timestamps it lists) and can now change the DOCTOR — presented as a filter over the times, because an appointment's practitioner is whoever holds its slot, so the desk cannot produce a row whose named doctor and session disagree. Two server defects found doing it: **`RescheduleAsync` never moved `DoctorId` with the slot** (invisible while the picker only offered the appointment's own doctor — the two could not disagree), and **`insideTransaction` was invoked TWICE**, so every reschedule enqueued `ApptRescheduled` twice and consumer dedupe could not help, each enqueue minting its own event id. `DoctorChanged` added as a distinct timeline step: "same time, different doctor" is not what a reschedule says. Reception card padding sp3 → sp5. 20 new assertions; 284 emr green with `--with-db`, 956 frontend + 51 design-system green including axe over every route × locale × theme; OpenAPI drift green; verified in the shipped bundle. |

> **Phase 25 (Branch Management) — the five sponsor decisions: RATIFIED as recommended, 2026-08-01.** D1–D5
> are signed off unchanged; the decision record is
> [`decisions/phase-25-sponsor-pack.md`](decisions/phase-25-sponsor-pack.md) and it is the source of truth.
> Nothing was overturned, so **no DPIA was triggered** and neither the DPO nor the Medical Director was
> required — both were only ever in scope for reversing **D2** (no encounter link) or **D5** (vaccines are
> pharmacy stock), the two that are load-bearing on invariants 8 and 9 (clinic inventory never dispenses to a
> patient; inventory carries no PHI).
>
> **What the sign-off exercise found.** Writing the decisions for a non-engineer forced the question "what is
> each of these actually enforced BY?" — and D1–D4 answered with a `CHECK` constraint, a unique index, a
> five-fact test suite and a role-reach mode, while **D5 answered with nothing**: no reference to vaccines,
> injectables or any medicine identifier existed anywhere in inventory-service. `ADR-0029 §4.1` records what
> closed it — the catalogue now asks masterdata whether an item is a medicine and refuses it if so, *and
> refuses it when masterdata is unreachable*, so the gate cannot quietly open during an outage. Design:
> [`../HBMP-Design/42-branch-management.md`](../HBMP-Design/42-branch-management.md) §8; ADR:
> [`adr/0029-branch-management.md`](adr/0029-branch-management.md).

> **Phase 21 (User & Access Model)** implements [`../HBMP-Design/40-user-access-model.md`](../HBMP-Design/40-user-access-model.md), adapting an external
> reference design. **Doc 40 §0 (adaptations A1–A6) is normative**: the platform-admin flag is never a PHI wildcard (A1);
> denied switches/scopes are audited, never silent (A2); no namespace-prefix gate loosening (A3); enablement is programme
> administration, not upsell (A4); tokens carry role-shaped claims only — ABAC stays request-time (A5); revocation-check
> degradation is bounded + alarmed (A6). Restructures who holds authority; must not move what authority reaches — all
> pre-existing min-necessary/RLS/SoD/sensitive-gate suites must stay green. Runs after phase 17 and phase 18 Gates B/C.

> **Phase 20 (Patient Profile)** implements [`../HBMP-Design/39-patient-profile.md`](../HBMP-Design/39-patient-profile.md).
> **Highest-risk feature in the platform** — it aggregates everything about a patient onto one screen.
> Three non-negotiables: **server-side projection only** (a withheld field is absent from the JSON, never
> CSS-hidden), **composed under the caller's token, never a service account** (the classic aggregation
> vulnerability), and **it is an intersection of existing rules, never a union** — treating relationship,
> provider ownership, branch scope, payer scope, call-centre verification and the §37 sensitive-result
> grant all still bind. Run after 18 Gate B and 19.3b/19.3c.

> **Phase 19 (Policy & Member Administration)** implements [`../HBMP-Design/38-policy-member-administration.md`](../HBMP-Design/38-policy-member-administration.md)
> — turns Beneficiary Management into a real PAS. **Run after phase 18 Gate A**: every utilization view
> reads the `consumed_value` accumulator that 18.A1 makes work. Extends `services/policy` (no rewrite);
> `coverage`/`coverage_limit` become *generated* from a plan version, so the eligibility engine and the
> phase-18 accumulator keep working untouched.

> **Phase 18 (E2E Remediation)** comes from [`AUDIT-R2-E2E.md`](AUDIT-R2-E2E.md) — a five-stream end-to-end
> review (quality · security · **correctness** · delivery · **UX/best-practice**). **It gates the pilot and
> go-live.** Gate A is benefit/money **correctness**, not hardening: today `coverage_limit.consumed_value`
> is never incremented (every member eligible forever), batch rollups erase applied adjustments at the
> settlement freeze, and the allowed-amount cap is inverted (`Math.Max`) so approvals can exceed the
> contract tariff. Note phases 12/13 are ticked ☑ but the R2 reachability matrix found interop has **no
> compose entry and no Kong route** — closed by 18.C2.

> **Phase 17 (In-App Identity)** implements ADR-0015 (Keycloak → ASP.NET Identity + OpenIddict). Sequence:
> 16.1–16.2 → **17.1–17.6** → remaining 16.x — phase 16's Keycloak-specific steps (16.3 MFA enforcement,
> 16.7 realm/scope reconciliation) land on the NEW issuer instead; do not fix the realm twice.

> **Phase 16 (Audit Remediation)** comes from `docs/AUDIT-2026-07-26.md` and **gates 12 (go-live)**. Note:
> 11.1–11.3 were ticked after the audit snapshot; the audit's 11.x observations may be partially stale, but
> findings C1–C3 and H1–H10 were verified against code and stand until closed.
>
> **Phase 16 status: COMPLETE for all work actionable now** — 16.1, 16.2, 16.4, 16.6, 16.7, 16.8, 16.9 done;
> 16.3 (admin MFA, C3) and 16.5's global edge-JWT plugin (H3) are **deliberately folded into Phase 17** because
> the issuer moves in-house (ADR-0015) and doing them on Keycloak first would be thrown-away work. Everything that
> can be closed without the identity migration is closed. **17.0 DONE** (ADR-0015 + frozen token contract +
> the ETag/If-Match opt-in it gated). **Phase 17 (In-App Identity) COMPLETE** — 17.0–17.6 done: ADR-0015 +
> frozen contract → identity-service (ASP.NET Identity + OpenIddict) issuing byte-compatible tokens → login +
> TOTP 2FA → in-app admin (closes **C3**) → SPA rewired (closes **H6**) → cutover + Keycloak retired + edge-JWT
> resolved (**H3**). Identity 13/13 real-PG, web 55/55, full sln 0-warning. Prod key follow-up landed in 12.3.

> **Phase 12 (Migration/Go-live) COMPLETE** — 12.1 reversible/audited/idempotent migration toolkit (proven on
> real PG) + DPIA → 12.2 gated dev→QA→staging→prod release pipeline + expand/contract CI gate + go-live gate
> enforcement + progressive-delivery/auto-rollback config → 12.3 persistent OpenBao issuer keys (closes the
> Phase-17 follow-up) + cutover/fallback/hypercare runbooks + bilingual AR/EN training + pilot dashboard + exit
> criteria. Migration 26/26 + identity 17/17 real-PG; full sln 0-warning. Infra-execution items (prod run, live
> canary drill, human sign-offs, OpenBao provisioning) are operational gates 🟡 (target infra), per phase 11.
> **Next: Phase 13 (Interoperability).**

### Phase 21 — User & Access Model (design 40, ADR-0021)

`tenant_membership` is now **the security principal**: authorization evaluates against the membership, never
the identity, so one person can hold genuinely different authority in two organisations.

- **21.0** ADR-0021 + three additive token claims (`membership_id`, `level`, `features`) behind the frozen
  contract, with a byte-compat guard.
- **21.1a/b/c** membership model + access-neutral backfill; tenant-local role→scope grants (catalog stays
  global); login membership chooser, with authorize/token re-resolving the membership on every use.
- **21.2** catalog metadata (`deprecated` / `replaced_by` / `is_platform_admin_key`), role `level`,
  SoD-vetted per-membership overrides, and ONE effective-set evaluator in two modes. **The A1 denial test and
  the mode-1/mode-2 parity suite are CI-pinned by name** (`AuthzParityGuardTests`) — deleting either fails
  the build.
- **21.3** time-bounded branch grants + the four-step precedence chain with dual failure semantics, and a
  fail-closed branch **sentinel**. Fixed a live whole-tenant leak: `RowScope.WithBranchScope` used to fall
  open when a branch-scoped caller's reach did not resolve.
- **21.4** per-tenant programme features + caps, with two deliberately DISTINCT problem types
  (`program-not-enabled` vs `program-limit-reached`, both ≠ a permission denial — the remedies differ). Caps
  counted live inside the transaction under an advisory lock, so two parallel creates at cap−1 yield exactly one.
- **21.5** ambient membership attribution (stamped by the interceptor, not by handlers); A6 revocation
  degradation (refresh fails OPEN, explicit revoke fails CLOSED); A2 switch guards; session/device controls
  with a concurrent cap that revokes the OLDEST; login-attempt history with a COARSE failure reason;
  access-review snapshot (JSON + CSV) audited as an export, carrying overrides WITH their reasons.
- **21.6** explicit Kong routes for the new surfaces (route-coverage guard green), docs (40 indexed, 22
  gains the table catalog, 18 gains the degradation policy), and the admin UI: the membership roster and its
  five detail tabs (roles, exceptions, branch reach, sessions, effective-access preview), plus the
  programme-enablement screen. Backed by two new read surfaces — `GET /identity/admin/memberships[/{id}]`,
  deliberately NOT the access-review snapshot (that recomputes every effective set and is audited as an
  Export; browsing a roster is neither) — and `/api/v1/admin/programs`, platform-admin only. Identity also
  gained a per-session administrative revoke: the only prior option was revoke-all, which is the right tool
  for off-boarding and the wrong one for one stolen device, and its cost meant the safe action got postponed.
  Cross-tenant roster reads are **403 + audited, never silently narrowed** — a page of your own tenant under
  another tenant's heading is worse than an error, because the reviewer believes they reviewed the right one.

### Known gap carried out of Phase 21 — CLOSED 2026-07-29 except two of the four caps

**What was wrong.** `ProgramEnablement` and `TenantProgramStore` were complete and well tested — the advisory
lock, the live counting, the two distinct problem types — but nothing called them, so the third orthogonal
gate (design 40 §4) refused nothing and the SPA's `program-not-enabled` treatment rendered a problem type no
server emitted. Investigating it surfaced a second, larger hole underneath: **the `features` claim was
documented in the token contract and read by `HbmpPrincipal.Features`, but the issuer never emitted it.**
Every `HasFeature` call in the platform would have returned false, so wiring a gate would have refused
everything rather than nothing — and because the switches live in `admin.tenant_feature`, another service's
schema, the issuer had no way to know them.

**Closed now (propagation).** `SetFeatureAsync` stages `TenantFeatureChanged` to `admin.events` in the same
transaction as the row and its history; identity-service tails that queue into `identity.tenant_feature`
(migration 0015) and reads it at issuance, so the claim is resolved once per token and every later check is an
in-memory lookup — design 40 §5 mode 1. The projection compares the admin-stamped `changed_at` and refuses to
move backwards, because delivery is at-least-once and unordered and a redelivered stale "off" would otherwise
darken a live module with nothing reporting a failure; dedupe is a durable table, since "have I ever seen this
id" is not a question a process lifetime can answer. Both are one transaction, so a crash cannot mark an event
processed without applying it. **Existing tenants were backfilled ON** (admin 0009, identity 0015): absence
means disabled, which is right for a new tenant and would otherwise have taken every current partner off every
module the moment a gate appeared. Verified end to end in the running stack: `PUT .../features/interop`
`{enabled:false}` → 200 → the projection followed (`source_event_id` set) → the next token carried 10 features
instead of 11 → restored, and both tables agree at 11.

**Closed now (call sites).** Ten services that ARE one module each — claims, callcentre, interop, pharmacy,
orders, approvals, emr, finance, document, case — gate the whole service via `UseProgramFeature(...)`, placed
immediately after `UseAuthorization()` so enablement is asked LAST and a caller lacking the scope still gets the
authorization denial. Whole-service rather than per-group deliberately: these declare route groups across a
dozen files (claims alone has four), so per-group gating is a dozen chances to miss one and the endpoint added
next year defaults to ungated; here the default inverts. Reporting is the exception and uses the per-endpoint
`RequireFeature(...)`, because `reporting_extracts` gates EXTRACTS only — a tenant not on that programme still
reads its own dashboards on screen.

Two kinds of request pass untouched, both because enablement is a question about an authenticated
ORGANISATION: **anonymous** ones (the health probes — a gate that broke liveness would take a disabled module's
container down instead of refusing its requests) and **tenant-less** ones (the client-credentials token behind
the event pipeline's four `*:ingest`/`*:project` scopes belongs to no organisation; refusing it would stop the
platform's own machinery for every tenant rather than enforce a policy). The carve-out is pinned by test so it
cannot widen. The ADMINISTRATION surface is deliberately ungated — gating the screen that switches a feature
back on would make a tenant with everything off unrecoverable.

Verified live: doctor token → `GET /api/v1/appointments?mine=true` 200 → switch `emr` off → next token carried
10 features → the SAME request 403 `program-not-enabled` naming `emr`, while `/health/live` on the same
container stayed 200 → restored → 200 again.

**One consequence, found by wiring it.** profile-service composes clinical sections from emr/orders/pharmacy
under the caller's own bearer, so gating those services changes what the profile sees. `SectionForbiddenException`
was thrown by `CallerScopedHttp` and **caught nowhere** — it fell into the composer's broad catch and reported
`upstream-error`, so a section the owner declined (a caller without the scope then; a tenant not on the
programme now) rendered as "temporarily broken". An organisation told that waits for a recovery that will never
come, when what it needs is to ask someone for access or for the module to be switched on. The composer now
catches it and returns **Restricted / `owner-declined`**, keeping design 39 §6's three states distinct. The
exception moved to Domain to make that possible — Domain must not reference Infrastructure.

**The caps — two of four wired.** `RoleAdminService.GrantAsync` now calls `CheckLimitAsync` for
`active_users` and `active_provider_users`, inside an explicit transaction that also does the insert: the
advisory lock is transaction-scoped, so outside one it would be released immediately and the serialization it
exists for would silently not happen. A breached cap rolls back and answers with its own problem carrying
`limit`/`max`/`current` — folded into the SoD conflict view it would arrive as "denied" with an empty violation
list, telling an administrator nothing about whether to free a slot or ask Mersal to raise the cap.

The semantics needed care, and both errors would have been invisible. A cap counts USERS, not bindings, so a
second role for someone who already has one consumes no slot — enforcing per-binding would freeze role
administration for every tenant sitting at its limit and read as a bug in role admin. And the two caps ask
DIFFERENT questions of the same grant: a user active under a tenant-scoped role still takes a provider slot on
their first provider-scoped binding, so sharing one "is this user new" test between them would let the provider
cap be bypassed by granting a tenant-scoped role first. Five tests pin this, including that a revoke frees the
slot immediately (true by construction — live rows, not a counter that drifts).

**Still open: `monthly_extracts` and `storage_mb`, blocked on a design decision, not on effort.**
`TenantProgramStore` takes an `AdminDbContext` — it reads `admin.tenant_limit` and takes its lock on admin's
connection — so it is structurally admin-only, and the rows these two caps count live in reporting-service and
document-service. Enforcing them needs the cap VALUE propagated to the owning service (the features-style
event + projection; projecting a limit is fine, and does not contradict 0008's "caps are never cached", which
is about the COUNT). Two further specifics: reporting has **no extract log table at all**, so `monthly_extracts`
currently has nothing to count; and `storage_mb` does not fit `WouldBreach(max, liveCount)`, which hardcodes
`liveCount + 1 > max` — adding a 5 MB file is not +1 MB, so that primitive needs an increment parameter.
document-service does already record `size_bytes`, so it is the closer of the two.

Full backend suite **1953 passing / 0 failing / 1 skipped**, run against a Postgres with all 128 migrations
applied so the env-gated integration and RLS suites actually execute — DB-less, 356 of them skip and the
number reads ~1598. Frontend **220** (contracts 9 + design-system 18 + web 193), including the axe sweep,
which is table-driven over the portal catalog and therefore covered the three new routes in EN/AR × light/dark
without anyone adding them to it.

Twelve mutations were used to prove the new guards actually bite — platform-admin-as-wildcard,
allow-beats-deny, ignored expiry, deleting the A1 test, the branch fail-open, the missing cap lock, and
reversing the A6 pairing were each killed by the test written for them.

Divergences from the build prompt and the defects found along the way are recorded in **ADR-0021 §4–§5**.

### Phase 28 — Clinical validation hardening (28.1–28.11, complete)

Design `HBMP-Design/44-clinical-validation-hardening.md`; spec
`docs/superpowers/specs/2026-08-06-phase-28-clinical-validation-hardening-design.md`; **ADR-0037**.

Phase 26 built an engine whose architecture is right and whose clinical content was not. Four defects shared
one shape — *a check that could not run reported that it had run cleanly*:

- **28.1 the allergy false negative (Critical).** masterdata compared an allergen CODE (`ALG-PENICILLIN`)
  against a drug's ATC ancestor chain (`J01C`) — disjoint code spaces, a constant false — and the engine
  rendered it as "no conflict with the 3 recorded allergies" on every prescription ever issued. Fixed by an
  ingredient model (`ingredient`, `drug_ingredient`), pharmacist-reviewed mappings for all 15 seeded
  allergens, and side-chain-aware cross-reactivity carrying a stated confidence. A penicillin-allergic
  beneficiary prescribed amoxicillin now warns, asserted through the real matcher end to end.
- **28.2 the trusted-client diagnosis (Critical).** Step 2 re-ran every check server-side and then read
  `req.DiagnosisIcdCodes` from the request body. Diagnoses moved OUT of `ValidationRequest` into
  `ValidationSnapshot` behind `Fetched<T>`, so trusting the client is no longer expressible; new emr endpoint
  `GET /encounters/{id}/validation-context`. emr down ⇒ Unavailable, never NotChecked and never Ok.
- **28.3 interactions keyed on molecules.** `drug_interaction` keyed pairs on two product uuids against a
  22,653-product catalogue — zero rows, and unpopulatable by construction. Replaced by `interaction_rule`
  keyed on ingredient/ATC class, seeded with 13 pharmacist-reviewed rules from the ONC/NLM high-priority
  list, each with mechanism, clinical effect, management and a citation. Coverage (count + date) is stated.
- **28.4 severity tiering.** Only Contraindicated and Major interrupt and gate; Moderate renders inline,
  Minor collapses; a null severity still interrupts. Changes interruption, never blocking.
- **28.5 duplicate therapy.** New `CheckKind.Duplication`. Same molecule across lines — including inside a
  combination product and including the same product twice, which phase 26 skipped in one line.
- **28.6 mechanism / effect / management** on interaction and allergy findings, with the management line in
  the message rather than behind a disclosure.
- **28.7 ICD hierarchy.** `icd_code.parent_code` + `node_kind` + a materialised `icd_ancestor` closure,
  populated from columns the loader had always read and discarded. Matching is descendant-or-self; a
  less-specific diagnosis reports as a possible match. The duplicate dot-normaliser is deleted — one
  implementation, in `libs/clinical-codes`, asserted by an architecture test.
- **28.8 patient context.** Age and weight reach the engine. A stale weight (>30d child, >90d adult) is a
  MISSING weight. Missing inputs yield NotChecked naming them; renal function states that eGFR does not
  exist on this platform rather than dosing around it.

- **28.9 drug–disease contraindications.** New `CheckKind.Contraindication`, deliberately separate from
  Indication: a mismatch means off-label (legitimate, common, dismissed constantly) while a contraindication
  means harm, and conflating them buries the second inside the first. Eight rules seeded with citations —
  NSAIDs in peptic ulcer disease / CKD / heart failure, beta-blockers in asthma, metformin in renal
  impairment, ACE inhibitors and tetracyclines and fluoroquinolones in pregnancy. Pregnancy is a STATUS
  (`emr.pregnancy_status` + history), not an ICD scope: a rule keyed on O00-O9A would fire only for the
  patient nobody needs reminding about, and Unknown does not count as pregnant.
- **28.10 indication-keyed dosing.** `dosing_rule` on (subject, indication scope, population, route),
  most-specific-first selection, mg/kg capped at the adult maximum. The fetcher that was
  `_ = drugIds; _ = ct;` returning an empty dictionary now reads real rules. No recorded age selects NO
  population rule — defaulting to Adult would apply an adult ceiling to a four-year-old.
- **28.11 severity in the UI.** Severity is its own four-cue chip beside the state chip, visible without
  opening the modal; the modal groups by severity then kind. Moderate renders inline and does not gate.

**Catalogue loaded (`--release phase-28-reload-2`):** 7,052 molecules and 45,992 product→molecule links
derived from 22,653 products, 59,554 ICD ancestor rows. **8,009 products (35%) are combinations** that
decompose into more than one molecule — the figure that makes the case for the model change concrete, since
each of those is invisible to a product-keyed rule. 1,093 prescribable products resolve to no molecule and
report "not checked" rather than passing.

The load found a defect no test had: the Egyptian catalogue writes **`amoxycillin` on 220 products and
`amoxicillin` on none**, and `sulphamethoxazole` on 21 against `sulfamethoxazole` on one. The curated rules
are written in INN, so every one of them matched nothing. Fixed by a targeted BAN→INN orthography fold
(`sulph`→`sulf`, `amoxy`→`amoxi`, `ceph`→`cef` prefix) — deliberately not a general `ph`→`f` normaliser,
which would rewrite morphine and phenytoin into molecules that do not exist. Migration 0014 additionally maps
the penicillin salts the catalogue actually carries (`benzathine penicillin g`, `phenoxymethyl penicillin`,
`penicillin g`, `procaine penicillin`) and topical silver sulfadiazine. Penicillin-allergy reach by exact
molecule went from 116 products to 317; one curated warfarin × NSAID rule now covers 10,065 product pairs.

Ten invariants registered with named tests. Two pre-existing failures fixed along the way: the credential
scanner fired on the word "Password" in an interpolated log message, and `ScopeIntegrityTests` could not
parse three newer migration shapes — so it reported `auth:configure` as missing from a vocabulary that had
seeded it since 0027. Both now fail loudly rather than silently under-reading.

## Fixed 2026-07-29 — "it signs me out rapidly": the session was 5 minutes, not 30

**Reported symptom:** being signed out again and again, shortly after signing in.

**It was not a timeout value.** The clocks are: access token 5 min (frozen contract, 300s), refresh token 10 h
(frozen, SSO max), and the SPA hangs its warn/logout timers off the ACCESS token's `exp`, attempting a silent
renew first. So the session lasts 5 minutes *unless the renew works* — and the renew was failing:
`grant_type=refresh_token` → `invalid_grant` / "The specified token is invalid" in the issuer log.

**Cause: `IssuerKeys` used `AddEphemeralEncryptionKey()` in Development, and that key wraps refresh tokens as
JWE.** Every restart of identity-service therefore made every outstanding refresh token undecryptable. The user
sees a session that dies minutes after signing in; nothing on screen connects it to a container restart. The
same ephemeral SIGNING key was separately responsible for the `{"message":"Invalid signature"}` toil at the
gateway, because its `kid` changed on every restart and Kong pins the public key.

**Fix: persistent issuer keys in Tier 1.** The configuration path already existed
(`Issuer:SigningKeyPemPath` / `Issuer:EncryptionKeyPemPath`) but Development ignored it. Development now PREFERS
configured keys and falls back to ephemeral only when they are absent, so `docker compose up` still works
unconfigured. Both or neither: a persistent signing key with an ephemeral encryption key would keep the gateway
happy while still logging everyone out on restart — the confusing half-fix. `up.sh` generates the pair once into
`infra/compose/secrets/` (gitignored by `*.pem`), owned by uid 10001 mode 400, chowned via a throwaway root
container so it needs no host sudo; Compose mounts them read-only. **OpenBao's transit engine remains the
production home (the Phase-12 follow-up already recorded against 17.6) — this is the Tier 1 arrangement, on the
same LUKS-encrypted disk as the database.**

Verified: `kid` identical across two container recreates (so Kong's pinned key no longer goes stale), and a
refresh token captured BEFORE a recreate exchanged successfully AFTER it — HTTP 200 with a fresh access, id and
rotated refresh token, where it previously returned `invalid_grant`. Effective session is now up to the 10-hour
refresh window with silent renewal every 5 minutes, rather than 5 minutes total.

**Not changed:** the 5-minute access token and 10-hour refresh lifetimes are frozen-contract values and cover a
working shift; neither was touched.

## Fixed 2026-07-29 — sign-in returned HTTP 500 after any identity-service restart

**Reported symptom:** `POST /connect/login` → `{"title":"An error occurred while processing your request.","status":500}`.
**Cause:** data protection was never configured, so ASP.NET Core used an EPHEMERAL, in-process key ring. Every
restart invalidated every anti-forgery token in flight, so anyone sitting on the login page when the container
was recreated got a 500 on submit with nothing on screen to suggest that reloading would fix it — and a deploy
does that to everyone who happens to be signing in. It also made more than one replica impossible: each would
hold its own ring and reject forms served by the others, non-deterministically.

Two fixes, because there were two defects:

1. **The key ring is persisted** to `/var/lib/hbmp/dpkeys` on a named volume (`identitykeys`), named per
   application, path overridable via `DataProtection:KeyRingPath`. The directory is created **and chowned in the
   Dockerfile**: Docker initialises a fresh named volume from the image's contents and ownership, so without that
   it came up root-owned, the non-root app could not write to it, and the first token failed with "Access to the
   path … is denied" — a 500 at first use rather than at startup. The startup probe now WRITES a file rather than
   calling `CreateDirectory`, which succeeds on a directory that already exists and was exactly why the
   root-owned volume passed the check.
2. **A stale token is a user event, not a server fault.** The four rendered form POSTs bind `[FromForm]`, and
   that binding validates the token before the handler body runs, so no handler could answer for itself. One
   middleware now catches it for those four paths and re-renders the login page with a distinct bilingual
   message — deliberately NOT "invalid credentials", which would send someone to reset a password that was never
   wrong. 200, not 4xx: it is a usable page, and a 4xx makes tooling treat it as a failed request.

Verified: token captured → `docker compose restart identity-service` → the SAME pre-restart form posted → 302
(was 500), with the identical key file on the volume. A genuinely corrupt token → 200 with "This sign-in form
expired." A full browser sign-in issues a token carrying all 11 features.

**Related, still open:** the issuer's RS256 SIGNING key is also ephemeral, so every identity-service restart
still invalidates Kong's pinned public key and needs `jwks-to-pem.py | set-env-key.py` re-run plus a Kong
recreate (symptom: `{"message":"Invalid signature"}` at the gateway). That is the existing Phase-12 OpenBao
follow-up already recorded against 17.6, and persisting signing keys is a security decision rather than a
tidy-up — not changed here.

## Fixed 2026-07-29 — two things that had never worked

- **`tools/ci/apply-migrations.sh` could not be re-run.** identity `0001` seeds `role_scope` with
  `ON CONFLICT (role_name, scope_name)`, but `0011` widened that PK to include `tenant_id`. Postgres resolves an
  ON CONFLICT target against the constraints that exist when the statement RUNS, so from 0011 onwards the seed
  failed — and under `ON_ERROR_STOP` that aborted the whole identity sequence, meaning **every later identity
  migration silently stopped being applied to an existing database**. 19 statements across 9 files carried the
  same named target; all now use a bare `ON CONFLICT DO NOTHING`, which matches whichever unique constraint the
  table currently has and is correct on both a fresh and a migrated database. The script now completes: 133
  files across 21 services, exit 0.
- **The same script silently changed DB credentials.** It `ALTER`ed `hbmp_app`/`hbmp_audit` passwords
  unconditionally, defaulting to a built-in CI value when the env vars were unset. Run against the live Tier 1
  database — which is also how you apply a new migration locally — it changed the credentials out from under
  every running container, whose only symptom was `password authentication failed` everywhere. Setting a
  password is now limited to roles the script CREATES, or to an explicit `HBMP_FORCE_ROLE_PASSWORDS=1`.
- **`alertmanager` had never started.** `config/alertmanager.yml` carried `url: "${ALERT_OPS_WEBHOOK}"` and two
  siblings, with a comment claiming they were env-injected. Alertmanager does not expand environment variables,
  Compose mounts the file verbatim, and those three variable names appeared nowhere else in the repo — so it
  read the literal `${...}` as a URL and died with `invalid URL: unsupported scheme ""`, which also means none
  of its routing had ever been exercised. The routing tree is kept and now loads; receivers deliver nowhere by
  default, so alerts fire, group, inhibit and show in the UI. Rendering the file (Helm/envsubst) is what a real
  deployment must do — the variable names are recorded in the file for that.

## Environment notes
- .NET 8 SDK: user-local `~/.dotnet` (use `./dotnet.sh`). Node 20, psql 17 present.
- **Frontend (Phase 9):** pnpm workspace at repo root (`pnpm-workspace.yaml`); `apps/design-system` (9.1),
  `apps/web` (9.2 portals + 9.3 flagship screens), and `libs/contracts` (`@mersal/contracts` — shared zod
  mirror) are live. Node 20 ⇒ use **pnpm 9** (`npx pnpm@9.15.9 …`); pnpm ≥10 needs Node 22. Filters:
  `pnpm --filter @mersal/{design-system,web,contracts} {dev,test,build,lint}` (design-system + web test =
  vitest unit + **axe** gate). Frontend suite: contracts 5 + design-system 18 + web 18 = **41 tests**. The
  six flagship screens are `React.lazy` (per-portal chunks); the dev app uses `DevApiClient` fixtures
  (bilingual, contract-valid, no PHI) — swap `HttpApiClient` once services are reachable behind Kong. CI in
  `.github/workflows/frontend-ci.yml`.
- **Phase 10 (Case + Finance):** two new .NET services — `case-service` (schema `case`, the `case-assignment`
  ABAC condition in `libs/authz`, beneficiary-360 coordination summary, PHI-read audited) and `finance-service`
  (schema `finance`, the `FinanceProjection` whitelist + `FinanceCannotReadDiagnosisTests` proving finance ≠
  diagnosis, settlements priced from `provider_contract`). Plus the **Case Manager + Finance portals** in
  `apps/web` (10.3): `CaseManager.tsx` (My Cases → coordination-360 with masked clinical sections + tasks;
  Escalations) and `FinancePortal.tsx` (Utilization / Settlements / Summaries with US-073 data-table toggle /
  audited Exports) — both `React.lazy` chunks. New `@mersal/contracts` modules `case.ts` + `finance.ts` (the
  finance≠diagnosis + coordination-summary invariants are structural + contract-tested). Frontend suite:
  contracts 9 + design-system 18 + web 21 = **48 tests**. Backend: case-service 27 + finance-service 14.
  DB-integration tests env-gated (`CASE_TEST_DB` / `FINANCE_TEST_DB`, hbmp superuser conn).
- **Phase 20 (Unified Patient Profile)** — design `HBMP-Design/39-patient-profile.md`, prompt
  `claude-code-prompts/phase-20-patient-profile.md`, ADR **0026**.
  **20.1** `libs/authz/ProfilePolicies` — design 39 §4's role × 15-section matrix as DATA (one cell per
  role/section, naming the projection variant and the ABAC conditions), plus Full/Operational/Meta call-history
  levels and a clamp that only narrows. **`services/profile`** — a composition service with **no DbContext, no
  migrations and no schema**: 15 `ISectionProvider`s fan out in parallel under the CALLER'S bearer with
  per-section timeouts, the matrix gates BEFORE fetch, variants narrow AFTER, and one `ProfileViewed` audit
  event names served **and withheld** sections. Restricted / Unavailable / NotApplicable stay three distinct
  states. An architecture test fails the build on a client-credentials path, a `DbContext` or a `.sql` file;
  profile joins the RLS exemption register (no database to bind a tenant GUC on).
  **20.3** beneficiary photo — `IdentityPhoto` on policy's `DocumentClass`, consent-gated at upload (refusal
  permitted, never blocks care), short-TTL signed retrieval, audited, with an allow-list NARROWER than the
  Administrative class it is filed under (`MayDownload` became class-aware).
  **20.3b** `call_interaction.summary` — a NEW capped column, deliberately not a rename of `notes`; required at
  close unless Abandoned; corrections write a revision row and set a visible edited marker; `copyText` is
  generated from the NARROWED row so a Meta viewer's clipboard cannot carry a summary their JSON omitted.
  **20.4** one role-driven profile screen + the patient context bar, three visually distinct states, four-cue
  call direction (RTL-mirrored arrows), and an accessible copy button that copies the server's `copyText`
  verbatim with an `aria-live` confirmation and a textarea fallback.
  **20.5** Kong routes, a compose block with no connection string, three scopes (`profile:read`,
  `profile:export`, `callcentre:history:read`), `docs/api/profile.json`, and docs 11/14/16/19/22 + the index.
  **20.4 completed:** permission-gated module deep-links from each section (rendered or absent, never
  disabled — a greyed-out "Start encounter" on a receptionist's screen advertises a capability they will never
  have); the print/export summary fetched from the SERVER and printed with its payload watermark rather than
  rendered from the DOM; the patient context bar mounted in encounter, lab, dispense and approvals screens
  (for a lab and a pharmacy its projection is min-header + ALLERGIES, which is exactly what matters at the
  bench and at the counter); search → profile from the member-admin result row and from the call-centre 360
  after a verification pass. **20.5 completed:** `perf/k6/06-patient-profile.js` encodes both budgets and
  asserts the context bar returns a SUBSET as well as being fast — a regression that made it fetch the whole
  profile would still be correct and would silently cost seconds on every clinical screen.
  Tests: authz **109**, profile-service **82**, callcentre **37**, web **150**. Four existing gates caught real
  slips during the build — scope-integrity (a scope granted to roles with no call-history cell), the bare-clock
  scanner, the OpenAPI-coverage gate, and the display-truth rule (a raw `toLocaleString`).
  **20.2** the four partial 360s are consolidated onto the one contract. The case beneficiary-360 and the
  call-centre member 360 now DELEGATE to profile-service under the caller's own bearer instead of fanning out
  themselves; `HttpBeneficiary360Assembler` is deleted. Three of the five endpoints it used to call
  (`/care-plan-summary`, `/coordination-summary`, `/beneficiaries/{id}/appointments`) never existed, so those
  blocks had been silently empty in every environment — the coordination view's active-diagnosis list is
  populated for the first time. Appointments/contacts/follow-ups stay with their owning services: they are
  call-centre ACTION affordances, and design 39 §3 has no section for them; widening the contract to fit the
  implementation would have been the wrong direction.
  **Upstream seams** — five new beneficiary-scoped reads (`emr /profile-context`,
  `orders|pharmacy|approvals|case /for-beneficiary/{id}`), each gated by `ProfileSeam`: the OWNING service
  resolves the ABAC fact it owns (emr the treating relationship, case the assignment, orders the sensitivity
  and the grant) and consults the SAME design-39 §4 matrix. The existing narrow rules (`orders:read` =
  doctor+treating, `rx:read`, emr's treating/oversight split) are untouched and still guard every other
  endpoint. `SectionWiringTests` now fails the build if a provider points at a route nothing serves — the
  defect that hid here before, because a permanently-broken section and a momentarily-broken one look
  identical at runtime by design.
- **Phase 14 (Branch & Sensitivity)** is listed above **before 7** because it is a cross-cutting *retrofit* of
  `libs/authz`, identity, provider, emr and orders — that is its correct execution slot. Phases 7–10 were built
  ahead of it, so landing 14 also means revisiting the approvals worklist (member-scoped, no sensitive-result
  content) and the phase-9 portals (branch switcher, locked-result state). Migrations are additive only and the
  full existing suite must stay green. Design: `HBMP-Design/37-branch-scoping-and-clinical-sensitivity.md`;
  prompt: `HBMP-Design/claude-code-prompts/phase-14-branch-scoping-and-sensitivity.md`.
- **Phase 11 (Hardening & NFR assurance)** is cross-cutting evidence, not new features. **11.1** `/perf` k6 suite
  (eligibility/consume-race/worklists/dashboards/mixed-soak, thresholds = NFR §1/§2 targets → CI-gated) + deterministic
  synthetic volume generator (NFR-012, no PHI, verified runnable) + `docs/PERFORMANCE-BASELINE.md` (measured cols
  = PENDING-staging, not fabricated) + ADR 0009 (RLS-first indexes + Valkey invalidation) + `perf-ci.yml`. **11.2**
  `docs/security/` STRIDE model + OWASP API Top 10 sweep; `security-ci.yml` (gitleaks + Trivy SCA/config/image +
  CodeQL + ZAP, block on Critical/High); `docs/compliance/security-sign-off.md` (✅ code/CI/tests vs 🟡
  operational-gate-on-target-infra). **11.3** Prometheus `/metrics` now live on **all 17 services** (OTel Prometheus
  exporter + runtime instrumentation, solution 0-warning) + `services` scrape job + Alertmanager + Grafana
  dashboards-as-code (golden-signals + business-kpis) + alert rules (SLO burn / event-bus / failed-consume /
  approvals-SLA / auth-anomaly / audit-chain) + 7 runbooks + `infra/dr/restore-rehearsal.sh` **executed** (138
  tables/18 schemas reconciled exactly, audit chain-linkage intact → PASS; second-site failover pending target k3s).
  The ✅ items gate now; the 🟡 items need staging/k3s/OpenBao/external-pentest to sign off. Prompt:
  `HBMP-Design/claude-code-prompts/phase-11-hardening-and-nfr.md`.

## Phase 29 — encounter, OP procedures, service history and chronic prescribing

Design: `HBMP-Design/45-encounter-and-prescription-adjustments.md` · ADR-0038 ·
Prompt: `HBMP-Design/claude-code-prompts/phase-29-encounter-and-chronic-prescribing.md`

> The prompt's INVARIANTS block (lines 21-32) contradicts its own gates — it says the rename is label-only and
> that E/M is never orderable, where Gate 1 mandates a full identifier rename and Gate 2 routes E/M to a
> Referral. Doc 45 §8 agrees with the GATES, and the gates were followed. Recorded in ADR-0038 §0.

- **29.1 the Radiology rename, identifiers included.** Full rename executed expand → backfill → switch →
  contract. `libs/auth/LegacyRoleAliases` expands roles at the single token→principal boundary — BIDIRECTIONALLY,
  because services deploy independently and the switched issuer mints the new name at a service that has not
  been redeployed, which recurs on every rollout rather than only in the first 300 s. The CONTRACT step lives
  in `Migrations/deferred/`, which the migration runner does not glob: beside the others it would have applied
  on the same deploy as the switch and the window would have been zero seconds wide. Historical audit rows are
  never rewritten — `LegacyIdentifierDisplay` resolves them for readers permanently, with a counter-proof test
  that rewriting one breaks the chain. Token contract AMENDED with a checked-in pre-switch fixture. NOT renamed:
  the `IMAGING` benefit category (a coverage vocabulary with live accumulators) and the reporting consumer's
  translation (it maps replayed historical events, forever).
- **29.2 OP Procedures as an order type.** `order_type = 'Procedure'` reuses orders-service entirely; sessions
  are the line's QUANTITY, metered by the existing atomic consume. `masterdata.procedure_type` is master data
  with `is_session_based`, validated against the code's CPT section on the WRITE path. Sessions flow from the
  APPROVED scope, never the requested one. **Two reconciliation findings reported, not silently resolved:**
  `cpt_code.category` holds the CPT taxonomy (Category I/II/III/PLA/MAAA), not the section, so it cannot drive
  routing; and doc 45 §2's Medicine and E/M ranges overlap. Emitted with every load against all 10,810 codes.
- **29.2b the external provider portal.** `assigned_provider_id` binds ownership at the ROW, with the
  two-provider test written before the queue endpoint — the audit-R3 `DispensingGate` defect asks the ownership
  question of the caller's own id, and no test caught it because answering it needs TWO providers. Not-yours is
  404, not 403 (a 403 is a membership oracle). Per-session idempotency; referral loops cannot close on an empty
  report; identity at the counter behind two identifiers.
- **29.3 encounter + history tabs.** OP Procedures tab on the SHARED composer (no third copy). History pane
  filters the SAME authorised section by order type — no new access path. Also fixed a 29.1 leftover: the
  encounter tab, heading and compose button still said "Imaging".
- **29.4 per-line service history.** ONE endpoint, ONE component. An INTERSECTION of treating relationship,
  branch scope and the design-37 §6 sensitivity rule — a restricted result is existence-only with no value
  anywhere in the payload, asserted over the SERIALIZED bytes. Three states distinct in words: "could not load"
  is never "none". Trend shown where numeric and permitted, with the table alongside.
- **29.5 acute/chronic prescriptions.** Brainstormed the window model, then TDD'd the allocation. **The counter
  enforces; the sweeper records** — `Blocked` and `Missed` are stored (events with money consequences), `Open`
  is never written, so a stalled sweeper delays a forfeiture but can never refuse a patient. Round ONCE at the
  total; the sum invariant is a PROPERTY test, not four examples. 66 TDD tests in `libs/prescribing`.
- **29.6 prescribing unit, pack size, splittability.** `pack_size` ← X "Minor Units (total)" at 100% coverage.
  **W "Major Units (per box)" is strips-per-box** and mapping it would be 10x wrong. 89% derive a unit and
  splittability from the dosage form; the other 2,495 rows report NotChecked NAMING the field. Coverage printed
  on every load.
- **29.7 lowest price and availability.** Group = ingredient + strength + form; comparison is per PRESCRIBING
  UNIT (`price ÷ pack_size`). A drug with no pack size is never labelled — falling back to pack price is the
  exact error the rule prevents. `availability` is three-valued, defaults to `Unknown`, and Unknown renders
  NOTHING.
- **29.8 docs.** ADR-0038 (numbered 0038 because 0029 was taken — same collision ADR-0037 records), 13 new
  invariant-registry entries each with named tests, the rename runbook, the window-model spec and the
  workbook-mapping note.

## Phase 30 — amending and cancelling signed orders ✅

Design: `HBMP-Design/46-order-amendment-and-cancellation.md` · ADR-0039 ·
`docs/phase-30-gate-0-audit.md`, `docs/phase-30-gate-3-notes.md`, `docs/phase-30-gate-5-notes.md`

- **30.0 audit.** Reported per order kind before building. Three findings changed the plan: lab, radiology and
  OP procedure are ONE table discriminated by `order_type`, not three; the fulfilment queues are LIVE QUERIES
  rather than read models, so cancellation propagates in the same transaction; and
  `tools/ci/check-event-symmetry.py` does not exist, so the "~40 orphaned events" figure is an unverified
  claim from a prompt and "symmetry gate green" could not be an acceptance criterion.
- **30.1 supersede-and-cancel.** Line-level version chain, coded reason vocabulary, append-only amendment
  ledger. "Nothing signed is mutated" is a TRIGGER; "nothing is deleted" is a REVOKED privilege. **Found two
  defects predating the phase:** neither `PartiallyUsed → Cancelled` nor `PartiallyDispensed → Cancelled`
  existed, so a partly-fulfilled order or prescription could not be cancelled AT ALL — design 46 §3's opening
  example, unreachable. Both roll-ups would also have stranded a superseded line as live.
- **30.2 the guarded transition.** One atomic statement; the conflict names what happened, when and by whom;
  the mirror refuses a consume/dispense on a withdrawn line with the reason and the corrected line's id.
  Concurrency proven on the existing harnesses.
- **30.3 chronic amendment.** Brainstormed the window model first. Nothing is moved and nothing is copied: the
  original keeps its schedule, its uncollected windows take a terminal `Superseded`, the successor gets a
  fresh one. **Reducing below the chronic definition is reported, never decided.**
- **30.4 authorisation scope.** ONE comparator — the subset predicate moved down into `libs/amendment` and
  approvals' `ValidatePartialScope` now calls it. Re-approval reuses the event the original routing used.
- **30.5 propagation.** Four events with a named subscriber (the care timeline). Two cases turned out
  structural and were recorded rather than built around: the provider queue and claims.
- **30.5b order notes.** The doc-38 model on a different subject. The Internal-note projection is asserted
  over the SERIALIZED payload, and the external provider reads through their own portal.
- **30.5c the timeline opens at check-in.** `checked_in_at` did not exist; `updated_at` is overwritten by
  every later transition, so waiting time could not honestly be derived. Three cases kept distinct.
- **30.6 authority, dialog, two therapies.** Denial tests for reception, call centre, pharmacist and lab tech.
  Occupational and Speech Therapy are a migration and nothing else — `ProcedureTypeIsDataNotCodeTests` proves
  no source file branches on a type NAME.
- **30.7 docs.** ADR-0039, 10 invariant-registry entries each with named tests, doc 23 §2b/§3b, three phase
  notes.

### Two gaps found and closed

- **Phase 29's chronic prescribing was never wired.** No endpoint set `kind='Chronic'`, no refill window was
  ever created, the sweeper ran hourly against an empty table — while all 66 allocation tests passed. Closed:
  submit builds the schedule, the counter meters against it. `INV-CHRONIC-PRESCRIBING-IS-REACHABLE` exists
  because a green suite says code is correct, never that it is *called*.
- **`GET /investigation-orders/queue` returned 500** on the call the bench screen makes — non-nullable
  `page`/`pageSize` — and no test could reach it because the fixture's lab client held less scope than a real
  `lab_tech` token. Both fixed.

### Not done in phase 30

- Wiring the amend dialog into DoctorEncounter and the worklists; surfacing waiting time on screen.
- Pharmacy's notes endpoints (`pharmacy.rx_note` is correct, unwired schema — flagged in its own header).
- `tools/ci/check-event-symmetry.py`, still outstanding from phase 22.

- Docker/Compose, Helm, OpenTofu: **not yet installed** (Docker needs root). Tier 1 infra authored in `infra/compose`; run once Docker is installed.
- Repo initialized in place at `/home/mohab/Mersal` with `HBMP-Design/` as a subfolder.
