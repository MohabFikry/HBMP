# Mersal HBMP — Continuation / Handoff (read this first)

**Purpose:** everything a new engineer/LLM needs to continue building the Mersal HBMP exactly as started. Pairs with `docs/BUILD-STATUS.md` (the phase-by-phase checklist) and the prompt library at `HBMP-Design/claude-code-prompts/00-MASTER-PROMPT-LIST.md`.

Last updated: 2026-07-25 (Phase 8b — admin & platform management — complete; next is Phase 9 / 10).

---

## 1. What this project is
A phase-by-phase production build of the Mersal Healthcare Benefit Management Platform (.NET 8 microservices + React later, open-source on-prem-first stack). The **design set** is in `HBMP-Design/` (docs 0A–35). The **build prompts** are in `HBMP-Design/claude-code-prompts/` — one sub-prompt ≈ one commit/PR. Root `CLAUDE.md` carries the conventions (auto-loaded). 20 skills in `.claude/skills/`.

**Golden rule:** read the design docs a prompt names *before* coding; if reality diverges, flag it in the commit, don't silently deviate.

## 2. How to build/test (this machine)
- **.NET 8 SDK is user-local** at `~/.dotnet`. Always use **`./dotnet.sh`** (wrapper) — e.g. `./dotnet.sh build`, `./dotnet.sh test HbmpPlatform.sln`.
- Node 20, npm, git, `psql 17` client present. **No** system .NET.
- Full suite: `./dotnet.sh test HbmpPlatform.sln -c Release`. **107 tests green** as of last commit (document-service tests not written yet).

## 3. Progress (commits, newest last)
```
82667bf chore(platform): scaffold monorepo, CI/CD, dev IaC (0.1)   <- SHA rewritten by the 2026-07-30 history purge (docs/runbooks/history-purge-2026-07-30.md)
e4ec9dd feat(auth): libs/auth + Keycloak realm-as-code (0.2)
6be102a feat(audit): audit spine — libs/audit-client + audit-service (0.3)
f4d3624 feat(authz): libs/authz RBAC+ABAC row+field + break-glass (0.4)
958a216 feat(platform): libs/events outbox + hello-service slice (0.5)  ← Phase 0 COMPLETE
f754520 feat(masterdata): masterdata-service + real ICD/CPT/ATC/drug ingest (0b)  ← Phase 0b COMPLETE
23cab1b chore(infra): turnkey Tier1 up.sh + seed-masterdata.sh
761de68 feat(patient): patient-service registration + dedup (1.1)
b257cd4 feat(policy): policy-service coverage/limits/reset (1.2)
```
**UNCOMMITTED work in the tree right now** (finish + commit these):
- `services/document/**` — Phase **1.3 document-service, PARTIAL**: Domain done (`Entities`, `UploadValidation`, `UploadPipeline` with `IMalwareScanner`/`IBlobStore` ports), Infrastructure `DocumentDbContext` + `0001_document_schema.sql` done. **NOT done:** `Api/Program.cs` (still hello-world), Infrastructure ClamAV scanner + MinIO blob store + DI, Tests. Projects ARE added to the solution.
- Master-data snake_case fix: added `EFCore.NamingConventions` (Directory.Packages.props), masterdata Infra csproj refs it, masterdata Api + loader call `.UseSnakeCaseNamingConvention()`, and `MasterDataDbContext` maps `Icd11Map → icd11_map` explicitly (convention renders digits wrong).
- `infra/compose/compose.yaml`: postgres host port changed to **55432** (see §5); patient(:8092)/policy(:8093)/masterdata(:8091)/audit/hello services added.
- `infra/compose/seed-masterdata.sh`: uses port 55432.

## 4. Live stack state (as left running — ephemeral)
- **Docker** installed (29.1.3). Socket access was granted with `sudo chmod 666 /var/run/docker.sock` — **this resets when the Docker daemon restarts**; re-run it (or `sudo usermod -aG docker $USER` + new login) to use docker again.
- **Running containers:** postgres, keycloak, kong, minio, rabbitmq. **Not pulled/started:** nats, valkey, opensearch, openbao, clamav, prometheus, grafana, loki, tempo (Docker Hub was flaky — pull them with retries; see §6).
- **Real master data IS loaded** into the `hbmp` DB `masterdata` schema: 16,751 icd_code · 10,810 cpt_code · 2,150 atc_class · 25,063 drug (15,311 ATC-linked). Verified via psql.
- `masterdata-service` was run on the host at `http://localhost:5072` (background `dotnet run`; **dies with the session** — restart with §7).
- **Keycloak realm `mersal` imports fine** (OIDC config serves the custom scopes). **KNOWN ISSUE:** the master-realm bootstrap admin login fails ("Invalid user credentials"/"user_not_found") even after recreating the keycloak DB — so we could NOT mint a token to demo an authenticated 200. Unauthenticated calls correctly return 401 (auth is enforced). **To fix/get a token** (needed to exercise protected endpoints live): investigate KC 25 bootstrap (`KC_BOOTSTRAP_ADMIN_USERNAME/PASSWORD` in compose env) — possibly use `docker exec … /opt/keycloak/bin/kc.sh bootstrap-admin user` or set a fresh admin, then create a confidential client `hbmp-demo` (serviceAccounts + directAccessGrants + an `oidc-audience-mapper` for `hbmp-api`) and use client_credentials/password grant. The API validates `aud=hbmp-api`, so tokens MUST carry that audience.

## 5. Critical gotchas (will bite you)
1. **Postgres host port is 55432**, not 5432 (local PG16 owns 5432). Inside compose, services connect to hostname `postgres:5432`. Host tools (loader/psql) use `localhost:55432`. `.env` (gitignored) is in `infra/compose/.env` with dev secrets.
2. **.NET 8, not 9.** Do NOT use `ToHashSetAsync` (use `(await q.ToListAsync()).ToHashSet()`), `Convert.ToHexStringLower` (use `Convert.ToHexString(x).ToLowerInvariant()`), or `System.Threading.Lock` (use `object`).
3. **Analyzers = errors** (warnings-as-errors). Repo-wide `NoWarn`: CA1848/CA1859/CA1725/CA1716. Test projects also relax CA1707 etc. Records declared in a top-level-statements `Program.cs` trip CA1050 → put them in a namespaced file.
4. **NuGetAudit fails the build on HIGH/CRITICAL** advisories (matches the Trivy gate). Pin patched transitive versions in `Directory.Packages.props` when it complains.
5. **EF column naming:** patient/policy/audit map every column explicitly with `HasColumnName` (snake_case). masterdata uses `.UseSnakeCaseNamingConvention()`. Pick one per new service and be consistent; watch digit-boundary props (e.g. `Icd11Map`).
6. **Central Package Management:** every `PackageReference` has NO `Version=` — versions live in `Directory.Packages.props`. Classlibs that need ASP.NET types use `<FrameworkReference Include="Microsoft.AspNetCore.App" />`, not packages.
7. Nested `Tests/` under a lib project: exclude with `<Compile Remove="Tests/**/*.cs" />` in the lib csproj (services keep Tests as a SEPARATE sibling project — no exclude needed).
8. Background `docker compose up` of all images aborts if any one image fails to pull. **Pull sequentially** with per-image retries (Docker Hub TLS timeouts are common here).

## 6. Bring the stack fully up
```bash
# 0. (after any docker daemon restart) re-enable socket access:
#    sudo chmod 666 /var/run/docker.sock
# 1. pull the not-yet-pulled images (retry loop; do NOT background — they got SIGKILLed):
for img in nats:2.10 valkey/valkey:8 opensearchproject/opensearch:2.15.0 openbao/openbao:latest \
           clamav/clamav:latest prom/prometheus:latest grafana/loki:3.1.0 grafana/tempo:latest grafana/grafana:latest; do
  for a in $(seq 1 8); do docker image inspect "$img" >/dev/null 2>&1 && break; docker pull -q "$img" && break; sleep 4; done
done
# 2. start everything:
cd infra/compose && docker compose up -d
# 3. build + start the app-service images (or run on host, see §7):
docker compose up -d --build audit-service masterdata-service patient-service policy-service hello-service
```
Endpoints: Keycloak :8080, Kong :8000, Grafana :3000, MinIO console :9001, RabbitMQ :15672, masterdata :8091, hello :8090, patient :8092, policy :8093.

## 7. Run a service on the host (fast, no image build)
```bash
set -a; . infra/compose/.env; set +a
export ConnectionStrings__MasterData="Host=localhost;Port=55432;Database=hbmp;Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}"
export Auth__Authority="http://localhost:8080/realms/mersal" Auth__Audience="hbmp-api" Auth__RequireHttpsMetadata="false"
export ASPNETCORE_URLS="http://localhost:8091" OTEL_SDK_DISABLED="true"
./dotnet.sh run --project services/masterdata/Api -c Release   # (launchSettings may override the port → check the log for "Now listening on")
```
Re-seed master data any time (idempotent): `bash infra/compose/seed-masterdata.sh`.

## 8. The reusable service pattern (copy `services/hello` or `services/patient`)
Per `CLAUDE.md`: `Api/ Domain/ Infrastructure/ Tests/` + `Dockerfile` + `README.md`. `Program.cs` wires:
```csharp
builder.Services.AddHbmpAuthentication(builder.Configuration);   // libs/auth  (JWT+MFA)
builder.Services.AddHbmpAuditClient("<name>-service");           // libs/audit-client
builder.Services.AddHbmpAuthorization();                         // libs/authz (RBAC+ABAC row+field)
builder.Services.AddHbmpEvents(builder.Configuration, useInMemory: true); // libs/events outbox
// + AddDbContext(UseNpgsql...), OpenTelemetry, AddProblemDetails, Swagger
app.UseAuthentication(); app.UseAuthorization();
// endpoints: .RequireAuthorization(HbmpPolicies.Scope("<scope>")); authorize via IAuthorizationEngine;
// mutate in a txn that also EmitAsync(audit) + outbox.EnqueueAsync(event). RFC7807 problem+json on failure.
```
Then: add 4 projects to `HbmpPlatform.sln`, add a Dockerfile, a compose entry, a service README, and tick `docs/BUILD-STATUS.md`. Schema migrations are hand-authored SQL under `Infrastructure/Migrations/*.sql` (partition/grants/RLS/triggers need raw SQL); apply on dev startup or via a small runner.

## 9. IMMEDIATE next tasks (in order)
> **UPDATE 2026-07-27: Phase 18 (audit-R2 E2E remediation) is COMPLETE across gates A–E, with Gate F prioritised.** Every finding in `docs/AUDIT-R2-E2E.md` (X1–X10, S1–S9, W1–W8, U1–U10, Q1–Q4) is closed and held by a test or a CI gate that fails the build on regression. **Read `docs/security/security-sign-off.md` first** — it states what is proven, what is an unverified operational gate, and the one item still needing a product decision. Headlines: coverage limits actually bind and the claims money layer is capped by a `Money` type rather than six clamps; every runtime connection is a non-superuser role with a bound tenant GUC and **92/92 tenant-scoped tables are proven isolating** by a fuzzer driven off `information_schema`; admin + identity-admin are gated at the route table with MFA; CSRF is closed on the issuer's credential forms; sessions renew and branch switching works end to end; the FHIR façade, report-access approval, identity admin and the SPA are all reachable; no write path fails silently; axe is clean on every route in EN + AR and both themes. **Four defects were found by the remediation's own new gates, not by the audit:** `emr.appointment_history` held 105 blank-tenant rows readable by any caller without a tenant claim (an append-only clinical table); 141 rule/role pairs no token could satisfy, including the entire claims decision surface and break-glass for every clinician; `provider.practitioner` had no RLS policy at all; and provider-service hand-rolled its RLS binding. **Still open:** six roles named by policy rules but absent from the frozen vocabulary (a token-contract decision — see the sign-off §3), and the operational gates that need a live stack (Kong edge JWT, the FHIR smoke test, the Playwright contrast job). Everything below is historical reference.

> **UPDATE 2026-07-26: Phase 11 (Production hardening & NFR assurance) is now COMPLETE** — cross-cutting **evidence/harness/config, not new features**; 3 commits on `master`. **Integrity note:** this phase's gates are defined against a live prod-like **staging** cluster (1M-row load, external pen-test, second-site DR failover) which this dev repo lacks — so I built the real versioned deliverables and marked staging-only numbers **PENDING** rather than fabricating "measured" evidence. Split is explicit: ✅ enforced-now (code/CI/tests) vs 🟡 operational-gate-on-target-infra. **11.1 perf** `cac6e7a`: `/perf` k6 suite (eligibility NFR-002/004, consume NFR-003/073 incl. single-line **race proving zero double-commit**, worklists NFR-001/004, dashboards NFR-006, mixed-soak NFR-014) — each script's k6 **threshold** = the NFR target so a miss is a non-zero CI exit; `perf/data-gen/generate.mjs` deterministic synthetic volume gen (NFR-012, no PHI, **verified runnable**); `docs/PERFORMANCE-BASELINE.md` (measured cols = `PENDING (staging)`); ADR `0009` (RLS-first covering indexes + Valkey eligibility invalidation-on-policy-change); `.github/workflows/perf-ci.yml`. **11.2 security** `c9d20c7`: `docs/security/threat-model-stride.md` (edge→context→data-tier) + `owasp-api-top10-checklist.md` (BOLA row+field sweep per role×resource, proven by existing authz suites); `.github/workflows/security-ci.yml` = gitleaks + Trivy SCA/config/image + CodeQL C# + ZAP, **exit-code 1 on Critical/High**; `.gitleaks.toml` (repo verified: **zero hardcoded secrets**, all `${VAR}` from gitignored `.env`; prod=OpenBao); `docs/compliance/security-sign-off.md` (go-live gate consumes this). **11.3 reliability/observability/DR** `eb9c339`: **REAL CODE** — `/metrics` (OTel Prometheus exporter + runtime instrumentation) now on **all 17 services** (`using OpenTelemetry.Metrics;` + `.WithMetrics(...AddPrometheusExporter())` + `app.MapPrometheusScrapingEndpoint()`; new pkgs in Directory.Packages.props + each Api.csproj; **solution 0-warning**); prometheus.yml `services` scrape job + Alertmanager compose svc; Grafana dashboards-as-code (`golden-signals`+`business-kpis`); alert rules (SLO burn / event-bus / **failed-consume** / **approvals-SLA** / **auth-anomaly** / **audit-chain-stalled**); 7 runbooks in `docs/runbooks/` + `infra/observability/README.md`; **`infra/dr/restore-rehearsal.sh` EXECUTED** vs compose PG (pg_dump→restore-scratch→**exact count(\*) reconciliation** [n_live_tup is a stale estimate — use exact] + **audit hash-chain linkage** check): **138 tables / 18 schemas reconciled, 0 chain breaks = PASS** (`docs/runbooks/dr-drill-report.md`; dev audit table empty ⇒ chain traversal is a structural pass; second-site failover pending target k3s). Tests: callcentre 30 / emr 88 / audit 3 green. **Next: Phase 12** (data migration / release mgmt / go-live — consumes 11.2 sign-off + 11.3 DR report); then 13 (interop/FHIR); **10b Claims still pending** (R6/post-v1).

> **UPDATE 2026-07-26: Phase 15 (Call Centre portal) is now COMPLETE, backend→UI** — all six slices `15.1`–`15.6` landed on `master`. New **`callcentre-service`** owns schema `callcentre` (**MemberScoped / all branches** per design 37 §3; migrations `0001`+`0002` applied to live PG). **The defining control = verify before you disclose:** no member detail until a Passed `caller_verification` for THIS interaction + THIS beneficiary (`VerificationService.IsVerifiedAsync`, valid only while Open, expires on close). **Privacy rule:** `caller_verification.verified_identifiers` jsonb stores only WHICH identifier TYPES were confirmed — NEVER the values. **No clinical data ever:** `Member360` has no field that can carry a diagnosis/result/rx/note (proven by reflection + serialized-JSON authz test). Commits: 15.1 `bdcd50a` (interaction + caller-verification foundation; ≥2 distinct KNOWN identifier types for a Pass; Fail persisted AND audited; `libs/authz/CallCentrePolicies`; ADR 0008) · 15.2 `324bfc7` (thin pre-verification search + clinical-free 360 across ALL branches, 403+audit until verified; `IMemberDirectory` fail-soft composition) · 15.3 `533467a` (book/reschedule/cancel **delegated to the emr engine** — Idempotency-Key + If-Match forwarded verbatim, no-double-book preserved; cancel reason mandatory; `AppointmentLink` migration `0002`; queues clinical-free `AppointmentConfirmationRequested`) · 15.4 `eea308c` (contact corrections via patient-service + referral→booking conversion) · 15.5 `66feba8` (web `call-centre` portal — call-shaped `CallCentreWorkspace`: start→search→verify→360→act→wrap-up; no detail in DOM pre-verify; aria-live; four-cue lock; 409 recovery) · 15.6 `(15.6)` (PHI-free `KpiService` + `/call-centre/kpis` supervisor-only; **E2E journey test** asserting the correlated event chain + clinical-free notification + KPIs). Infra `8f7b249`: compose `callcentre-service` :8107 + Kong routes. **Tests green:** 30 callcentre (env-gated `CALLCENTRE_TEST_DB`, live PG :55432) + web 33; full solution 0-warning. **Next: Phase 11** (hardening/NFR); then 12/13.

> **UPDATE 2026-07-26: Phase 14 (Branch scoping, practitioner specialty & clinical sensitivity) is now COMPLETE, backend→UI** — all eight slices `14.1`–`14.8` landed on `master`. Commits: 14.1 `d878aec` (provider-service **`branch`** entity — internal Mersal facilities, distinct from `provider_location`; six branches ASW/ALX/OCT/MAA/DOK/NSR seeded EN+AR; migration `0005`) · 14.2 `9e2edc4` (admin-service **`user_branch_assignment`** — one active Home partial-unique + Additional; `libs/auth` `BranchAssignment`/`BranchAssignmentRules`/`IBranchContext` + `X-Active-Branch`; `/me/branches` + `/me/active-branch`, out-of-set → 403 + High `BranchScopeDenied`; migration admin `0004`) · 14.3 `a1529ab` (`libs/authz` **`BranchScope`** ABAC condition + `ResourceRef.BranchId/PermittedBranchIds/ActiveBranchId` + engine case + `RowScope.BranchIds`/`WithBranchScope` + **`ScopeMode`/`BranchScopeModes.ModeFor`** role classifier — operational=BranchScoped, approvals/director/finance=MemberScoped, external=ProviderScoped) · 14.4 emr `2bfeedb` + orders `25e6f4e` (`branch_id` on appointment/slot/availability/waitlist/queue [emr `0006`] + `ordering_branch_id` on investigation_order [orders `0003`]; **`BranchScopeResolver`/`IBranchDirectory`** [HTTP→admin `/me/branches`, 60s cache, fail-closed] + per-request middleware; worklists filtered to the active branch, cross-branch by-id → 403 not 404; **provider fulfillment queue untouched**; phase-3 no-double-book intact) · 14.5 `f3e54bc` (provider-service **`practitioner`/`specialty`/`practitioner_specialty`/`practitioner_branch_assignment`** [migration `0006`]; specialty seed incl. **Psychiatry + Clinical Psychology**; one-primary partial-unique; doctor picker min-necessary [no licence to non-admin]; `serves-branch` probe) · 14.6 masterdata `27db413` (**`examination_type`** reference + sensitivity classification [migration md `0003`]; mental-health = Sensitive+MentalHealth; orders `examination_type_id`+`sensitivity_level` PINNED at creation, fail-closed 422 on unknown, `SensitiveResultRestricted` on non-Standard result; migration orders `0004`) · 14.7 `25d3154` (**THE PRIVACY HEART**: `report_access_request`+`report_access_grant` [orders `0005`]; a non-Standard result is default-deny except the authoring doctor or an active grant → everyone else incl. approvals gets **existence-metadata only**; justified request → author OR **Medical Director** decides [MD flagged + extra-audited]; time-boxed [72h/24h] single-result non-transferable revocable auto-expiring grants; every read-under-grant = distinct **`SensitiveResultReadUnderGrant`** audit) · 14.8 `0f5a4bd` (web **BranchSwitcher** [app-bar, aria-live, ≥44px, fail-soft `useBranchContext`] + **RestrictedResultCard** [four-cue locked state, no values in DOM] + **RequestAccessDialog** [purpose+justification required]). **Tests green:** authz 24 · emr 88 · orders 56 · provider 39 · admin 70 · masterdata 16 · web 27 (all vs live PG :55432 where DB-gated; migrations applied). Full solution builds 0-warning. **Next: Phase 15** (Call Centre portal — depends on this phase). Then Phase 11/12/13.
>
> **UPDATE 2026-07-26: Phase 10b (Claims Management) is now COMPLETE** — all nine slices `10b.1`–`10b.9` landed on `master`, **153 claims tests green** against real Postgres (:55432). New **claims-service** owns the `claims` schema (migrations `0001`–`0008`, RLS + append-only ledgers). Commits: 10b.1 `0278c0b` (foundation + auto-derived, no-double-billing DB index) · 10b.2 `e68a64e` (batching, single-open-batch DB guard) · 10b.3 `2323e2f` (9-step pre-adjudication, ALL reasons) · 10b.4 `735021a` (officer worklist + line decisions: SoD + dual control + append-only) · 10b.5 `6851e54` (provider-submitted claims + document matching; matched/unmatched/DUPLICATE via the 10b.1 index) · 10b.6 `fc2d76a` (beneficiary reimbursement + pluggable OCR — assistive/never-authoritative, human-gated, append-only `ocr_extraction`, min(tariff,receipt) cap, NO bank fields) · 10b.7 `2bac316` (reconciliation buckets with fixed precedence + append-only signed adjustments, sign-per-type, recovery-reference, negative-net dual control) · 10b.8 `acf86eb` (immutable settlement advice + CSV/XLSX/PDF exports with ZERO clinical fields — **the platform NEVER moves money**, ADR 0007, enforced by a source-scan test) · 10b.9 `7dc6ea7` (appeals preserving the original decision thread + original-decider SoD + settled-batch→adjustment; aggregate-only claims KPI feed). **The 4 claims invariants** all proven vs Postgres: no double-billing (partial unique index), append-only ledgers (trigger + no UPDATE/DELETE grant), claims≠diagnosis (authz overlay + clinical-free DTOs + no clinical column — `ClaimsCannotReadDiagnosisTests`), provider isolation (ABAC PO + RLS). Deferred seams (default no-op, wired later): `IFulfillmentResolver` (orders/pharmacy), `IDocumentOcrProvider`/`IDocumentScanner` (Tesseract/ClamAV), `IAuthorizedServiceResolver` (approvals), `IContractTariffProvider`/`ISettlementDocumentStore` (provider/document HTTP). Claims integration tests are env-gated `CLAIMS_TEST_DB` and need the **hbmp superuser** conn; serialized via the `claims-db` collection. **Next: Phase 14** (branch scoping, practitioner specialty & clinical sensitivity — precedes Phase 15 Call Centre).
>
> **UPDATE 2026-07-25: Phase 8b (admin & platform management) is now COMPLETE** (8b.1 `70e8f17`, 8b.2 `e761ea3`, 8b.3 `8107dc1` — new **admin-service**, `admin` schema — **543 tests green**; admin 67). Org Admin (`tenant:own`) + Super Admin (`global`) administer **WHO can access, not content**; every admin action AND admin read immutably audited. **The crux is the reusable SoD engine** `libs/authz/SegregationOfDuties.cs` — the `10-role-matrix §7` conflict matrix expanded to concrete role/capability tokens, evaluated at **assignment time** here (re-checked at decision time by the deciding services): Doctor↔Medical-Approval/Director, Finance payment-initiate↔release, **Org Admin↔Super Admin (no privilege escalation)**, Provider Admin↔any clinical role (no PHI self-elevation), Network Team↔payment-release, the claims pairs (submitter↔officer/reviewer, officer↔reviewer dual-control, adjudication/settlement↔finance halves, provider-affiliated↔claims adjudication). A coarse `finance` grant is treated as covering both payment halves. `libs/authz/AdminPolicies` (org_admin+super_admin, every action Sensitive→audited; Org Admin tenant-pinned, Super Admin global via null resource-tenant). **8b.1** — role bindings (grant SoD-checked + justification-required → 409 with the conflict reason; revoke; **de-provision** FR-IAM-010 revokes every binding + blocks the subject so `EffectiveRolesAsync` returns empty = denied everywhere), soft-lifecycle (one active grant per tenant/subject/role); **access-review campaigns** snapshot active T3/T4 grants → recertify/revoke, unconfirmed **auto-expire** at the deadline (revokes the binding) FR-IAM-007; effective-dated **session policy** (§9) + **device/IP policy** (§3.4–3.5); staged **policy-bundle proposals** (propose/diff only, never hot-patch live ABAC — Super Admin only). **8b.2 governance** — effective-dated **master-data versions** (`master_data_version`, JSON attribute snapshot per code system, append-a-version + close-prior + `ResolveAsOfAsync` so a historical order resolves the version in force at ITS time, FR-MDM-007) **clinical-governance (Medical Director)+Super Admin only** (FR-MDM-008, Org Admin denied); bilingual **notification templates** linted before save (`TemplateLinter`: PHI/clinical token in an SMS/email body → rejected, AR/EN parity required; in-app exempt from PHI rule not parity, FR-NOT-005); typed+validated+effective-dated **system config** (`Text/Whole/Number/Boolean/Duration`, tenant or platform `*`). **8b.3** — **tenant administration** (Super Admin, `tenant` registry, RLS isolation, FR-IAM-008; provider metadata stays in provider-service = oversight view only); **break-glass lifecycle** (18-security §11): request (reason code + justification + scoped resource types/ids) → **dual-control approve** (approver≠requester; self-approval rejected 409 + audited) → **step-up MFA activate** (requester + stepUpSatisfied else denied) → scoped auto-expiring window → **access** (in-scope 200 / out-of-scope 403 = **no field-deny bypass beyond scope**) → sweep auto-expiry; every access = **HIGH-severity `break_glass` audit event** (loud audit + Security/DPO alert seam) for post-hoc review; active grant maps to runtime `libs/authz/BreakGlassGrant`; **governance dashboards** (`/dashboards/break-glass|access-review|sod-violations`) read-only, **tenant-scoped** (tenant admin sees own; Super Admin per-tenant), **the view itself audited** (§7), SoD dashboard re-evaluates active bindings for latent conflicts. Migrations `0001_admin.sql`+`0002_admin_governance.sql`+`0003_admin_breakglass_tenant.sql` (RLS tenant isolation, no DELETE grant anywhere = auditable history) applied to host PG (:55432). Tests (admin 67): pure `SegregationOfDutiesTests` (every §7 pair blocked + no-escalation) / `RoleAssignmentTests` / `AdminAuthzTests` / `GovernanceUnitTests` (linter + config validation + effective-dating) / `BreakGlassPolicyTests`; env-gated (`ADMIN_TEST_DB`) `AdminIntegrationTests` / `GovernanceIntegrationTests` / `BreakGlassIntegrationTests`, serialized via the `admin-db` collection. **Test-DB note:** admin integration tests run under the `hbmp` superuser conn (RLS not exercised there; RLS enforces under `hbmp_app`). **Next is Phase 9** (frontend — design system + role portals + flagship screens; also lands the shared TS/zod dashboard-contract mirror in `/libs/contracts`) or **Phase 10** (case + finance) per the master list; `8b`+`9` run continuously.

> **UPDATE 2026-07-25: Phase 8 (notifications + reporting) is now COMPLETE** (8.1 `35604d3`, 8.2+8.3 `4768621` — two new services — **476 tests green**; notification 27, reporting 20). **8.1 notification-service** (`notification` schema) — an event-driven **fan-out engine**, not business logic: idempotent domain-event fan-out (dedupe on event id + a unique `(source_event_id, recipient_user_id, channel)` index) mapping each event → role→channel targets via `RoutingTable` (config, not logic; approval decisions → requesting provider in-app+email + beneficiary, result-ready → ordering doctor, SLA-breach → reviewer+Director, etc.). **Two live channels** behind `INotificationChannel` (in-app = the persisted row the inbox reads; email over `IEmailProvider`, dev `LoggingEmailProvider`, failure→Failed→backoff retry via `RetryFailedEmailAsync`); **SMS + WhatsApp are future-channel STUBS** flagged OFF (`Notification:EnableSms/EnableWhatsApp`) — the dispatcher never calls a disabled channel, the stub returns `Skipped` with a "not yet enabled" log = **no live send**. **Bilingual AR/EN templates** (`notification_template`, versioned, both authored, Arabic RTL never machine-translated) rendered by `TemplateRenderer` (only `{token}` interpolation of min-necessary NON-clinical fields + a `ForbiddenKeys` clinical guard the dispatcher enforces). **Escalation** (`EscalationService.SweepAsync`) — an actionable notification (InfoRequested/SLA-breach/out-of-stock) unread past its window escalates to the captured next recipient (Director/supervisor), idempotent + once per (event,target). Delivery lifecycle `Queued→Sent→Delivered/Failed/Skipped`; **sensitive-context sends audited** (`Create`/`NOTIFY`). APIs: `GET /notifications` (own in-app inbox, row-filtered to caller), `GET /{id}/delivery`, `POST /{id}/read` (stops escalation), `POST /ingest` (the system fan-out seam, scope `notification:ingest`). `NotificationPolicies` v8.1 (`notification:read` any-role self-service + `notification:ingest` seam). Migration `0001_notification.sql` (+ seeded 24 AR/EN templates; DELETE granted — operational store, unlike append-only ledgers) applied to host PG. Tests: `TemplateAndRoutingTests`, `NotificationAuthzTests`, `NotificationDispatchTests` (env-gated `NOTIFICATION_TEST_DB`: fan-out in-app+email in locale/no-PHI, idempotent redelivery, email retry/backoff, escalation timer, SMS no-send, sensitive audit). **8.2 reporting-service** (`reporting` schema) — an event-projected **READ-MODEL**: never writes source domains, never re-derives PHI. `EventProjector` (idempotent, dedupe on event id) maps `Auth*`/`Encounter`/`Appointment{Booked,Attended,NoShow}`/`OrderLineConsumed`/`RxDispensed`/`DiagnosisRecorded`/`ServiceValued` → **aggregate, de-identified fact tables** (`authorization_fact` + `pending_authorization` snapshot, `encounter_fact`, `utilization_fact`, `code_count`, `financial_fact`). **Minimization enforced in the projection**: `code_count` holds coded counts only; **`financial_fact` has NO diagnosis column** (asserted against `information_schema`) and finance is default-denied clinical-coded reports in authz (finance ≠ diagnosis). Zone-split KPI APIs `/api/v1/reports/{approval-tat|pending-approvals|clinic-workload|utilization?dimension=|no-show|top-diagnoses|top-medications|rejected-requests|financial-summary}` (trailing-30-day default; **approval-tat p95** computed from the fact sample), an **async job handle** for >92-day ranges (`GET /reports/jobs/{id}`, NFR-006), and an **audited CSV export** (`GET /reports/{report}/export`, scope `reporting:export`). `POST /reports/projections` is the system seam (scope `reporting:project`). **8.3** — `GET /api/v1/dashboards/executive`, versioned contract `1.0`: **every `DashboardWidget` carries chart series AND a mandatory accessible `dataTable` (labelled columns/rows) AND complete bilingual AR/EN labels** (`IsAccessible`, CI-asserted); clinical + financial widgets appear only for a caller authorized for those zones. `ReportingPolicies` v8.2 (operational/clinical/financial read zones enforce finance ≠ diagnosis + project + export). Migration `0001_reporting.sql` applied to host PG. Tests: `ReportModelsTests`, `ReportingAuthzTests` (finance denied top-diagnoses, financial-fact has no diagnosis field), `ProjectionTests` (env-gated `REPORTING_TEST_DB`: TAT+p95+breach, pending snapshot drop-on-decision, idempotent, no-show/workload, financial from ServiceValued only, no-diagnosis-column in live schema), `DashboardContractTests` (every widget has dataTable+bilingual labels; zone gating). Both DB test suites serialized via `notification-db` / `reporting-db` collections. **NOTE (deferred wiring — same fanout-bus seam as phases 5–7):** the live-broker consumers that turn raw domain events (`Auth*`, `OrderLineAvailable`, `ResultReady`, `RxLineOutOfStock`, `Encounter*`, `Appointment*`, `ServiceValued`) into notification `POST /ingest` calls and reporting `POST /projections` calls land when the shared event bus arrives (dev uses per-service in-memory outbox; no fanout yet). The shared TS/zod dashboard-contract mirror in `/libs/contracts` lands with the Phase 9 frontend. **Next is Phase 8b** (admin & platform: user/role admin + SoD + access review, master-data/template/config admin, tenant/provider + break-glass governance) per `phase-8b-admin-platform.md`, or **Phase 9** (frontend) — both run continuously from R0/R1.

> **UPDATE 2026-07-25: Phase 7 (medical approvals) is now COMPLETE** (7.1 `15ba511`, 7.2 `a098550`, 7.3 `d13a4d5` — new **approvals-service**, `approvals` schema — **429 tests green**; approvals 54). The R4 authorization slice. **Canonical lifecycle** (23 §5) `Draft→Submitted→UnderReview→(Approved|PartiallyApproved|Rejected|InfoRequested)` +`Overridden`/`EmergencyApproved`/`Expired` in `AuthorizationWorkflow` (illegal → 409). The aggregate `Status` is the projection of the latest **APPEND-ONLY** `authorization_decision` row (immutable: deny-mutation trigger + revoked UPDATE/DELETE grant on `hbmp_app`; corrections are new rows). **7.1** — ingestion seam `POST /authorizations` (scope `auth:ingest`, Idempotency-Key) creates Submitted requests from routed order-lines/prescriptions (the target for the phase-4 routing saga / the `OrderPendingApproval`|`RxSubmitted` consumer); worklist `GET /authorizations` (min-necessary projection, NO clinical payload — filter status/priority/sla/unassigned, sort by sla_due_at) + `POST /{id}/assign` (Submitted→UnderReview, xmin optimistic-concurrency → one wins/409, starts priority-based SLA timer, emits `AuthUnderReview`); **review view `GET /{id}/review`** is the ONLY clinical-context endpoint (scope `auth:review` + role medical_approval/medical_director, ABAC purpose **PUR**), a field-scoped DTO from `IClinicalContextProvider` (fail-closed HTTP to emr oversight projection) with a **PHI-read audit** naming the field classes; finance/reception default-denied. **7.2** — decisions (scope `auth:decide`, Idempotency-Key): approve / partially-approve (approved_scope must be a NON-EMPTY STRICT SUBSET → 422) / reject (**rationale MANDATORY → 422**) / request-info / resupply — each writes the append-only decision row + status + canonical event (`AuthApproved`/`AuthPartiallyApproved`/`AuthRejected`/`AuthInfoRequested`/`AuthInfoSupplied`) in ONE tx, captures TAT (decided−submitted) + sla_breached; downstream event carries `sourceRef`+`approvedScope`+`releasesDownstream` so orders/pharmacy gate consumers release approved lines/leave rest gated. **Concurrency**: shared `Decide` helper updates PARENT FIRST (exclusive lock + xmin check) THEN inserts the append-only child — avoids the FK-share→exclusive deadlock; two reviewers → one wins/409. **7.3** — break-glass: `emergency-approve` (Submitted→EmergencyApproved, scope `auth:emergency`, **Director only**) + `override` (Rejected→Overridden, `auth:override`, Director only) reuse `Decide(breakGlass:true)`; `manual` (`auth:manual`) creates source=Manual (no provider) + decides in one tx; all require **non-blank justification (422)**, write a High-severity flagged break-glass audit + set `retrospective_review_required`; `GET /retrospective-queue` + `GET /tat-summary` (count/avg/**p95**/breach via `TatReporting` percentile_cont) for phase-8 reporting. **`libs/authz/ApprovalsPolicies` v7.0** (tenant-scoped oversight; review/decide/break-glass Sensitive→audited allow; Director-only emergency/override). **Migrations `0001_approvals.sql` + `0002_breakglass.sql` applied to host PG (:55432).** Tests: `AuthorizationWorkflowTests`, `DecisionRulesTests`, `ApprovalsAuthzTests` (min-necessary: finance/reception denied + audited), `ApprovalsIntegrationTests` (append-only ledger rejects UPDATE/DELETE), `DecisionConcurrencyTests` (real 8-way PG, one wins/one row), `BreakGlassTests` (justification CHECK, retrospective queue, TAT aggregation) — DB classes serialized via the `approvals-db` collection. **NOTE:** the cross-service consumer that turns `OrderPendingApproval`/`RxSubmitted`/`RxSubstitutionRoutedToApproval` into `POST /authorizations` ingestion, and the orders/pharmacy consumers of the `Auth*` release events, are the wiring seam to complete when the shared event bus lands (dev uses per-service in-memory outbox; no fanout yet). **Next is Phase 8** (notifications + reporting) per `phase-8-notifications-reporting.md` — it consumes the `Auth*` events + `RxLineOutOfStock` and reads `tat-summary`.

> **UPDATE 2026-07-25: Phase 6 (pharmacy dispensing) is now COMPLETE** (6.1 dispensable search, 6.2 partial dispensing batch/expiry, 6.3 substitution + out-of-stock — all in **pharmacy-service**, one commit — **375 tests green**; pharmacy 33→53). This is the **R3 medication slice**, the pharmacist analogue of Phase 5 applied to `dispense_event`. **6.1** — `GET /prescriptions/search?rxNo=|patientIdentifier=|policyNo=|passport=|memberNo=` (scope `pharmacy:read`, US-050): returns ONLY **dispensable** prescriptions (status Approved/PartiallyDispensed, within validity window, ≥1 line remaining), projected (`DispensableRxView`) to dispensing-relevant fields only (drug/dose/route/frequency, **remaining qty**, patient id) — never diagnoses/notes; policy/passport/member resolve to a beneficiary via patient-service (`IBeneficiaryResolver`, fail-safe). `GET /prescriptions/{id}/dispensing` enforces the **reject rule** (23 §3) → audited 409 with reason for an Expired/Cancelled/Rejected/fully-Dispensed Rx. Provider-scoped by `DispensingGate` over `ProviderPolicies.QueueRead` (pharmacist), NO treating gate (a pharmacist doesn't treat; an Rx is dispensable network-wide); PHI reads audited. **6.2 THE INVARIANT** — `POST /prescriptions/{rxId}/lines/{lineId}/dispense` (scope `pharmacy:dispense`, **Idempotency-Key required**): the three-mechanism guard lives in **`DispenseExecutor`** (Infrastructure), the medication twin of `ConsumeExecutor`, shared by the endpoint AND the concurrency tests — (1) append-only `dispense_event` insert keyed by **UNIQUE `idempotency_key`** (the raw header key; one line per call); (2) **optimistic concurrency on the prescription line's `xmin`** (EF `IsRowVersion` — newly mapped on `prescription_line`) so exactly one of N racers wins (losers → `DbUpdateConcurrencyException` → 409); (3) **idempotent replay** — same key returns the prior dispense_event, no new row/state. DB `CHECK (0≤dispensed≤prescribed)` (migration 0001) is the backstop. Rx status recompute (`Dispensing.RecomputePrescriptionStatus`) via `ExecuteUpdate` OUTSIDE the line's optimistic guard. Body `{quantity, batchNo, expiryDate, substitutedDrugId?, substitutionReason?}`; **expired lot** (`expiryDate ≤ today`) → 422, expired/cancelled/rejected/dispensed Rx → 409, no-reuse on a fully-dispensed line → 409. Partial → line/Rx PartiallyDispensed (remainder stays available); all → Dispensed. `RxLinesDispensed` (+`RxDispensed`) via outbox atomically. **6.3** — **substitution** honored only when `substitutedDrugId` is a **policy-approved alternative** (`IFormularyService` → masterdata approved-alternatives; a clearly-marked swappable PBM/formulary stand-in); off-list → NOT dispensed, emits `RxSubstitutionRoutedToApproval` + 409; approved substitution records `substituted_drug_id`+reason on the dispense_event + audit. **Out-of-stock** — `POST /…/lines/{lineId}/out-of-stock` flags WITHOUT consuming (accumulator untouched, quantity stays available), emits `RxLineOutOfStock` to notify prescriber+beneficiary (phase 8) + audit. `PharmacyPolicies` version bumped to 6.0 (+`rx:dispense` on `prescription_line`, pharmacist + provider-ownership). **Migration `0002_dispense.sql` (dispense_event, UNIQUE idempotency_key + batch/expiry + substitution cols) applied to host PG (:55432).** Tests: `DispensingRulesTests` (pure rules + substitution gate), `DispensingAuthzTests` (own-vs-other pharmacy, dispense, doctor-cannot, **min-necessary: pharmacy bundle grants a pharmacist no order-result read**), `DispenseConcurrencyTests` (env-gated `PHARMACY_TEST_DB`, REAL 8-way parallel PG proof: exactly one wins, one dispense_event row, replay adds none, partial→Dispensed, no-reuse, expired-lot no-state-change, substitution+batch/expiry pins). DB test classes serialized via the `pharmacy-db` xUnit collection. **Next is Phase 7** (medical approvals: `approvals-service` worklist + decisions + break-glass + SLA/TAT) which consumes `OrderPendingApproval`/`RxSubmitted` (+ now `RxSubstitutionRoutedToApproval`), per `phase-7-approvals.md`.

> **UPDATE 2026-07-25: Phase 5 (lab & imaging fulfillment) is now COMPLETE** (5.1 provider queue+search, 5.2 atomic idempotent consume, 5.3 result upload+routing — all in **orders-service**, one commit — **350 tests green**; orders 25→45). This is **R3, the fulfillment slice**: a lab/imaging provider gets an authorized queue, performs the platform's most important operation — the **atomic, idempotent, duplicate-proof consume** — and uploads results. **5.1** — `GET /investigation-orders/queue` + `/search?patientIdentifier=|orderNo=` (scope `orders:read`): returns ONLY **available** lines (order Active/PartiallyUsed, line not used/cancelled) of the caller's **capability** (`ProviderCapability`: lab_tech→Lab, imaging_tech→Imaging), projected to the minimum a fulfiller needs (`QueueItemResponse`: patient id + line code + **quantity remaining**), never diagnoses/notes/pharmacy. Provider-scoped by the shared engine's provider-ownership rule (`FulfillmentGate` over `ProviderPolicies.QueueRead`); PHI reads audited. **5.2 THE INVARIANT** — `POST /investigation-orders/{orderId}/consume` (scope `orders:consume`, **Idempotency-Key required**): the three-mechanism guard lives in **`ConsumeExecutor`** (Infrastructure) so the endpoint AND the concurrency tests share it — (1) append-only `order_fulfillment` insert per line keyed by **UNIQUE `idempotency_key`** (`<key>::<lineId>`); (2) **optimistic concurrency on the line's `xmin`** (EF `IsRowVersion`) — the consume UPDATE lands only if the line hasn't moved, so exactly one of N racers wins (losers → `DbUpdateConcurrencyException` → 409); (3) **idempotent replay** — same key returns the prior fulfillment, no new row/state. DB `CHECK (0≤consumed≤ordered)` is the backstop. Order status recompute (`OrderConsume.RecomputeOrderStatus`) is done via `ExecuteUpdate` OUTSIDE the line's optimistic guard so concurrent consumes of DIFFERENT lines don't falsely collide. `OrderLinesConsumed` (+`OrderCompleted`) via outbox atomically. No-reuse: a used line → 409 `already-used`; a fully-consumed single-line order → 409 `order-not-consumable` (both correct 409s). **5.3** — `POST /…/lines/{lineId}/result` (scope `orders:consume`): result value(s) + optional report → report stored in **document-service** (`HttpReportDocumentClient`, scanned CMK blob), ref pinned on the consumed line's fulfillment row; a result may only attach to a line THIS provider consumed; emits `OrderResultUploaded` routed to ordering doctor (+approvals if gated). `GET /…/lines/{lineId}/result` (scope `orders:read`) is **min-necessary**: ordering doctor (treating) OR approval team only (`OrdersPolicies.ReadResult` — doctor rule on `investigation_order` w/ treating, oversight rule on `order_result` tenant-only), else 403+audit. `OrdersPolicies` version bumped to 5.0 (+`orders:consume-line`, `orders:read-result`). **Migration `0002_fulfillment.sql` (order_fulfillment) applied to host PG (:55432).** Tests: `OrderConsumeTests` (pure rules), `FulfillmentAuthzTests` (queue own-vs-other, consume, doctor-cannot, result read approval-vs-unrelated), `OrderConsumeConcurrencyTests` (env-gated `ORDERS_TEST_DB`, REAL 8-way parallel PG proof: exactly one wins, one fulfillment row, replay adds none, partial→Completed, no-reuse, result-ref pins). DB test classes serialized via the `orders-db` xUnit collection (DisableParallelization) so the many-connection race doesn't collide with other datastore tests. **Next is Phase 6** (pharmacy dispensing: 6.1 dispensable search, 6.2 partial dispensing batch/expiry — create `dispense_event` per DD §8.3 with the SAME consume/no-reuse pattern, 6.3 substitution + out-of-stock) per `phase-6-pharmacy.md`, then Phase 7 (approvals) consumes `OrderPendingApproval`/`RxSubmitted`.

> **UPDATE 2026-07-25: Phase 4 (clinical EMR + investigation orders + e-prescriptions) is now COMPLETE** (4.1 emr clinical docs `8de1c25`, 4.2 orders-service `a4d65bc`, 4.3 pharmacy-service `ed42f21` — **330 tests green**). The doctor-facing R2 half of the platform. **Treating-relationship (US-030) is the spine**: a row-level check feeds the shared authorization engine's `treating-relationship` ABAC condition; the truth lives in **emr-service** (encounters) and is exposed as a min-necessary boolean probe `GET /api/v1/treating-relationship?beneficiaryId=` that orders/pharmacy call (caller token forwarded) so all three enforce the SAME rule. Non-treating clinician → 403 + audited attempted-PHI-access. **4.1 emr-service** (fills the Phase-2.3 stub): SOAP/Progress/Nursing notes (**sign-lock** → immutable, corrections via **addendum** only, unsigned editable by author), ICD-10 diagnoses (validated vs masterdata), range-validated vitals (+optional LOINC), allergies + medication history; all codes validated against masterdata **fail-closed**; soft-delete only; **FHIR R4 read projection** (Encounter/Condition/Observation/AllergyIntolerance/MedicationStatement). New `EmrPolicies` bundle: doctor/nurse read+write need treating; medical-approval team reads via distinct `emr:read-oversight` action (no treating); reception/labs/pharmacy default-denied clinical. Migration `0005_clinical.sql`. **4.2 orders-service** (NEW, `orders` schema, :port TBD): `POST /investigation-orders` — treating-gated, every CPT/LOINC/LOCAL line validated vs masterdata (422 on unknown), config-driven `OrderRoutingPolicy` routes high-cost/gated → `Requested→PendingApproval` (`OrderPendingApproval`) else auto-activates → `Active` (`OrderActivated`); `OrderCreated`+routed event via **outbox** in the same tx; idempotent create; cancel transition-guarded; `order_line` carries the consume accumulator `CHECK (0≤consumed≤ordered)` for phase-5. `OrdersPolicies` = ProviderPolicies (phase-5 provider reads) + order-create. Migration `0001_orders.sql`. **4.3 pharmacy-service** (NEW, `pharmacy` schema): `POST /prescriptions` — treating-gated, drug_id validated vs masterdata, **advisory (non-blocking) interaction + allergy alerts** (masterdata `/drug-interactions/check-by-ids` + `/allergies/check-by-ids`; allergies pulled from emr `GET /beneficiaries/{id}/allergies`) recorded with acknowledgement; **Draft→Submitted** then config-driven `RxRoutingPolicy` keeps expensive/gated Submitted (dispensable only once Approved) else auto-approves; `RxCreated`/`RxSubmitted`(/`RxApproved`) via outbox; `prescription_line` accumulator `CHECK (0≤dispensed≤prescribed)` for phase-6. `POST /referrals` → Requested + `ReferralRequested` (US-034). `PharmacyPolicies`; pharmacist cannot prescribe. Migration `0001_pharmacy.sql`. masterdata gained by-id existence + by-id screening endpoints. **emr/orders/pharmacy migrations applied to host PG (:55432).** **Next is Phase 5** (lab/imaging fulfillment: provider order queue + **atomic idempotent consume** + result upload) and **Phase 6** (pharmacy dispensing) — both consume what Phase 4 produced; they can run in parallel, per `phase-5-*.md` / `phase-6-*.md`.

> **UPDATE 2026-07-25: Phase 3 (appointments) is now COMPLETE** (3.1 booking `a39b647`, 3.2 reschedule/cancel/no-show `19c0669`, 3.3 queue + reminders `8717b70` — **242 tests green**; emr-service 52). Scheduling was added to **emr-service** (`emr` schema), not a new service. **3.1** — `provider_availability` → materialized `appointment_slot`s; `POST /appointments` books with a **hard no-double-book** guarantee: the booking tx locks the slot `FOR UPDATE` and the `ux_appointment_active_slot` **partial-unique index** (WHERE status IN Booked/CheckedIn) is the backstop — the losing racer gets 23505→409. Proven by a live 12-parallel-booker concurrency test. Persisted `appointment.status` is EXACTLY {Booked,CheckedIn,Completed,NoShow,Cancelled}; Requested/Waitlisted live on `waitlist_entry`. Referral bookings link REF-* (emit `ReferralScheduled`); follow-ups link an encounter; no-slot → next-slots or a `202` waitlist. **3.2** — `/reschedule` (atomic release-old + acquire-new in ONE tx), `/cancel`, `/no-show` (guarded: window passed AND still Booked; sets `no_show` reporting flag; frees slot for backfill; repeat no-shows ≥3 → `BeneficiaryNoShowThresholdReached` for Case Manager). Slot "release" is implicit (the partial-unique index only counts active holds). All mutations honor `Idempotency-Key` (→ `emr.processed_request` ledger) + `If-Match` (xmin ETag from GET → 412); illegal moves are an audited 409 `TransitionDenied`. **3.3** — reception walk-in queue per (location,provider,doctor): `/check-in` (Booked→CheckedIn + enqueue min-necessary ticket), `GET /queues` (position/memberNo/name/type/wait only — no EMR, reflection-guarded), `call-next`/`requeue`/`remove`/`complete`; cancel/no-show auto-remove tickets. **Reminders hook**: `IReminderChannel` + `ReminderDispatcher` (preferred-channel selection, in-app fallback) — in-app live (outbox→`notification.events`), SMS/WhatsApp are stubs; a Booked reminder fires on booking + `POST /appointments/reminders/run`. emr migrations `0002`–`0004` applied to host PG (:55432). **Next is Phase 4** (clinical EMR: SOAP/orders/e-prescriptions — fills the rest of the emr stub) per `phase-4-*.md`. Everything below is historical reference.

> **UPDATE 2026-07-23: Phase 2b (provider network & isolation) is now COMPLETE** (2b.1 provider-service `f54e08b`, 2b.2 onboarding `7f2bd27`, 2b.3 isolation `78bb6da` — **201 tests green**). New `provider-service` (:8097) owning the `provider` schema — the R2 fulfillment backbone for phases 5/6. Providers/locations/contracts/service-lines/credentials CRUD + `GET /capabilities` (routable only from an Active provider under an in-effect contract; `agreed_price` masked without `provider:finance`; CPT validated vs masterdata); contract effective-range GiST exclusion + primary-location partial-unique. **Network Team onboarding** state machine (`OnboardingWorkflow`): guarded `/activate` (blocked without primary location + valid mandatory credentials + active contract), `/suspend` + dual-controlled `/terminate` (revoke all provider users, stop routing), provider-user provisioning with **SoD** (`ProviderUserRules`: no self-elevation, no clinical roles), credential-expiry reminder sweep. **Provider isolation in depth**: token provider_id check, reusable `ProviderPolicies` ABAC bundle (imported by orders/pharmacy later), **PostgreSQL RLS** (`0003` ENABLE+FORCE + session-GUC `RlsConnectionInterceptor`), min-necessary `ProviderBoundaryPatient`, provider-scoped metrics. **KEY OPS FINDING:** RLS only bites under a **non-superuser `NOBYPASSRLS`** role — the app must connect as `hbmp_app` (`0004_app_role.sql`); the default `hbmp` role is superuser and silently bypasses RLS (proven both ways). Provider migrations `0001`–`0004` already applied to host PG (:55432); `hbmp_app` role created (password set out of band — OpenBao `secret/hbmp/db/app`; local dev via gitignored `infra/compose/.env`, never committed). **Next is Phase 3** (appointments) per `phase-3-*.md`. Everything below is historical reference.
>
> **UPDATE 2026-07-23: Phase 2 is now COMPLETE** (2.1 eligibility-service `df91758`, 2.2 reception search `4d930ce`, 2.3 emr visit-gating stub `15ea73e` — all committed, **169 tests green**). New services: `eligibility` (:8095 — decision engine {Eligible|Ineligible|NeedsAuthorization} from member status + coverage + remaining limits; Valkey cache keyed (beneficiaryId,category) TTL 15m + in-memory fallback; `EventConsumer`/`ProjectionUpdater` consume `patient.events`+`policy.events` idempotently and invalidate; min-necessary reception search `GET /reception/search` with an authz reflection test proving no EMR leak; `GET /eligibility/members/{id}/status` for the gate) and `emr` (:8096 — `POST /encounters` status-gated: Active→ENC-* shell + clinician queue + EncounterStarted/ApptCheckedIn, else 422 guidance, idempotent). Enriched patient `BeneficiaryRegistered`/`Activated` + policy `CoverageChanged` payloads with min-necessary fields; **turned on the outbox relay** in patient/policy/eligibility/emr (also fixes audit delivery in dev) with a **lazy/resilient** `RabbitMqEventPublisher` (unreachable broker degrades, no startup crash). **Before a live demo, apply the two new migrations** to host Postgres (:55432): `services/eligibility/Infrastructure/Migrations/0001_eligibility.sql` and `services/emr/Infrastructure/Migrations/0001_emr.sql`. **Next is Phase 2b** (provider network + isolation) per `phase-2b-provider-network.md`. The Phase-1 sub-sections below are historical reference.
>
> **UPDATE 2026-07-23 (earlier): Phase 1 COMPLETE** (1.1 patient, 1.2 policy, 1.3 document, 1.4 registration→activation — 128 tests green, activation proven live: register→approve→MRS-M-2026-000001 + BeneficiaryActivated). The 1.3/1.4 sub-sections below are DONE; keep them for reference.

### 9a. FINISH Phase 1.3 — document-service (started, uncommitted)
Prompt: `HBMP-Design/claude-code-prompts/phase-1-registration-policy.md` §1.3 (US-002). Design: `15-database-erd.md §12`.
- **Infrastructure:** `ClamAvScanner : IMalwareScanner` (TCP to ClamAV `clamd` on `clamav:3310`, INSTREAM; fail-closed), `MinioBlobStore : IBlobStore` (AWSSDK.S3 → MinIO, private bucket `beneficiary-documents`), `DependencyInjection.AddDocumentInfrastructure` (DbContext + scanner + blob store).
- **Api/Program.cs:** `POST /api/v1/beneficiaries/{id}/documents` multipart upload → `DocumentUploadService.UploadAsync` → 201 (stored, versioned, checksum+uploader) / 400 (rejected type/size, reason) / 422 (quarantined malware). Audit every upload/attach/reject; scope `document:write`. `GET` list (metadata only, min-necessary).
- **Tests:** validation matrix (oversize/bad-type rejected), malware-positive → Quarantined (fake scanner), clean → Stored + version + checksum. Use fakes for `IMalwareScanner`/`IBlobStore` (no Docker needed).
- Dockerfile + compose entry (:8094) + README + BUILD-STATUS. Commit `feat(document): ... (phase 1.3)`.

### 9b. Phase 1.4 — registration workflow + activation (US-003/US-004)
In `patient-service`. Model a `registration` aggregate (status Pending|InfoRequested|Rejected|Active) distinct from beneficiary.status. Endpoints: `POST /registrations` (Idempotency-Key), PATCH step data (If-Match→412), `POST /registrations/{id}/submit`, `POST /registrations/{id}/decision {Approve|RequestInfo|Reject, notes}`. **Approve (transactional):** guard = documents verified AND coverage bound → set beneficiary.status Active, issue `MRS-M-YYYY-NNNNNN` via `MemberNoIssuer`, write MemberNo identifier, **emit `BeneficiaryActivated`** (phase-2 eligibility consumes it). Reject → reason mandatory. Also expose the lifecycle transitions (suspend/expire/block/reactivate — `BeneficiaryLifecycle` already exists) with mandatory reasons + PATCH If-Match. Tests: full register→submit→approve→activate path asserts MemberNo + event; illegal-transition 409; idempotent replay. Commit → **Phase 1 COMPLETE**.

### 9c. Then continue in dependency order (see BUILD-STATUS + master list)
`Phase 2` (eligibility engine + reception min-necessary search + visit gating) → `2b` (provider network + isolation) → `3` (appointments) → `4` (EMR/orders/prescriptions) → `5`/`6` (lab-imaging consume / pharmacy dispense — the atomic-idempotent invariant) → `7` (approvals) → `8` (notify+reporting) → `8b` (admin) → `9` (frontend) → `10` (case+finance) → **`10b` (claims management — NEW, see below)** → `11` (hardening) → `12` (migration/go-live) → `13` (interop). `8b`+`9` run alongside.

> **DESIGN UPDATE 2026-07-25 — NEW Phase 10b (Claims Management).** The design set grew a 36th deliverable, `HBMP-Design/36-claims-management.md`, and a new prompt `phase-10b-claims-management.md` (9 sub-prompts, 10b.1–10b.9): `claims-service` + auto-derived claims (no double-billing) · batching (single-open-batch) · automated 9-step pre-adjudication (all reasons) · Claims Officer line-level decisions (SoD + dual control + min-necessary) · provider-submitted claims + document matching · beneficiary reimbursement + OCR (assistive only) · reconciliation + **append-only adjustments** · **settlement advice + exports (NO payment execution — Finance pays outside the platform)** · appeals + claims KPIs. **Dependencies:** it sits after 7 and alongside/after 10 — it needs `order_fulfillment`/`dispense_event` (5/6) as the payable anchors, provider contracts/tariffs (2b) for pricing, authorization linkage (7) for gated lines, and `document-service` (1) for invoices/receipts/settlement advice. Release R6/post-v1. Design docs updated with claims content: 07 (functional reqs), 10 (role matrix — Claims Officer), 11 (permission matrix — claims min-necessary), 16 (service architecture — `claims-service`), 22 (data dictionary — claim/claim_line/batch/adjustment/settlement tables), 23 (state machines — claim + batch lifecycles), 31 (backlog EPIC-13), 32 (user stories US-CLM-*). Phase-9 frontend prompt also gained a **v1.1 visual-refinement block** (0B §10b — layered canvas, elevation physics, KPI-card treatment) that the design system must match. Read `36-claims-management.md` first when Phase 10b begins. All 10b BUILD-STATUS rows are ☐ (not started).

## 10. Non-negotiable invariants (every phase)
1. Order/prescription consume-dispense is **atomic, idempotent, duplicate-proof** (unique index + optimistic version + Idempotency-Key). 2. **Minimum-necessary** row+field, in code (`RowScope`/`FieldProjector`). 3. **Immutable hash-chained audit** on every mutation/decision/consume/dispense/export/PHI-read. 4. **WCAG 2.2 AA + Arabic RTL** on every UI. 5. **Soft-delete + history**, never hard delete clinical/benefit data. 6. Tokens validated at gateway AND service; MFA for protected scopes.
