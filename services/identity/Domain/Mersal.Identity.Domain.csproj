<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup><RootNamespace>Mersal.Identity.Domain</RootNamespace></PropertyGroup>
  <ItemGroup>
    <!-- IdentityUser<TKey> / IdentityRole<TKey> base types (no EF dependency in the domain layer). -->
    <PackageReference Include="Microsoft.Extensions.Identity.Stores" />
  </ItemGroup>
</Project>
