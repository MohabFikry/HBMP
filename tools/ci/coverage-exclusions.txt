# Coverage exclusions — repo-relative path PREFIXES, one per line, each with a one-line reason above it.
#
# ------------------------------------------------------------------------------------------------
# THIS FILE IS DELIBERATELY EMPTY. That is the finding, not an oversight.
# ------------------------------------------------------------------------------------------------
#
# Phase 24 Gate 0.1 expected the ~164,000 unexplained lines to be generated code — scaffolded OpenAPI
# clients, EF *.Designer.cs, generated DTOs — and instructed us to confirm or refute that with a report
# rather than assume it. It is REFUTED. The repo contains:
#
#   * zero C# files carrying an <auto-generated> header  (grep -rl "<auto-generated" --include=*.cs)
#   * zero *.Designer.cs files
#   * docs/api/*.json — OpenAPI specs, which are JSON data and never compiled, so never in coverage
#
# The gap was never generated code. It was the gate summing one cobertura report PER TEST ASSEMBLY
# without deduplicating, so every shared library was counted once per test project that loaded it (161
# files appeared in more than one report; one appeared in 27). The proof is arithmetic: the old gate
# reported 22,625 "domain lines" while the entire Domain layer holds 12,488 PHYSICAL source lines,
# blanks and braces included. A count of unique coverable lines cannot exceed the file's line count.
#
# The fix therefore belongs in the measurement (tools/ci/coverage-report.py merges by union), not here.
# Nothing is excluded, so nothing is hidden.
#
# ------------------------------------------------------------------------------------------------
# IF YOU ADD AN ENTRY, READ THIS FIRST
# ------------------------------------------------------------------------------------------------
# Allowed:   files with an auto-generated header; scaffolded API clients; EF *.Designer.cs; generated
#            contract types. Anything a tool writes and a human never edits.
# FORBIDDEN: hand-written code that is merely inconvenient to test. A whole service. ANYTHING under a
#            Domain/ directory. If you want to exclude domain code, that wish IS the finding — write the
#            test instead.
#
# Adding a line here is treated exactly like lowering a floor: tools/ci/check-coverage-exclusions.py
# fails the build unless the commit that grows this file cites an ADR (ADR-\d{4}) that exists and names
# the path. Gaming the denominator must be as hard as moving the bar.
#
# Format: a bare repo-relative prefix per line, e.g.
#   services/foo/Infrastructure/Generated/     # reason, in a comment on the line above or beside it
