---
name: project_mersal_phase11
description: "Mersal HBMP Phase 11 (Production hardening & NFR assurance) COMPLETE 2026-07-26 — perf harness + security gates + observability/DR"
metadata:
  node_type: memory
  type: project
---

Mersal HBMP **Phase 11 COMPLETE** on `master` 2026-07-26 (see [[project_mersal_hbmp]], [[project_mersal_phase15]]). Cross-cutting NFR-assurance phase — **evidence/harness/config, not new features**. 3 commits (11.1 `cac6e7a`, 11.2 `c9d20c7`, 11.3 `e5581c1`) + docs. Full solution builds **0-warning** after fleet metrics change; callcentre 30 / emr 88 / audit 3 green.

**Integrity framing (important):** this phase's gates are defined against a live prod-like **staging** cluster (1M-row load, external pen-test, second-site DR failover) which this dev repo lacks. So I built the **real versioned deliverables** the phase specifies (runnable harnesses, dashboards/alerts-as-code, CI gates, threat model, runbooks, sign-off checklists) and marked staging-only numbers **PENDING** rather than inventing "measured" evidence. Split is explicit everywhere: ✅ enforced-now (code/CI/tests) vs 🟡 operational-gate-on-target-infra.

- **11.1 perf** `cac6e7a`: `/perf` k6 suite (01-eligibility NFR-002/004, 02-consume NFR-003/073 incl. single-line **race proving zero double-commit**, 03-worklists NFR-001/004, 04-dashboards NFR-006, 05-mixed-soak 1h + NFR-014 event-bus). Each script encodes the NFR target as a k6 **threshold** → miss = non-zero exit = CI gate. `perf/data-gen/generate.mjs` deterministic (seedable) synthetic volume gen sized to NFR-012 (SYN-* keys, no PHI; **verified runnable + deterministic**). `perf/run.sh` (SMOKE profile) + `.github/workflows/perf-ci.yml` (nightly). `docs/PERFORMANCE-BASELINE.md` (measured cols = `PENDING (staging)` — NOT fabricated). `docs/adr/0009` = RLS-first covering indexes + Valkey eligibility-snapshot invalidation-on-policy-change (TTL backstop only).
- **11.2 security** `c9d20c7`: `docs/security/threat-model-stride.md` (STRIDE: edge ModSecurity/CRS→Kong→k3s + per-context + data tier). `docs/security/owasp-api-top10-checklist.md` (API1–10 → Mersal controls; BOLA row+field sweep per role×resource, proven by existing authz suites). `.github/workflows/security-ci.yml` = gitleaks + Trivy fs SCA/config/SAST + Trivy image + CodeQL C# + ZAP DAST, **exit-code 1 on Critical/High** (blocks release). `.gitleaks.toml` (env-var `${VAR}` allowlist — **repo verified: zero hardcoded secrets**, all injected from gitignored `.env`; prod target = OpenBao/Vault+SOPS). `docs/compliance/security-sign-off.md` = each pre-prod gate → ✅/🟡 (go-live gate phase-12 consumes this).
- **11.3 reliability/observability/DR** `e5581c1`: **REAL CODE** — added `.WithMetrics(m => m.AddAspNetCoreInstrumentation().AddRuntimeInstrumentation().AddPrometheusExporter())` + `app.MapPrometheusScrapingEndpoint()` to **all 17 services'** Program.cs (needs `using OpenTelemetry.Metrics;`; new pkgs `OpenTelemetry.Exporter.Prometheus.AspNetCore` 1.10.0-beta.1 + `OpenTelemetry.Instrumentation.Runtime` in Directory.Packages.props + each Api.csproj). Golden-signal metric = `http_server_request_duration_seconds*`. prometheus.yml `services` scrape job (all 17) + Alertmanager compose service; Grafana dashboards-as-code (`golden-signals.json` + `business-kpis.json`) provisioned; alert rules (`rules/slo-and-ops-alerts.yml`): SLO burn, latency/saturation, event-bus-backlog, **failed-consume**, **approvals-SLA**, **auth-anomaly**, **audit-chain-stalled** → Alertmanager (page/ticket/security, env webhooks). 7 runbooks in `docs/runbooks/` (dr-failover, backup-restore, deploy-rollback, approvals-backlog, failed-consume-replay, auth-anomaly, event-bus-backlog) + `infra/observability/README.md`. **`infra/dr/restore-rehearsal.sh` EXECUTED** vs compose PG :55432: pg_dump→restore-to-scratch→**exact per-table count(\*) reconciliation** (n_live_tup is a stale estimate — must use exact count) + **audit hash-chain linkage check** (prev_hash continuity per partition_key/seq). Result: **138 tables / 18 schemas reconciled exactly, 0 chain-linkage violations = PASS** (`docs/runbooks/dr-drill-report.md`). Honest caveat: dev audit table empty ⇒ chain traversal is structural pass; full second-site failover pending target k3s.

GOTCHAs: metrics `AddAspNetCoreInstrumentation` needs `using OpenTelemetry.Metrics;` (distinct from Trace ns) else CS1929. `pg_stat_user_tables.n_live_tup` diverges after a fresh restore (autovacuum estimate) → reconcile with exact `count(*)`. audit chain groups by `partition_key` ordered by `seq`, `prev_hash`→`record_hash`. **Next: Phase 12** (data migration / release mgmt / go-live — consumes the 11.2 security sign-off + 11.3 DR drill report), then 13 (interoperability/FHIR); **10b Claims still pending** (R6/post-v1).
