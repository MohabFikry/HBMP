---
name: project_mersal_phase15
description: "Mersal HBMP Phase 15 (Call Centre portal) COMPLETE be→ui 2026-07-26 — new callcentre-service + verify-before-disclose"
metadata:
  node_type: memory
  type: project
---

Mersal HBMP **Phase 15 COMPLETE** on `master` 2026-07-26, backend→UI, 6 slices (see [[project_mersal_hbmp]], [[project_mersal_phase14]]). New **callcentre-service** (schema `callcentre`, **MemberScoped / all branches** per design 37 §3). Full solution 0-warning; 30 callcentre tests + web suite 33 green (DB-gated vs live PG :55432, env `CALLCENTRE_TEST_DB`); migrations 0001+0002 applied.

THE DEFINING CONTROL = **verify before you disclose**: no member detail until a Passed `caller_verification` for THIS interaction + THIS beneficiary (`VerificationService.IsVerifiedAsync`, valid only while interaction Open, expires on close). **Privacy rule**: `caller_verification.verified_identifiers` jsonb stores only WHICH identifier TYPES were confirmed — NEVER the values (they live in patient-service). **No clinical data ever**: the 360 (`Member360`) has no field that can hold a diagnosis/result/rx/note — proven by reflection + serialized-JSON authz test.

- 15.1 `bdcd50a` callcentre-service foundation: `call_interaction` + `caller_verification` (migration 0001), `VerificationPolicy` (≥2 distinct KNOWN types for Pass; unknown "types" dropped so a recited VALUE can't pose as a type), CallRef `CALL-YYYY-NNNNNN`. Endpoints open/patch/close/list + POST verification (Fail persisted AND audited; <2 types→422; Pass binds interaction→beneficiary). `libs/authz/CallCentrePolicies` (call_center + call_center_supervisor scopes callcentre:interaction/verify/read/act; read/act Sensitive; NO branch condition). ADR docs/adr/0008.
- 15.2 `324bfc7` member search + min-necessary 360: `IMemberDirectory` seam (`HttpMemberDirectory` fail-soft, bearer-forwarded to eligibility reception-search/emr/patient/pharmacy). GET /call-centre/search (thin: name+id+challenge TYPES only, audited) + GET /call-centre/members/{id}/summary?interactionId= (403+audit until verified; PHI-read audited). Appointments across ALL branches.
- 15.3 `533467a` book/reschedule/cancel: `IAppointmentGateway` (`HttpAppointmentGateway` delegates to emr, forwards bearer+**Idempotency-Key**+**If-Match** verbatim → no-double-book preserved). AppointmentLink (interaction↔appt linkage, migration 0002). Each action verification-gated (403); cancel reason MANDATORY (422); referralRef→appointmentType=Referral; success queues `AppointmentConfirmationRequested` (clinical-free).
- 15.4 `eea308c` contacts: pure `ContactValidation` (phone/email/address; invalid→422 pre-delegation); `IContactGateway`→patient-service (owns one-primary+history); audit BEFORE/AFTER minimized to KIND. Referral→booking conversion proven.
- 15.5 `66feba8` web Call Centre portal (role `call_center`, base `/call-centre`): call-shaped `CallCentreWorkspace` (start→search→verify checklist→360→act→wrap-up; no detail in DOM pre-verify; aria-live announces; four-cue lock chip; 409 "slot just taken" recovery; self-contained `CcApi` seam=fetch default, tests inject fake) + `CallHistory`. Registered permissions/catalog/registry/DISPLAY_NAMES.
- 15.6 `(15.6)` PHI-free `KpiService` (calls handled, AHT, FCR, verify-fail + abandoned rates, appt actions, reason mix) + GET /call-centre/kpis (supervisor/manager only; agent→403). **E2E journey test**: open→search-by-phone→verify(2 types)→360(no clinical)→book→cancel(reason)→close asserts correlated event chain + appt links + clinical-free notification (by call_ref) + KPIs reflect activity. Infra `8f7b249`: compose callcentre-service :8107 + Kong routes /api/v1/call-interactions+/api/v1/call-centre.

GOTCHAs: minimal-API needs `ConfigureHttpJsonOptions(JsonStringEnumConverter)` (SPA sends string enums else 500 on body bind). WebApplicationFactory endpoint tests reuse hello's TestAuthHandler pattern (`role`/`scope`/`tenant_id`/`amr` claims) + inject fake seams + env-gated live DB. StatusChip prop is `kind` (ok/info/part/warn/bad/neu) NOT tone; InputField does NOT forward ref. Projection authz test: avoid substring "lab" (⊂ "Label") — use "labresult". **Next: Phase 11** (hardening/NFR), then 12/13; 10b Claims still pending.
